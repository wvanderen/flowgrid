---
phase: 04-core-alternation-and-rejuvenation-economy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/records.ts
  - src/domain/ids.ts
  - src/domain/result.ts
  - src/domain/invariants.ts
  - src/content/formulas.ts
  - src/content/starter-state.ts
  - src/content/index.ts
  - src/persistence/repository.ts
  - src/persistence/import-validation.ts
  - src/simulation/commands/log-rejuvenation.ts
  - src/simulation/commands/start-rejuvenation.ts
  - src/simulation/commands/cancel-rejuvenation.ts
  - src/simulation/commands/purchase-activation-boost.ts
  - src/simulation/commands/start-focus-session.ts
  - src/simulation/commands/complete-focus-session.ts
  - src/simulation/engine.ts
  - src/simulation/economy-events.ts
  - src/simulation/operation-events.ts
  - tests/simulation/rejuvenation.test.ts
  - tests/simulation/activation-boost.test.ts
  - tests/properties/rejuvenation-safety.property.test.ts
  - tests/simulation/foundation-loop.test.ts
autonomous: true
requirements: [CORE-06, REJ-01, REJ-02, REJ-03, REJ-04]

must_haves:
  truths:
    # From SPEC R3 acceptance (amended / duration-gated) + Edge Coverage R1 rows (covered)
    - "log_rejuvenation with 100 Core Charge and >=10 min rest yields 50 Integration and 0 remaining Core Charge (full processing)"
    - "log_rejuvenation with 100 Core Charge and 5 min rest yields 25 Integration and leaves 50 Core Charge (partial, capped by duration)"
    - "log_rejuvenation with 101 Core Charge and >=11 min rest yields 50 Integration and retains 1 Core Charge (odd remainder not lost)"
    - "log_rejuvenation with 0 Core Charge and any duration applies and appends a RejuvenationRecord with chargeConsumed=0, integrationGained=0, tokensGranted=0 (no-op rest)"
    - "chargeConsumed equals integrationGained times 2 (consumption snaps to the even 2:1 grid; odd remainder retained)"
    - "chargeProcessedRaw equals min(coreCharge, floor(durationMinutes times REJUVENATION_CHARGE_PER_MINUTE))"
    # From SPEC R4 acceptance + Edge Coverage R2 rows (covered)
    - "Integration crossing the first threshold (>=50) grants exactly 1 Module Token; crossing from 0 to >=125 grants exactly 2 tokens (multi-threshold grant)"
    - "Integration and Module Tokens never decrease across any command sequence"
    - "The threshold sequence is 50, 75, 112, 168, 252 for the first five tokens (each Math.floor-ed)"
    - "A rejuvenation that brings Integration exactly to nextThreshold grants the token (>= comparison)"
    # From SPEC R5 acceptance + Edge Coverage R3 rows (covered)
    - "At level 0 with >=50 Energy, purchase_activation_boost succeeds: Energy decreases by 50, activationBoostLevel becomes 1"
    - "After purchasing boost level 1, the next activated focus session earns +15% Current (derived 10 + 1*5)"
    - "Purchasing a 4th level (at cap 3) is rejected with unchanged state; purchasing with Energy below the next level's cost is rejected with unchanged state"
    - "0 Energy cannot afford level 1 and is rejected"
    # From SPEC R6 + D-01/D-02 (live-timed trio, mutual exclusion) + Edge Coverage R4 (covered)
    - "start_rejuvenation sets a durable activeRejuvenationStartedAt marker and is mutually exclusive with focus; cancel_rejuvenation clears the marker and writes nothing durable"
    - "Replaying the same log_rejuvenation operationId does not duplicate the RejuvenationRecord (id is 1:1 with operationId)"
    - "Every finished log_rejuvenation appends exactly one RejuvenationRecord; cancel_rejuvenation appends none"
    # Determinism / boundary
    - "log_rejuvenation and purchase_activation_boost are exactly replayable (same inputs -> identical SimulationResult)"
    - "All new simulation code imports only src/domain, src/content, src/simulation and uses env.now for time (boundary scanner stays green)"
  artifacts:
    - path: "src/domain/records.ts"
      provides: "CoreRecord +activationBoostLevel +activeRejuvenationStartedAt; RejuvenationRecord interface; rejuvenations on FlowgridSnapshot"
      contains: "interface RejuvenationRecord"
    - path: "src/simulation/commands/log-rejuvenation.ts"
      provides: "Duration-gated rejuvenation finish handler (charge->integration->threshold->token grant loop)"
      exports: ["logRejuvenation"]
    - path: "src/simulation/commands/start-rejuvenation.ts"
      provides: "Sets activeRejuvenationStartedAt marker, enforces cross-type mutual exclusion"
      exports: ["startRejuvenation"]
    - path: "src/simulation/commands/cancel-rejuvenation.ts"
      provides: "Clears marker, writes nothing durable"
      exports: ["cancelRejuvenation"]
    - path: "src/simulation/commands/purchase-activation-boost.ts"
      provides: "Energy-spend upgrade (validate cost/cap -> decrement Energy -> increment level)"
      exports: ["purchaseActivationBoost"]
    - path: "src/content/formulas.ts"
      provides: "nextIntegrationThreshold + activationBoostCost + activationBonusPercent + content constants"
      contains: "nextIntegrationThreshold"
    - path: "tests/simulation/rejuvenation.test.ts"
      provides: "Handler acceptance tests (duration-gating, thresholds, no-op rest, multi-grant, replay)"
    - path: "tests/properties/rejuvenation-safety.property.test.ts"
      provides: "fast-check invariants + cross-type one-active-session property"
  key_links:
    - from: "src/simulation/engine.ts"
      to: "src/simulation/commands/log-rejuvenation.ts"
      via: "dispatch case 'log_rejuvenation' calls logRejuvenation (replaces logRejuvenationNotImplemented stub)"
      pattern: "logRejuvenation"
    - from: "src/simulation/commands/log-rejuvenation.ts"
      to: "src/content/formulas.ts"
      via: "imports REJUVENATION_CHARGE_PER_MINUTE + nextIntegrationThreshold"
      pattern: "nextIntegrationThreshold"
    - from: "src/simulation/commands/complete-focus-session.ts"
      to: "src/content/formulas.ts"
      via: "Activation bonus now derived via activationBonusPercent(core.activationBoostLevel)"
      pattern: "activationBonusPercent"
    - from: "src/simulation/commands/start-focus-session.ts"
      to: "src/domain/records.ts"
      via: "mutual-exclusion check reads core.activeRejuvenationStartedAt"
      pattern: "activeRejuvenationStartedAt"
  prohibitions:
    - statement: "MUST NOT grant Integration or Module Tokens from a rejuvenation when Core Charge is 0 (rest-farming exploit guard, REJ-03)"
      status: resolved
      verification: test — assert 0-Charge rejuvenation yields integrationGained=0, tokensGranted=0
    - statement: "MUST NOT grant a Module Token for the same Integration threshold more than once (no token duplication)"
      status: resolved
      verification: test — assert each threshold crossing grants at most one token and derived threshold advances so it cannot re-grant
    - statement: "MUST NOT reset Integration progress or the threshold sequence (parallels the forge-count-no-reset prestige rule)"
      status: resolved
      verification: test — assert Integration/moduleTokens are monotonic across commands; threshold derives from monotonic counter
    - statement: "MUST NOT grant an upgrade level without deducting its full Energy cost (no free levels)"
      status: resolved
      verification: test — assert level increment implies Energy decremented by exact cost (50/100/200)
    - statement: "MUST NOT mutate or delete RejuvenationRecord rows after creation (history is sacred / append-only)"
      status: resolved
      verification: test — assert records unchanged across subsequent commands and identical on replay (id 1:1 with operationId)
---

## Phase Goal

**As a** Flowgrid player, **I want to** route my focused effort through the Core, choose between spendable Energy and stored Charge, and convert rest into Integration and Module Tokens, **so that** both activity and recovery produce visible, useful progression in a forgiving loop.

---

<objective>
This plan delivers the simulation-truth slice of Phase 4. After it completes, a developer can run `npx vitest run` and exercise the full activity/rest alternation loop deterministically: start/finish rejuvenation, see duration-gated Core Charge convert into Integration, watch geometric thresholds grant Module Tokens, spend Energy on a 3-level Activation-bonus upgrade, and confirm focus and rejuvenation are mutually exclusive app-wide.

This plan does NOT touch persistence (migration/repository/export) or UI — those are plans 04-02 and 04-03. The `rejuvenations` array lives in the in-memory snapshot here; durability is wired in 04-02.

Purpose: Establish the pure, deterministic, invariant-safe economy truth that 04-02 persists and 04-03 surfaces. Simulation owns truth; this is the foundation vertical slice (mirrors Phase 3 plan 03-01).
Output: 4 new command handlers, RejuvenationRecord + CoreRecord field extensions, content constants/derived functions, dispatch wiring, and the simulation + property tests that prove every SPEC acceptance criterion.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-SPEC.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-CONTEXT.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-RESEARCH.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain records + content foundation (compile-green type layer)</name>
  <read_first>
    - src/domain/records.ts (CoreRecord lines 54-65, FlowgridSnapshot lines 132-142 — being extended)
    - src/domain/result.ts (LogRejuvenationCommand lines 51-55 to refactor; SimulationCommand union lines 116-127; ECONOMY_EVENT_NAMES lines 155-165; StartFocusSessionCommand lines 104-108 as the startedAt/operationId template)
    - src/domain/ids.ts (SessionId/ForgeHistoryId lines 17-20, EntityType lines 23-34)
    - src/domain/invariants.ts (validateMonotonicCounters lines 213-229, validateNoNegativeResources core block lines 63-76)
    - src/content/formulas.ts (ACTIVATION_CURRENT_BONUS_PERCENT line 26, splitCoreCurrent lines 59-69 — the integer-math + pure-function precedent)
    - src/content/starter-state.ts (core seed block lines 107-118)
    - src/content/index.ts (formulas re-export block lines 12-31)
    - src/persistence/repository.ts (loadSnapshot return object lines 160-170 — compile shim target)
    - src/persistence/import-validation.ts (snapshot construction lines 75-87 — compile shim target)
  </read_first>
  <files>src/domain/records.ts, src/domain/ids.ts, src/domain/result.ts, src/domain/invariants.ts, src/content/formulas.ts, src/content/starter-state.ts, src/content/index.ts, src/persistence/repository.ts, src/persistence/import-validation.ts</files>
  <action>
Extend the type/content layer so Task 2's handlers compile, and shim the two persistence snapshot-construction sites so `npx tsc --noEmit` stays green across the whole project at this task boundary. All edits are additive.

src/domain/records.ts (per D-02 marker-on-Core choice + CORE-06 upgrade level + SPEC R6 append-only record):
- Add two fields to CoreRecord: `readonly activationBoostLevel: IntNonNegative;` and `readonly activeRejuvenationStartedAt: IsoDateTimeString | null;` (place them after `forgeCount`, before `updatedAt`).
- Add a new `RejuvenationRecord` interface mirroring SessionRecord: `readonly id: RejuvenationId; readonly startedAt: IsoDateTimeString; readonly endedAt: IsoDateTimeString; readonly durationSeconds: IntSeconds; readonly chargeConsumed: IntNonNegative; readonly integrationGained: IntNonNegative; readonly tokensGranted: IntNonNegative; readonly createdAt: IsoDateTimeString;`
- Add `readonly rejuvenations: readonly RejuvenationRecord[];` to FlowgridSnapshot (parallel to `sessions`).

src/domain/ids.ts:
- Add `export type RejuvenationId = string;` (plain alias, consistent with the documented Phase 1 decision — NOT branded).
- Add `'rejuvenation'` to the `EntityType` union.

src/domain/result.ts (REFACTOR the stub command; add 3 new commands per the D-01/D-02 trio + CORE-06):
- Refactor `LogRejuvenationCommand` from `{ type, operationId, durationSeconds }` to `{ type: 'log_rejuvenation'; operationId: OperationId; startedAt: IsoDateTimeString; endedAt: IsoDateTimeString; }` (mirrors CompleteFocusSessionCommand timing fields; D-04 diff-for-truth derives duration at finish). Safe to change — the current handler returns not_implemented so no real data depends on the old shape.
- Add `StartRejuvenationCommand { type: 'start_rejuvenation'; operationId: OperationId; }`, `CancelRejuvenationCommand { type: 'cancel_rejuvenation'; operationId: OperationId; }`, and `PurchaseActivationBoostCommand { type: 'purchase_activation_boost'; operationId: OperationId; }`.
- Add all four (LogRejuvenationCommand, StartRejuvenationCommand, CancelRejuvenationCommand, PurchaseActivationBoostCommand) to the `SimulationCommand` union.
- Add economy event name constants to `ECONOMY_EVENT_NAMES`: `rejuvenationCompleted: 'rejuvenation_completed'`, `tokenGranted: 'token_granted'`, `activationBoostPurchased: 'activation_boost_purchased'`.

src/domain/invariants.ts:
- Extend `validateNoNegativeResources` to also assert `core.activationBoostLevel >= 0`.
- Extend the monotonic-counter guard (`validateMonotonicCounters` / token_regression block) to assert `integration` never decreases and `activationBoostLevel` never decreases across previous->next. Reuse the existing `token_regression`/monotonic pattern verbatim for `integration_regression` and `activation_boost_regression` (same code shape, different field/issue label).

src/content/formulas.ts (constants + pure derived functions; per D-03/SPEC R3 + CORE-06 + Don't-Hand-Roll derive-the-threshold):
- Add `export const REJUVENATION_CHARGE_PER_MINUTE = 10;`
- Add `export const INTEGRATION_THRESHOLD_BASE = 50;` and `export const INTEGRATION_THRESHOLD_RATIO = 1.5;` (multiplier only inside Math.floor; float never persists).
- Add `export function nextIntegrationThreshold(moduleTokens: number): number { return Math.floor(INTEGRATION_THRESHOLD_BASE * Math.pow(INTEGRATION_THRESHOLD_RATIO, moduleTokens)); }` — derived from the monotonic counter (no persisted nextThreshold field; cannot drift).
- Add `export const ACTIVATION_BOOST_PER_LEVEL = 5;`, `export const ACTIVATION_BOOST_MAX_LEVEL = 3;`, `export const ACTIVATION_BOOST_COSTS = [50, 100, 200] as const;` (cost for level 1, 2, 3).
- Add `export function activationBoostCost(currentLevel: number): number | null { return currentLevel < ACTIVATION_BOOST_COSTS.length ? ACTIVATION_BOOST_COSTS[currentLevel] : null; }` (null = at cap).
- Add `export function activationBonusPercent(level: number): number { return ACTIVATION_CURRENT_BONUS_PERCENT + level * ACTIVATION_BOOST_PER_LEVEL; }` (10/15/20/25 for levels 0/1/2/3).

src/content/starter-state.ts:
- Seed the two new CoreRecord fields in the core factory: `activationBoostLevel: 0, activeRejuvenationStartedAt: null`.
- Add `rejuvenations: []` to the `buildStarterSnapshot` return object (the snapshot now requires it; it starts empty).

src/content/index.ts:
- Re-export every new constant and function added to formulas.ts (extend the existing formulas re-export block).

Compile shim — the new required `FlowgridSnapshot.rejuvenations` field ripples to every snapshot constructor. The simulation handlers inherit it via `...previousState` spread (no edit needed there), but two persistence constructors build snapshots fresh from DB/archive arrays and must be given the field so tsc passes now (the Dexie `rejuvenations` store and the archive field arrive in plan 04-02, which replaces these shims with real reads):
- src/persistence/repository.ts loadSnapshot return object: add `rejuvenations: []` (the store does not exist yet; 04-02 swaps this for `await this.db.rejuvenations.toArray()`).
- src/persistence/import-validation.ts snapshot construction: add `rejuvenations: []` (the archive does not carry rejuvenations yet; 04-02 swaps this for `archive.rejuvenations`).

Design note (fold-vs-separate): per RESEARCH, the pure threshold/payout math lives in content/formulas.ts alongside the existing `splitCoreCurrent` precedent rather than a new systems/rejuvenation.ts file; the payout derivation (chargeProcessedRaw/integrationGained/chargeConsumed) is computed inline in the log-rejuvenation handler (Task 2) since it mutates economy state.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the new CoreRecord fields are seeded in starter-state so all snapshot construction stays well-typed).
    - `rg "interface RejuvenationRecord" src/domain/records.ts` matches exactly once.
    - `rg "activationBoostLevel" src/domain/records.ts` matches in CoreRecord; `rg "activeRejuvenationStartedAt" src/domain/records.ts` matches in CoreRecord.
    - `rg "readonly rejuvenations" src/domain/records.ts` matches once on FlowgridSnapshot.
    - `rg "type RejuvenationId" src/domain/ids.ts` matches once; `rg "'rejuvenation'" src/domain/ids.ts` matches once in EntityType.
    - `rg "nextIntegrationThreshold" src/content/formulas.ts` matches the function definition; `rg "REJUVENATION_CHARGE_PER_MINUTE" src/content/formulas.ts` matches the constant.
    - `rg "startedAt:" src/domain/result.ts` matches inside LogRejuvenationCommand (refactored shape); the old `durationSeconds` member is gone from LogRejuvenationCommand.
    - `rg "StartRejuvenationCommand|CancelRejuvenationCommand|PurchaseActivationBoostCommand" src/domain/result.ts` matches all three interface definitions plus three union members.
    - `rg "rejuvenation_completed|token_granted|activation_boost_purchased" src/domain/result.ts` matches all three event name constants.
    - `rg "activationBoostLevel: 0" src/content/starter-state.ts` matches once (seed default — Pitfall 6 backward-compat).
    - `rg "rejuvenations: \[\]" src/persistence/repository.ts src/persistence/import-validation.ts` matches once in each (compile shims keeping tsc green until 04-02 wires the store/archive).
  </acceptance_criteria>
  <done>
    Domain records, ids, command contracts, invariants, content constants/functions, starter seed, and barrel re-exports are extended. `npx tsc --noEmit` is green. Every symbol Task 2 imports exists and is typed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rejuvenation command trio + Activation boost + engine/event wiring</name>
  <behavior>
    - log_rejuvenation: 100 Charge + 600s rest -> 50 Integration, 0 Charge remaining
    - log_rejuvenation: 100 Charge + 300s rest -> 25 Integration, 50 Charge remaining (duration cap)
    - log_rejuvenation: 101 Charge + 660s rest -> 50 Integration, 1 Charge retained (odd remainder)
    - log_rejuvenation: 0 Charge + any duration -> applied, zero-gain RejuvenationRecord appended
    - log_rejuvenation: Integration reaching >=125 from 0 grants exactly 2 Module Tokens
    - start_rejuvenation: rejects when a focus session is active; sets marker; rejects when marker already set
    - start_focus_session: now rejects when core.activeRejuvenationStartedAt is non-null (mutual exclusion)
    - cancel_rejuvenation: clears marker, status applied, zero operations/events/records
    - purchase_activation_boost: level 0 + Energy 50 -> Energy 0, level 1; at cap 3 rejected; Energy below cost rejected
    - complete_focus_session after level-1 boost on an activated cell: Current bonus is +15% (not +10%)
    - All four handlers are exactly replayable (identical SimulationResult for identical inputs)
  </behavior>
  <read_first>
    - src/simulation/commands/set-core-allocation.ts (the canonical validate->apply->emit->return minimal template)
    - src/simulation/commands/complete-focus-session.ts (finish-session pattern: duration derivation lines 109-118, immutable core spread 191-192/230-237, record-append 195-209, the Activation bonus lines ~155-162 being MODIFIED)
    - src/simulation/commands/start-focus-session.ts (active-session marker + mutual-exclusion scan lines 62-78, set-marker mutation 84-96 — being MODIFIED to also check core marker)
    - src/simulation/commands/cancel-focus-session.ts (clear-marker, writes nothing durable — the cancel template)
    - src/simulation/engine.ts (dispatch switch 32-55 + logRejuvenationNotImplemented stub 58-68 — being replaced)
    - src/simulation/operation-events.ts (entityTypeForCommand 17-42 + entityIdForCommand 44-69 — two exhaustive switches to extend)
    - src/simulation/economy-events.ts (make helper 19-27 + focusSessionCompletedEvent 29-39 — the constructor template)
    - src/domain/result.ts (the four new command interfaces from Task 1)
    - src/domain/records.ts (RejuvenationRecord + CoreRecord new fields from Task 1)
    - src/content/formulas.ts (REJUVENATION_CHARGE_PER_MINUTE, nextIntegrationThreshold, activationBoostCost, activationBonusPercent from Task 1)
  </read_first>
  <files>src/simulation/commands/log-rejuvenation.ts, src/simulation/commands/start-rejuvenation.ts, src/simulation/commands/cancel-rejuvenation.ts, src/simulation/commands/purchase-activation-boost.ts, src/simulation/commands/start-focus-session.ts, src/simulation/commands/complete-focus-session.ts, src/simulation/engine.ts, src/simulation/economy-events.ts, src/simulation/operation-events.ts</files>
  <action>
Create the four command handlers following the validate->apply->emit->return shape (PATTERNS S1-S3, S7), wire them into the dispatcher (S5), and modify complete_focus_session's bonus derivation + start_focus_session's mutual-exclusion check. Copy the per-file `rejectWith` helper verbatim into each new handler (codebase convention; do not extract a shared import). All time comes from `env.now` and the command's startedAt/endedAt — NEVER `Date.now()`/`new Date()` in a way that reads the wall clock at runtime except parsing the command's ISO strings (boundary scanner S6 enforces this).

NEW src/simulation/commands/log-rejuvenation.ts (REJ-01/02/03/04, D-03/D-04, SPEC R3 amended):
- Derive `durationSeconds = Math.floor((new Date(command.endedAt).getTime() - new Date(command.startedAt).getTime()) / 1000)` and `durationMinutes = Math.floor(durationSeconds / 60)` from the command's startedAt/endedAt (diff-for-truth, D-04).
- Compute the payout EXACTLY in this order (Pitfall 3 — collapsing steps loses the odd remainder): `chargeProcessedRaw = Math.min(prev.core.coreCharge, Math.floor(durationMinutes * REJUVENATION_CHARGE_PER_MINUTE))`; `integrationGained = Math.floor(chargeProcessedRaw / 2)`; `chargeConsumed = integrationGained * 2` (NOT chargeProcessedRaw).
- Threshold-grant loop (Pitfall 4 — derive from monotonic counter so it is bounded and self-advancing): `let moduleTokens = prev.core.moduleTokens; while (coreAfterCharge.integration >= nextIntegrationThreshold(moduleTokens)) { moduleTokens += 1; }`. Because nextIntegrationThreshold derives from moduleTokens and moduleTokens increments each iteration, the loop terminates.
- Build the new CoreRecord: `coreCharge: prev.core.coreCharge - chargeConsumed`, `integration: prev.core.integration + integrationGained`, `moduleTokens`, `activeRejuvenationStartedAt: null` (finish clears the marker), `updatedAt: env.now`.
- Append exactly one RejuvenationRecord with `id: command.operationId` (1:1 with operationId — idempotent replay), `tokensGranted: moduleTokens - prev.core.moduleTokens`, and the derived timing fields. ALWAYS append (including 0-Charge no-op rest).
- Emit one SyncOperation via `operationFromCommand(command, env.now, { entityId: prev.core.id, payload: { durationSeconds, chargeConsumed, integrationGained, tokensGranted } })` and the `rejuvenation_completed` economy event (+ `token_granted` when tokensGranted > 0).
- Return status 'applied' with nextState carrying the new core + appended rejuvenations + appended operations + bumped client.updatedAt.

NEW src/simulation/commands/start-rejuvenation.ts (D-01/D-02, Pitfall 2):
- Reject (issues via `invalid_reference`, entityType 'core') when ANY cell has a non-null `activeSessionStartedAt` (scan previousState.cells) OR when `previousState.core.activeRejuvenationStartedAt !== null` already. This is the cross-type mutual-exclusion check.
- On pass: set `activeRejuvenationStartedAt: env.now` on the core (immutable spread), emit one operation, return applied.

NEW src/simulation/commands/cancel-rejuvenation.ts (mirrors cancel-focus-session):
- Reject when `previousState.core.activeRejuvenationStartedAt === null` (nothing to cancel).
- On pass: clear the marker (`activeRejuvenationStartedAt: null`), return status 'applied' with NO operation, NO economy event, NO visual event, NO record (the critical cancel contract — `operations: []`, `economyEvents: []`).

NEW src/simulation/commands/purchase-activation-boost.ts (CORE-06, per set-core-allocation template):
- `const level = prev.core.activationBoostLevel; const cost = activationBoostCost(level);`
- If `cost === null`: reject with `invalid_reference` issue "already at maximum boost level (3)". Else if `prev.core.energy < cost`: reject with `invalid_reference` issue "energy below cost".
- On pass: new core with `energy: prev.core.energy - cost`, `activationBoostLevel: level + 1`, emit one operation + `activation_boost_purchased` event.

MODIFY src/simulation/commands/start-focus-session.ts (Pitfall 2 — symmetric check): inside the existing one-active-session scan block (after the cell loop, before the `issues.length > 0` check), add a rejection when `previousState.core.activeRejuvenationStartedAt !== null` (issue: a rejuvenation session is already in progress). Do not change any other behavior.

MODIFY src/simulation/commands/complete-focus-session.ts (CORE-06 wiring): replace the Activation bonus derivation (the line(s) using `ACTIVATION_CURRENT_BONUS_PERCENT` for the activated-cell bonus) with `activationBonusPercent(prev.core.activationBoostLevel)`. Because the migration (plan 04-02) and starter-state (Task 1) default the level to 0, level-0 behavior is byte-identical to Phase 3 (10%) — existing activation-bonus tests stay green (Pitfall 6).

MODIFY src/simulation/engine.ts: add three import statements (logRejuvenation, startRejuvenation, cancelRejuvenation, purchaseActivationBoost) and three dispatch cases; replace the `case 'log_rejuvenation': return logRejuvenationNotImplemented(...)` line with `return logRejuvenation(previousState, command, env);` and add `case 'start_rejuvenation'`, `case 'cancel_rejuvenation'`, `case 'purchase_activation_boost'`. DELETE the `logRejuvenationNotImplemented` function (the stub is gone). The `RunForgeCommand`/`InstallModuleCommand` imports and their not-implemented stubs REMAIN (Phase 5).

MODIFY src/simulation/operation-events.ts: add cases for all four new command types to BOTH exhaustive switches (`entityTypeForCommand` and `entityIdForCommand`). The compiler errors until every case is covered (S5). For log_rejuvenation/start_rejuvenation/cancel_rejuvenation/purchase_activation_boost return entityType 'core' and entityId the core id. (cancel_rejuvenation never calls operationFromCommand from its handler, but the switch still needs a case to compile.)

MODIFY src/simulation/economy-events.ts: add `rejuvenationCompletedEvent`, `tokenGrantedEvent`, and `activationBoostPurchasedEvent` constructors following the existing `make()` + `focusSessionCompletedEvent` pattern, using the event-name constants added in Task 1.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npx vitest run tests/simulation/boundaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (all exhaustive switches — engine.ts, operation-events.ts x2 — have cases for the 4 new command types).
    - `npx vitest run tests/simulation/boundaries.test.ts` passes (no new simulation file imports DOM/React/Pixi/Dexie/Zustand or references Date.now/setTimeout; all time via env.now / command strings).
    - `rg "logRejuvenationNotImplemented" src/simulation/engine.ts` matches ZERO times (stub removed).
    - `rg "case 'log_rejuvenation'|case 'start_rejuvenation'|case 'cancel_rejuvenation'|case 'purchase_activation_boost'" src/simulation/engine.ts` matches four times.
    - `rg "chargeConsumed = integrationGained" src/simulation/commands/log-rejuvenation.ts` matches once (Pitfall 3 odd-remainder formula, NOT chargeConsumed = chargeProcessedRaw).
    - `rg "while .* nextIntegrationThreshold" src/simulation/commands/log-rejuvenation.ts` matches once (derived-threshold grant loop, Pitfall 4).
    - `rg "id: command.operationId" src/simulation/commands/log-rejuvenation.ts` matches once (id 1:1 with operationId — idempotent).
    - `rg "activeRejuvenationStartedAt !== null" src/simulation/commands/start-focus-session.ts` matches once (Pitfall 2 symmetric mutual-exclusion check added).
    - `rg "activationBonusPercent" src/simulation/commands/complete-focus-session.ts` matches once (bonus now derived from level).
    - cancel-rejuvenation returns `operations: []` and `economyEvents: []` (no durable write on cancel).
  </acceptance_criteria>
  <done>
    Four handlers implemented and wired; start_focus_session and complete_focus_session modified; engine stub replaced; event + operation switches extended. tsc + eslint + boundary scanner green. Handlers are not yet exercised by acceptance tests (Task 3) but compile and type-check.
  </done>
</task>

<task type="auto">
  <name>Task 3: Simulation acceptance tests + rejuvenation-safety property tests</name>
  <read_first>
    - tests/simulation/session-lifecycle.test.ts (start/cancel/complete handler + replay + invariant template — the rejuvenation.test.ts analog)
    - tests/simulation/activation-bonus.test.ts (cost/cap/bonus-derivation assertions + buildFocusCommand helper — the activation-boost.test.ts analog)
    - tests/properties/economy-safety.property.test.ts (fast-check fc.integer + fc.assert + invariant assertions — the property-test analog)
    - tests/helpers/fixtures.ts (buildStarterSnapshot, createTestSimulationEnv, createTestIds)
    - tests/helpers/replay.ts (expectReplayEqual)
    - tests/helpers/expect-valid-state.ts (expectValidState)
    - tests/simulation/foundation-loop.test.ts (may assert log_rejuvenation returns not_implemented — being updated)
    - src/simulation/commands/log-rejuvenation.ts, src/simulation/commands/purchase-activation-boost.ts (handlers under test, from Task 2)
  </read_first>
  <files>tests/simulation/rejuvenation.test.ts, tests/simulation/activation-boost.test.ts, tests/properties/rejuvenation-safety.property.test.ts, tests/simulation/foundation-loop.test.ts</files>
  <action>
Write the tests that prove every SPEC R3/R4/R5 acceptance criterion and the economy-safety invariants. Use a local `snapshotWithCore(state, overrides)` helper (spread `state.core`) mirroring the `snapshotWithCell` precedent in session-lifecycle.test.ts.

NEW tests/simulation/rejuvenation.test.ts (SPEC R3 amended + R4 + R6):
- Full trio lifecycle: start_rejuvenation sets the marker (core.activeRejuvenationStartedAt non-null); log_rejuvenation clears it and appends a record; cancel_rejuvenation clears it and appends nothing.
- Duration-gated cases (assert exact numbers): 100 Charge + startedAt/endedAt spanning 600s -> 50 Integration, 0 Charge remaining, 1 RejuvenationRecord with chargeConsumed=100; 100 Charge + 300s -> 25 Integration, 50 Charge remaining (duration cap); 101 Charge + 660s -> 50 Integration, 1 Charge retained (odd remainder — Pitfall 3); 0 Charge + any duration -> applied, record appended with chargeConsumed=0/integrationGained=0/tokensGranted=0 (REJ-03 no-op rest).
- Threshold grants: from 0 Integration, a rejuvenation bringing Integration to >=50 grants exactly 1 token; bringing to >=125 grants exactly 2 tokens (multi-threshold, R2 boundary); assert the appended record's tokensGranted equals the delta.
- Mutual exclusion (Pitfall 2): start_rejuvenation rejects when a cell has an active focus session; start_focus_session rejects when core.activeRejuvenationStartedAt is non-null (use the modified start-focus-session).
- Idempotent replay: assert the same log_rejuvenation operationId produces identical results via expectReplayEqual; a second identical command's record shares the id and is not duplicated beyond what idempotentAppend semantics allow at the simulation layer.
- Append-only: run several commands after a rejuvenation and assert every prior RejuvenationRecord is byte-identical (history is sacred — prohibition 5).
- Use `expectValidState(result.nextState)` on every applied result to exercise the new invariants (integration_regression, activation_boost_regression, non-negative activationBoostLevel).

NEW tests/simulation/activation-boost.test.ts (SPEC R5):
- Level 0 + Energy 50 -> purchase succeeds, Energy 0, level 1; replay equality.
- After level-1 purchase, run an activated-cell complete_focus_session and assert the Current bonus is +15% (not the Phase-3 +10%) — reuse the buildFocusCommand helper pattern.
- At cap: pre-seed level 3, purchase rejected with unchanged state (operations empty).
- Energy below cost: pre-seed Energy 49 at level 0, purchase rejected with unchanged state.
- Assert the first five thresholds via nextIntegrationThreshold equal [50,75,112,168,252] (documents the float-before-floor is always snapped — open question 3).
- Assert activationBonusPercent(0..3) equals [10,15,20,25].

NEW tests/properties/rejuvenation-safety.property.test.ts (Pitfalls 3,4 + cross-type Pitfall 2):
- fast-check over coreCharge in [0, 10000] and durationMinutes in [0, 600]: after log_rejuvenation assert status 'applied', validateNoNegativeResources empty, validateFlowgridSnapshot empty, integration/moduleTokens monotonic vs previous, and `chargeConsumed === integrationGained * 2` (Pitfall 3) and `chargeConsumed <= coreChargeBefore` and tokensGranted >= 0.
- Property: at most one active session app-wide — fast-check over a small command sequence (start_focus on a cell, start_rejuvenation, etc.) asserting that after each step AT MOST ONE of (any cell.activeSessionStartedAt, core.activeRejuvenationStartedAt) is non-null (cross-type one-active-session invariant, Pitfall 2).

UPDATE tests/simulation/foundation-loop.test.ts: any assertion that `log_rejuvenation` returns status `not_implemented` is now wrong (the stub is replaced by a real handler). Update those assertions to the new reality — a log_rejuvenation command now returns 'applied' (with a no-op/zero-gain record when Charge is 0) or 'rejected' per the new preconditions. Remove the not_implemented expectation for log_rejuvenation only; leave run_forge/install_module not_implemented expectations intact (still Phase 5 stubs). If the file does not assert on log_rejuvenation, no change is needed.
  </action>
  <verify>
    <automated>npx vitest run tests/simulation/rejuvenation.test.ts tests/simulation/activation-boost.test.ts tests/properties/rejuvenation-safety.property.test.ts tests/simulation/foundation-loop.test.ts tests/simulation/activation-bonus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The targeted vitest run passes (all new + updated suites green).
    - `npx vitest run tests/simulation/rejuvenation.test.ts` passes and includes assertions for all four SPEC R3 amended numeric cases (100/600s, 100/300s, 101/660s, 0-Charge) and the two multi-threshold grant cases (>=50 -> 1 token, >=125 -> 2 tokens).
    - `rg "chargeConsumed === integrationGained \* 2|chargeConsumed.*integrationGained.*2" tests/properties/rejuvenation-safety.property.test.ts` matches once (Pitfall 3 property assertion present).
    - `rg "nextIntegrationThreshold" tests/properties/rejuvenation-safety.property.test.ts tests/simulation/activation-boost.test.ts` matches (threshold derive + sequence assertion present).
    - The property test asserts the cross-type one-active-session invariant (at most one non-null marker app-wide after each step).
    - `npx vitest run tests/simulation/foundation-loop.test.ts` passes (no lingering log_rejuvenation not_implemented assertion).
    - `npx vitest run tests/simulation/activation-bonus.test.ts` still passes (level-0 backward compatibility — Pitfall 6).
  </acceptance_criteria>
  <done>
    Every SPEC R3/R4/R5/R6 simulation acceptance criterion has a passing test; the rejuvenation-safety property suite covers no-negative, monotonic integration/tokens, the odd-remainder identity, and cross-type mutual exclusion; the foundation-loop test no longer expects log_rejuvenation to be not_implemented; Phase 3 activation-bonus tests remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command input -> simulation | Untrusted command payload crosses into the deterministic engine; the 4 new commands must reject malformed/precondition-violating input with structured ValidationIssue[] (no throws). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | log_rejuvenation rest-farming (progress without prior activity) | mitigate | REJ-03 guard: 0 Core Charge -> no-op rest (zero-gain record); enforced in handler + property test (prohibition 1) |
| T-04-02 | Tampering | Token duplication via replayed operationId | mitigate | RejuvenationRecord.id is 1:1 with operationId; idempotent replay semantics; test asserts no duplication (prohibition 2) |
| T-04-03 | Tampering | Threshold re-grant (same Integration crosses a threshold twice) | mitigate | Derive threshold from monotonic moduleTokens; each grant increments the count, advancing the threshold (Pitfall 4) |
| T-04-04 | Tampering | Free upgrade level (Energy not decremented) | mitigate | purchase_activation_boost decrements Energy by exact cost before incrementing level; invariant + test (prohibition 4) |
| T-04-05 | Tampering | Negative Energy/Charge underflow | mitigate | validateNoNegativeResources (extended) + Zod nonnegative schemas (04-02); IntNonNegative types |
| T-04-06 | Repudiation | History mutation (editing a RejuvenationRecord) | mitigate | No update/delete path; records immutable after creation; append-only test (prohibition 5) |
| T-04-07 | Denial of Service | Same-tick infinite threshold-grant loop | mitigate | Derived threshold advances with each grant (bounded loop); property test asserts termination (Pitfall 4) |
| T-04-08 | Tampering | Cross-layer economy computation | mitigate | ESLint no-restricted-paths + boundary scanner (tests/simulation/boundaries.test.ts) — UI never computes economy (enforced in 04-03) |
</threat_model>

<verification>
- `npx tsc --noEmit` green (all exhaustive switches covered; new fields seeded).
- `npx eslint .` green (no boundary violations).
- `npx vitest run` green across all suites (rejuvenation, activation-boost, property, foundation-loop, activation-bonus, boundaries).
- `npm run build` succeeds.
</verification>

<success_criteria>
- The full rejuvenation -> Integration -> Module-Token loop and the Activation-boost upgrade are exercisable deterministically in simulation tests with every SPEC acceptance number reproduced exactly.
- Focus and rejuvenation are mutually exclusive app-wide (cross-type invariant property test green).
- No new packages installed; no external services; boundary scanner stays green; Phase 3 tests (activation-bonus, session-lifecycle, foundation-loop minus the log_rejuvenation stub assertion) remain green.
</success_criteria>

<output>
Create `.planning/phases/04-core-alternation-and-rejuvenation-economy/04-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 04-01)

**Domain (src/domain):**
- `RejuvenationId` (type alias), `'rejuvenation'` EntityType union member
- `CoreRecord.activationBoostLevel`, `CoreRecord.activeRejuvenationStartedAt` (new fields)
- `RejuvenationRecord` (new interface)
- `FlowgridSnapshot.rejuvenations` (new array)
- `StartRejuvenationCommand`, `CancelRejuvenationCommand`, `PurchaseActivationBoostCommand` (new interfaces); `LogRejuvenationCommand` (refactored to startedAt/endedAt)
- `ECONOMY_EVENT_NAMES.rejuvenationCompleted`, `.tokenGranted`, `.activationBoostPurchased`
- `integration_regression`, `activation_boost_regression` invariant checks; `activationBoostLevel` non-negative check

**Content (src/content):**
- `REJUVENATION_CHARGE_PER_MINUTE`, `INTEGRATION_THRESHOLD_BASE`, `INTEGRATION_THRESHOLD_RATIO`, `ACTIVATION_BOOST_PER_LEVEL`, `ACTIVATION_BOOST_MAX_LEVEL`, `ACTIVATION_BOOST_COSTS` (constants)
- `nextIntegrationThreshold(moduleTokens)`, `activationBoostCost(currentLevel)`, `activationBonusPercent(level)` (pure functions)

**Simulation (src/simulation):**
- `logRejuvenation`, `startRejuvenation`, `cancelRejuvenation`, `purchaseActivationBoost` (command handlers)
- `rejuvenationCompletedEvent`, `tokenGrantedEvent`, `activationBoostPurchasedEvent` (economy-event constructors)
- Dispatcher cases for `log_rejuvenation`/`start_rejuvenation`/`cancel_rejuvenation`/`purchase_activation_boost`; removed `logRejuvenationNotImplemented`
- Modified `startFocusSession` (rejuvenation mutual-exclusion) and `completeFocusSession` (derived activation bonus)

**Tests (tests):**
- `tests/simulation/rejuvenation.test.ts`, `tests/simulation/activation-boost.test.ts`, `tests/properties/rejuvenation-safety.property.test.ts`
