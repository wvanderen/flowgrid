---
phase: 04-core-alternation-and-rejuvenation-economy
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/persistence/database.ts
  - src/persistence/diff.ts
  - src/persistence/repository.ts
  - src/persistence/export-json.ts
  - src/persistence/import.ts
  - src/persistence/import-validation.ts
  - src/persistence/validation-schemas.ts
  - tests/persistence/migration-harness.test.ts
  - tests/persistence/repository.test.ts
  - tests/persistence/schema.test.ts
  - tests/persistence/export-json.test.ts
  - tests/persistence/import-replace.test.ts
  - tests/persistence/import-merge.test.ts
  - tests/persistence/import-validation.test.ts
autonomous: true
requirements: [REJ-01]

must_haves:
  truths:
    # SPEC R6 acceptance + Edge Coverage R4 (covered) — durability + idempotency
    - "A RejuvenationRecord produced by log_rejuvenation is persisted to the Dexie rejuvenations store and is present after a repository reload (loadSnapshot)"
    - "Replaying the same log_rejuvenation operationId does not create a duplicate record (idempotent append via idempotentAppend; id 1:1 with operationId)"
    - "A RejuvenationRecord survives JSON export then restore (rejuvenations round-trips through JsonArchive and importArchive)"
    # SPEC R5/R3 precision + migration
    - "The v2->v3 Dexie migration adds the rejuvenations store and defaults activationBoostLevel=0 and activeRejuvenationStartedAt=null on every existing CoreRecord (level-0 behavior stays byte-identical to Phase 3)"
    - "All nine existing stores are repeated verbatim in version(3).stores(...) plus the new rejuvenations store (Dexie requires the full store set)"
    - "ARCHIVE_VERSION is bumped to 2; a v1 archive (without rejuvenations) is accepted on import by injecting rejuvenations: []; a v2 archive exports rejuvenations"
    - "coreSchema validates activationBoostLevel and activeRejuvenationStartedAt; rejuvenationSchema mirrors RejuvenationRecord field-for-field"
    - "The repository has NO update/delete path for rejuvenations — only idempotent append (history is sacred)"
  artifacts:
    - path: "src/persistence/database.ts"
      provides: "version(3) migration: rejuvenations store + upgradeCoresV2ToV3 extracted transform"
      contains: "upgradeCoresV2ToV3"
    - path: "src/persistence/diff.ts"
      provides: "appendRejuvenations arm in FlowgridWritePlan + diffFlowgridSnapshots"
      contains: "appendRejuvenations"
    - path: "src/persistence/repository.ts"
      provides: "rejuvenations in ALL_STORE_NAMES, idempotent append in applyResult, store read in loadSnapshot"
    - path: "src/persistence/validation-schemas.ts"
      provides: "rejuvenationSchema + coreSchema new fields + archiveSchema accepting archiveVersion 1|2"
      contains: "rejuvenationSchema"
    - path: "tests/persistence/migration-harness.test.ts"
      provides: "upgradeCoresV2ToV3 extracted-transform fixture"
  key_links:
    - from: "src/persistence/repository.ts"
      to: "src/persistence/database.ts"
      via: "loadSnapshot reads db.rejuvenations; applyResult appends within the rw transaction spanning ALL_STORE_NAMES"
      pattern: "rejuvenations"
    - from: "src/persistence/import.ts"
      to: "src/persistence/validation-schemas.ts"
      via: "validateArchive (archiveSchema) gates every import before any write"
      pattern: "validateArchive"
    - from: "src/persistence/export-json.ts"
      to: "src/persistence/database.ts"
      via: "exportJson reads db.rejuvenations into the v2 archive envelope"
      pattern: "rejuvenations"
  prohibitions:
    - statement: "MUST NOT mutate or delete RejuvenationRecord rows after creation (history is sacred / append-only)"
      status: resolved
      verification: test — repository exposes no update/delete for rejuvenations; idempotentAppend is the only write path; round-trip + reload leave records byte-identical
---

<objective>
This plan makes the Phase 4 economy durable. It replaces the compile-shims that plan 04-01 added to `repository.ts` / `import-validation.ts` with real implementations, adds the Dexie v2->v3 migration (rejuvenations store + CoreRecord field defaults), wires the diff-write + repository append + export/import round-trip, and bumps ARCHIVE_VERSION to 2 (accepting v1 archives by injecting an empty rejuvenations array).

After this plan, a rejuvenation record survives reload, export, and restore, and a real IndexedDB database (or its fake-indexeddb test double) upgrades cleanly from v2 to v3.

Purpose: The durable spine extension. Simulation truth (04-01) is only useful if it persists; this plan closes that loop and preserves the "history is sacred" append-only contract.
Output: v3 migration + extracted transform, repository/diff rejuvenations handling, Zod schemas, export/import coverage, and the persistence tests proving reload + idempotency + round-trip + v1 backward-compat.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-SPEC.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-CONTEXT.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-RESEARCH.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-PATTERNS.md
@.planning/phases/04-core-alternation-and-rejuvenation-economy/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: v3 migration + diff/repository + export/import + Zod schemas (durable wiring)</name>
  <read_first>
    - src/persistence/database.ts (upgradeCellsV1ToV2 lines 44-64 + CELL_V2_DEFAULTS; version(2) declaration lines 99-111; the four-version-axes header comment lines 10-15 — the migration template)
    - src/persistence/diff.ts (FlowgridWritePlan lines 28-41, EMPTY_PLAN 43-56, diffAppend 82-85, diffFlowgridSnapshots 87-133)
    - src/persistence/repository.ts (ALL_STORE_NAMES 42-52, applyResult transaction 91-130, idempotentAppend 64-78, loadSnapshot 132-171 — note the 04-01 compile shim `rejuvenations: []` at ~line 170 to be replaced)
    - src/persistence/export-json.ts (ARCHIVE_VERSION line 38, JsonArchive 40-52, exportJson Promise.all 54-66)
    - src/persistence/import.ts (ImportStats 27-37, ALL_STORE_NAMES 44-54, replace tx 113-135, merge tx 140-168, statsFor 82-94)
    - src/persistence/import-validation.ts (snapshot construction lines 75-87 — note the 04-01 compile shim `rejuvenations: []` to be replaced)
    - src/persistence/validation-schemas.ts (coreSchema 51-62, sessionSchema 84-97, archiveSchema 125-137, drift-guard pattern 149-155)
    - src/domain/records.ts (RejuvenationRecord + CoreRecord new fields from 04-01)
  </read_first>
  <files>src/persistence/database.ts, src/persistence/diff.ts, src/persistence/repository.ts, src/persistence/export-json.ts, src/persistence/import.ts, src/persistence/import-validation.ts, src/persistence/validation-schemas.ts</files>
  <action>
Wire the durable spine for rejuvenations and the two new Core fields. Replace the two `rejuvenations: []` compile-shims added by plan 04-01 with real store/archive reads.

src/persistence/database.ts (Pattern 4 / S10 — extracted transform + verbatim store repetition + Pitfall 5 archive-version awareness):
- Add `export const CORE_V3_DEFAULTS = { activationBoostLevel: 0, activeRejuvenationStartedAt: null } as const;`
- Add `export function upgradeCoresV2ToV3(core: Record<string, unknown>): Record<string, unknown> { if (core.activationBoostLevel === undefined) core.activationBoostLevel = CORE_V3_DEFAULTS.activationBoostLevel; if (core.activeRejuvenationStartedAt === undefined) core.activeRejuvenationStartedAt = CORE_V3_DEFAULTS.activeRejuvenationStartedAt; return core; }` — exported pure function so the migration-harness can exercise it without IndexedDB.
- Add `this.version(3).stores({...})` repeating ALL existing nine stores VERBATIM from version(2) and ADDING `rejuvenations: 'id, createdAt'` (indexed by createdAt for recency queries). Append `.upgrade(async (tx) => { await tx.table('core').toCollection().modify(upgradeCoresV2ToV3); })`. Do NOT change the populate/blocked handlers.
- Update the header comment's "Currently v2" note to "Currently v3".

src/persistence/diff.ts:
- Add `readonly appendRejuvenations: readonly RejuvenationRecord[];` to FlowgridWritePlan (parallel to appendSessions).
- Add `appendRejuvenations: []` to EMPTY_PLAN.
- In diffFlowgridSnapshots add `const appendRejuvenations = diffAppend(previous.rejuvenations, next.rejuvenations, (r) => r.id);` and include it in the returned plan and the `isEmpty` check (so a no-op result still short-circuits to EMPTY_PLAN).

src/persistence/repository.ts:
- Add `'rejuvenations'` to the ALL_STORE_NAMES tuple (now ten stores).
- In applyResult's transaction, after the forgeHistory append loop, add a loop appending plan.appendRejuvenations via `idempotentAppend` — REUSE a conflict kind. Rejuvenation has no dedicated conflict kind in the existing union; mirror forgeHistory and use `'write_failure'` with label 'Rejuvenation' (documented, consistent with the union).
- In loadSnapshot, REPLACE the 04-01 `rejuvenations: []` shim with `const rejuvenations = await this.db.rejuvenations.toArray();` and return it in the snapshot. (Access `this.db.rejuvenations` — declare the table on FlowgridDatabase as `rejuvenations!: Table<RejuvenationRecord, RejuvenationId>;` in database.ts, mirroring the other Table declarations. Import the types.)

src/persistence/export-json.ts (Pitfall 5):
- Change `export const ARCHIVE_VERSION = 1;` to `2`.
- Add `readonly rejuvenations: readonly RejuvenationRecord[];` to JsonArchive and change `readonly archiveVersion: 1;` to `2`.
- In exportJson's Promise.all add `db.rejuvenations.toArray()` and include `rejuvenations` in the returned envelope.

src/persistence/import.ts:
- Add `'rejuvenations'` to the local ALL_STORE_NAMES tuple (ten stores).
- Add `rejuvenations: number;` to ImportStats and `rejuvenations: archive.rejuvenations.length` to statsFor.
- In replace mode: add `db.rejuvenations.clear()` to the clear batch and `db.rejuvenations.bulkPut(validated.rejuvenations)` to the write batch.
- In merge mode: add a loop `for (const rejuv of validated.rejuvenations) await idempotentMergeUpsert(db.rejuvenations, rejuv, 'write_failure', 'Rejuvenation');`.

src/persistence/import-validation.ts:
- REPLACE the 04-01 `rejuvenations: []` shim with `rejuvenations: archive.rejuvenations ?? []` (archiveSchema makes rejuvenations optional so a v1 archive without it parses, then defaults to empty — see below).

src/persistence/validation-schemas.ts (Pitfall 5 — version bump + v1 backward-compat):
- Add the two Core fields to coreSchema: `activationBoostLevel: z.number().int().nonnegative()` and `activeRejuvenationStartedAt: z.string().datetime().nullable()`.
- Add `export const rejuvenationSchema = z.object({ id: z.string(), startedAt: z.string().datetime(), endedAt: z.string().datetime(), durationSeconds: z.number().int().nonnegative(), chargeConsumed: z.number().int().nonnegative(), integrationGained: z.number().int().nonnegative(), tokensGranted: z.number().int().nonnegative(), createdAt: z.string().datetime() });` (mirror RejuvenationRecord field-for-field). Add a drift-guard `satisfies` line for it (parallel to the session/operation guards).
- In archiveSchema: change `archiveVersion: z.literal(1)` to `z.union([z.literal(1), z.literal(2)])`, and add `rejuvenations: z.array(rejuvenationSchema).optional()` (v1 archives omit it; v2 include it). This is what lets import-validation use `archive.rejuvenations ?? []`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the 04-01 shims are replaced with real reads; JsonArchive/archiveSchema agree).
    - `rg "export function upgradeCoresV2ToV3" src/persistence/database.ts` matches once.
    - `rg "this.version\(3\)" src/persistence/database.ts` matches once; the version(3).stores block includes all ten stores (rg "rejuvenations: 'id, createdAt'" matches once).
    - `rg "appendRejuvenations" src/persistence/diff.ts` matches in the interface, EMPTY_PLAN, diff body, return, and isEmpty (>=4 matches).
    - `rg "'rejuvenations'" src/persistence/repository.ts` matches in ALL_STORE_NAMES and loadSnapshot read; `rg "appendRejuvenations" src/persistence/repository.ts` matches in the append loop.
    - `rg "ARCHIVE_VERSION = 2" src/persistence/export-json.ts` matches once; `rg "rejuvenations" src/persistence/export-json.ts` matches in JsonArchive + Promise.all + return.
    - `rg "z.union\(\[z.literal\(1\), z.literal\(2\)\]\)" src/persistence/validation-schemas.ts` matches once (archiveVersion accepts 1 and 2).
    - `rg "rejuvenationSchema" src/persistence/validation-schemas.ts` matches the export definition and the archiveSchema reference.
    - `rg "activationBoostLevel|activeRejuvenationStartedAt" src/persistence/validation-schemas.ts` matches both in coreSchema.
    - `rg "rejuvenations: \[\]" src/persistence/repository.ts src/persistence/import-validation.ts` matches ZERO times (shims replaced).
  </acceptance_criteria>
  <done>
    v3 migration + extracted transform, diff/repository rejuvenations handling, v2 archive envelope + import round-trip, and Zod schemas (core fields + rejuvenationSchema + version 1|2) are in place. tsc + eslint green. Persistence tests (Task 2) prove reload/idempotency/round-trip.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistence tests — migration fixture, repository append, schema, export/import round-trip</name>
  <behavior>
    - upgradeCoresV2ToV3 defaults both new fields on a v2 core and preserves existing economy fields
    - repository.applyResult persists a new RejuvenationRecord to the rejuvenations store; reload returns it
    - replaying the same operationId does not duplicate the record (idempotent append)
    - version(3) declares the rejuvenations store (schema test)
    - exportJson produces archiveVersion 2 with rejuvenations; importArchive restores them (replace + merge)
    - a hand-built v1 archive (no rejuvenations) imports successfully with rejuvenations defaulted to empty
  </behavior>
  <read_first>
    - tests/persistence/migration-harness.test.ts (runMigrationFixture D-10 block lines 63-130 — the upgradeCoresV2ToV3 fixture template)
    - tests/persistence/repository.test.ts (existing diff-write + idempotent append patterns via fake-indexeddb)
    - tests/persistence/schema.test.ts (Dexie store-declaration assertions)
    - tests/persistence/export-json.test.ts (round-trip pattern)
    - tests/persistence/import-replace.test.ts, import-merge.test.ts, import-validation.test.ts (round-trip + v1 fixture patterns)
    - src/persistence/database.ts (upgradeCoresV2ToV3 + version(3) from Task 1)
    - src/persistence/repository.ts, src/persistence/export-json.ts, src/persistence/import.ts (Task 1 wiring)
    - tests/helpers/fixtures.ts (buildStarterSnapshot — now carries rejuvenations: [])
  </read_first>
  <files>tests/persistence/migration-harness.test.ts, tests/persistence/repository.test.ts, tests/persistence/schema.test.ts, tests/persistence/export-json.test.ts, tests/persistence/import-replace.test.ts, tests/persistence/import-merge.test.ts, tests/persistence/import-validation.test.ts</files>
  <action>
Extend the existing persistence suites (do not rewrite them) to cover rejuvenations and the v2->v3 migration. Use fake-indexeddb exactly as the existing repository/import tests do.

tests/persistence/migration-harness.test.ts: add a `describe('v2 -> v3 core migration (upgradeCoresV2ToV3)')` block with a runMigrationFixture mirroring the D-10 block. Input a v2 CoreRecord (with energy/coreCharge/integration/moduleTokens/etc. but WITHOUT activationBoostLevel/activeRejuvenationStartedAt); expected output is the same record PLUS `activationBoostLevel: 0` and `activeRejuvenationStartedAt: null`, with all pre-existing economy fields byte-identical (Pitfall 6).

tests/persistence/repository.test.ts: add cases — (a) after applying a log_rejuvenation result (build one via runSimulationCommand on a starter snapshot pre-seeded with coreCharge, or construct a minimal result), reload returns the RejuvenationRecord from the rejuvenations store; (b) applying the same result twice (same operationId) does NOT duplicate (idempotent append — second applyResult is a no-op); (c) the record's fields (chargeConsumed/integrationGained/tokensGranted/durationSeconds) match what the simulation produced.

tests/persistence/schema.test.ts: add an assertion that the FlowgridDatabase version(3) store declaration includes `rejuvenations` (mirror however the existing test asserts v2 stores — e.g. open a fresh fake-indexeddb db and check `db.rejuvenations` is a defined table).

tests/persistence/export-json.test.ts: add an assertion that exportJson returns `archiveVersion === 2` and that a known RejuvenationRecord round-trips (export a snapshot with one rejuvenation, assert it appears in archive.rejuvenations).

tests/persistence/import-replace.test.ts + import-merge.test.ts: add a round-trip case — export a snapshot containing a RejuvenationRecord, then importArchive it (replace and merge), reload, and assert the record is present and byte-identical. For merge, assert the idempotent upsert path (importing the same archive twice does not duplicate).

tests/persistence/import-validation.test.ts: add a case with a hand-built v1 archive object (archiveVersion: 1, NO rejuvenations field) and assert `validateArchive` returns zero issues (the optional field + union version accept it) and `importArchive` succeeds with `stats.rejuvenations === 0`. Also add a case for a v2 archive WITH a malformed rejuvenation record and assert it is rejected (invalid_operation_shape).
  </action>
  <verify>
    <automated>npx vitest run tests/persistence/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/persistence/` passes (all existing + new cases green).
    - The migration fixture asserts `activationBoostLevel === 0` and `activeRejuvenationStartedAt === null` after upgradeCoresV2ToV3 and that prior economy fields are unchanged.
    - A repository case asserts a RejuvenationRecord survives reload and the same operationId is not duplicated on replay.
    - The schema test asserts the `rejuvenations` store exists on version(3).
    - An export/import round-trip case asserts a RejuvenationRecord is preserved across export->restore.
    - An import-validation case asserts a v1 archive (no rejuvenations) imports with zero issues and a malformed v2 rejuvenation is rejected.
    - `rg "archiveVersion: 1" tests/persistence/import-validation.test.ts` matches at least once (the v1 backward-compat fixture exists).
  </acceptance_criteria>
  <done>
    The v2->v3 migration, repository append + reload + idempotency, v3 store declaration, export/import round-trip, and v1-archive backward-compat are all covered by passing tests. The full persistence suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted JSON archive -> local DB | Imported archives cross the Zod validation boundary (archiveSchema) before any write; v1/v2 envelope + every record shape is validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | Token/record duplication via replayed operationId at the persistence layer | mitigate | idempotentAppend byte-identical check (Phase 2 D-04); repository test asserts no duplication |
| T-04-10 | Repudiation | History mutation (update/delete RejuvenationRecord) | mitigate | Repository has NO update/delete path for rejuvenations; only idempotent append + replace-mode clear (prohibition 5) |
| T-04-11 | Tampering | v1 archive import silently dropping/malforming rejuvenations | mitigate | archiveSchema accepts version 1|2; v1 archives get rejuvenations defaulted to []; malformed v2 rejuvenation rejected by Zod (invalid_operation_shape) |
| T-04-12 | Tampering | Migration leaves old Core records without the level default (NaN bonus) | mitigate | upgradeCoresV2ToV3 defaults activationBoostLevel=0; migration-harness fixture asserts it (Pitfall 6) |
</threat_model>

<verification>
- `npx tsc --noEmit` green.
- `npx eslint .` green (persistence imports no simulation runtime).
- `npx vitest run tests/persistence/` green (migration, repository, schema, export/import, validation).
- `npm run build` succeeds.
</verification>

<success_criteria>
- RejuvenationRecords persist across reload, export, and restore with no duplication; the v2->v3 migration defaults the new Core fields; ARCHIVE_VERSION=2 accepts v1 archives; the append-only contract holds (no update/delete path).
</success_criteria>

<output>
Create `.planning/phases/04-core-alternation-and-rejuvenation-economy/04-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 04-02)

**Persistence (src/persistence):**
- `upgradeCoresV2ToV3(core)` + `CORE_V3_DEFAULTS` (extracted migration transform + defaults)
- Dexie `version(3)` declaration with the `rejuvenations` store; `FlowgridDatabase.rejuvenations` table property
- `FlowgridWritePlan.appendRejuvenations` (diff arm)
- `FlowgridRepository` rejuvenations append (idempotent) + loadSnapshot read; `'rejuvenations'` in ALL_STORE_NAMES
- `JsonArchive.rejuvenations`; `ARCHIVE_VERSION = 2`; archiveVersion `1 | 2`
- `rejuvenationSchema`; `coreSchema.activationBoostLevel` + `.activeRejuvenationStartedAt`; `archiveSchema` version union + optional rejuvenations
- `ImportStats.rejuvenations`; replace/merge rejuvenations handling

**Tests (tests/persistence):** extended migration-harness, repository, schema, export-json, import-replace, import-merge, import-validation suites
