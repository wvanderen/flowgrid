---
phase: 01-deterministic-foundation-slice
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/domain/ids.ts
  - src/domain/primitives.ts
  - src/domain/records.ts
  - src/domain/operation-records.ts
  - src/domain/time.ts
  - src/domain/validation.ts
  - src/domain/invariants.ts
  - src/domain/result.ts
  - src/domain/index.ts
  - src/content/content-version.ts
  - src/content/starter-modules.ts
  - src/content/starter-state.ts
  - src/content/formulas.ts
  - src/content/index.ts
  - src/simulation/deterministic-env.ts
  - src/simulation/economy-events.ts
  - src/simulation/visual-events.ts
  - src/simulation/operation-events.ts
  - src/simulation/index.ts
  - tests/simulation/command-results.test.ts
  - tests/simulation/validation.test.ts
autonomous: true
requirements:
  - FND-03
  - FND-04
  - SIM-08
  - MOD-01
  - VER-01
must_haves:
  truths:
    - "Domain records use stable typed IDs for Cells, modules, routes, sessions, Core, forge history, settings, and sync-ready operations."
    - "Static ModuleDefinitions are versioned content and are separate from user-owned ModuleInstances."
    - "Simulation command/result types expose changed state, economy events, visual events, operations, and validation issues."
  artifacts:
    - path: "src/domain/records.ts"
      provides: "FlowgridSnapshot and durable record shapes"
      contains: "FlowgridSnapshot"
    - path: "src/content/starter-modules.ts"
      provides: "versioned starter ModuleDefinitions"
      contains: "STARTER_MODULE_DEFINITIONS"
    - path: "src/domain/result.ts"
      provides: "SimulationCommand, SimulationEnv, and SimulationResult contracts"
      contains: "SimulationResult"
  key_links:
    - from: "src/content/starter-state.ts"
      to: "src/domain/records.ts"
      via: "starter state factory returns FlowgridSnapshot"
      pattern: "FlowgridSnapshot"
---

<objective>
Create the pure domain, content, command, event, operation, and validation contracts that the executable foundation loop will implement.

Purpose: Lock Flowgrid's simulation truth shape early so persistence, renderer, UI, and sync phases can consume the same records and command result surface without moving economy rules across boundaries.
Output: Typed domain records, versioned starter ModuleDefinitions, starter state factory, deterministic environment contracts, event types, operation records, and command/result shape tests.
</objective>

<execution_context>
@/Users/eggfam/.codex/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
@.planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
@docs/technical-vision-draft.md
@docs/gameplay-spine-draft.md
</context>

## Dependency Graph

| Plan | Needs | Creates | Wave |
|---|---|---|---|
| 01 | Phase source artifacts and package legitimacy confirmation | Tooling, source folders, first failing tests | 1 |
| 02 | Plan 01 tooling and failing tests | Domain records, command contracts, starter content | 2 |
| 03 | Plan 02 contracts and content | Executable foundation loop, invariants, property tests | 3 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define stable domain records and validation issue contracts</name>
  <files>src/domain/ids.ts, src/domain/primitives.ts, src/domain/records.ts, src/domain/operation-records.ts, src/domain/time.ts, src/domain/validation.ts, src/domain/invariants.ts, src/domain/index.ts, tests/simulation/validation.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
    .planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
    docs/technical-vision-draft.md
  </read_first>
  <behavior>
    - Stable branded ID types exist for Cells, ModuleDefinitions, ModuleInstances, ModuleSlots, Routes, Sessions, Core, ForgeHistory, Settings, Operations, and Client.
    - `FlowgridSnapshot` stores normalized record maps and core/settings records using integer resource fields.
    - `ValidationIssue` uses structured codes, severity, entity type, entity ID, and path fields.
  </behavior>
  <action>Implement FND-03 and the validation surface required by FND-05. Define branded string ID aliases, `IsoDateTimeString`, `LocalDateString`, integer resource aliases, `CellRecord`, `ModuleDefinition`, `ModuleInstance`, `RouteRecord`, `SessionRecord`, `CoreRecord`, `ForgeHistoryRecord`, `SettingsRecord`, `SyncOperation`, `OperationStatus`, `ValidationIssue`, `ValidationIssueCode`, and `FlowgridSnapshot`. Include fields needed by Phase 1 and later phases: stable IDs, `createdAt`, `updatedAt`, Cell XP/Momentum/Charge/daily milestone/Activation, Core Energy/Core Charge/allocation/Integration/Module Tokens/lifetime Energy, append-shaped sessions, forge count, and sync operation metadata. Keep these files pure TypeScript with no DOM, React, PixiJS, Dexie, persistence, or browser API imports.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run test -- --run tests/simulation/validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/records.ts` exports `FlowgridSnapshot`, `CellRecord`, `ModuleDefinition`, `ModuleInstance`, `RouteRecord`, `SessionRecord`, `CoreRecord`, `ForgeHistoryRecord`, and `SettingsRecord`.
    - `src/domain/operation-records.ts` exports `SyncOperation` with stable operation ID, command type, entity type, payload version, timestamps, status, and payload fields.
    - `tests/simulation/validation.test.ts` can import the domain contracts without importing simulation implementation.
  </acceptance_criteria>
  <done>Stable domain records, operation shape, ID/time primitives, and validation issues compile under strict TypeScript.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define command/result contracts and deterministic environment</name>
  <files>src/domain/result.ts, src/simulation/deterministic-env.ts, src/simulation/economy-events.ts, src/simulation/visual-events.ts, src/simulation/operation-events.ts, src/simulation/index.ts, tests/simulation/command-results.test.ts</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
    .planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
    docs/technical-vision-draft.md
  </read_first>
  <behavior>
    - `SimulationCommand` is a discriminated union with `complete_focus_session`, `set_core_allocation`, `log_rejuvenation`, `run_forge`, and `install_module`.
    - `SimulationResult` always includes `status`, `previousState`, `nextState`, `economyEvents`, `visualEvents`, `operations`, and `validationIssues`.
    - `SimulationEnv` carries injected `now`, `localDate`, `rng`, and `contentVersion`; no command uses ambient time.
  </behavior>
  <action>Implement FND-04, SIM-08, and D-02 through typed command/result contracts. Define `Rng`, `SimulationEnv`, `SimulationStatus`, `SimulationCommand`, `CompleteFocusSessionCommand`, `SetCoreAllocationCommand`, `LogRejuvenationCommand`, `RunForgeCommand`, `InstallModuleCommand`, `SimulationResult`, `EconomyEvent`, `VisualEvent`, and operation event helpers. Include event names for `focus_session_completed`, `current_generated`, `cell_xp_gained`, `bloom_fired`, `cell_activated`, `current_routed_to_core`, `core_current_converted`, `core_charge_stored`, `state_validated`, and typed result support for `not_implemented`. Ensure operation IDs are command inputs, not generated by simulation.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run test -- --run tests/simulation/command-results.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `SimulationCommand` includes the five command variants named in the action.
    - `SimulationEnv` has injected `now`, `localDate`, `rng`, and `contentVersion`.
    - `SimulationResult` contains all required arrays even for rejected and `not_implemented` results.
    - `tests/simulation/command-results.test.ts` asserts result shape for applied, rejected, and typed not-implemented outcomes.
  </acceptance_criteria>
  <done>Command/result contracts and deterministic environment types are exported and shape-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create versioned starter content and starter state factory</name>
  <files>src/content/content-version.ts, src/content/starter-modules.ts, src/content/starter-state.ts, src/content/formulas.ts, src/content/index.ts, tests/helpers/fixtures.ts, tests/simulation/command-results.test.ts</files>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
    docs/gameplay-spine-draft.md
    docs/technical-vision-draft.md
  </read_first>
  <behavior>
    - Starter content exports Generator, Charge Core, Output, and Bloom definitions under one content version.
    - `createStarterFlowgridState` returns one Cell, one Core, four starter ModuleInstances, a default route to Core, default allocation, settings, forge history, and empty sessions/operations.
    - ModuleDefinitions never contain user ownership fields; ModuleInstances reference definitions and owner Cell/slot IDs.
  </behavior>
  <action>Implement MOD-01 and prepare D-01/D-04 execution. Define `CONTENT_VERSION`, `STARTER_MODULE_DEFINITIONS`, `STARTER_CELL_TEMPLATE`, `DEFAULT_CORE_ALLOCATION`, integer formulas for focus-to-Current, focus-to-XP, Bloom threshold, and Core convert/store rounding. Implement `createStarterFlowgridState(params)` with deterministic IDs/timestamps supplied by the caller or tests. The returned state must include a starter Cell with Generator, Charge Core, Output, and Bloom ModuleInstances; a route from the Cell Output to Core; Core allocation totaling 100; zeroed Energy/Core Charge/Integration/Module Tokens; monotonic forge count represented; and settings containing default session length, daily target seconds, and local day boundary.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run test -- --run tests/simulation/command-results.test.ts tests/simulation/foundation-loop.test.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `STARTER_MODULE_DEFINITIONS` includes exactly one starter definition each for `generator`, `charge_core`, `output`, and `bloom`.
    - ModuleDefinitions have `version` and no `ownerCellId`, `installedSlotId`, `createdAt`, or `updatedAt` fields.
    - Starter ModuleInstances have stable IDs, `definitionId`, `ownerCellId`, `installedSlotId`, `createdAt`, and `updatedAt`.
    - `createStarterFlowgridState` is deterministic for the same input params.
  </acceptance_criteria>
  <done>Versioned starter definitions and deterministic starter state factory exist with definitions separated from instances.</done>
</task>

</tasks>

## Artifacts this phase produces

- Types: `CellId`, `ModuleDefinitionId`, `ModuleInstanceId`, `ModuleSlotId`, `RouteId`, `SessionId`, `CoreId`, `ForgeHistoryId`, `SettingsId`, `OperationId`, `ClientId`, `IsoDateTimeString`, `LocalDateString`.
- Records: `FlowgridSnapshot`, `CellRecord`, `ModuleDefinition`, `ModuleInstance`, `RouteRecord`, `SessionRecord`, `CoreRecord`, `ForgeHistoryRecord`, `SettingsRecord`, `SyncOperation`.
- Commands/results: `SimulationCommand`, `SimulationEnv`, `SimulationResult`, `CompleteFocusSessionCommand`, `SetCoreAllocationCommand`, `LogRejuvenationCommand`, `RunForgeCommand`, `InstallModuleCommand`.
- Events: `EconomyEvent`, `VisualEvent`, sync-ready operation event helpers.
- Content: `CONTENT_VERSION`, `STARTER_MODULE_DEFINITIONS`, `DEFAULT_CORE_ALLOCATION`, `createStarterFlowgridState`.

<must_haves>
- FND-03: stable typed record shapes exist for durable entities.
- FND-04: command result shape is stable and includes state, economy events, visual events, operations, and validation issues.
- SIM-08: deterministic environment and RNG interfaces exist.
- MOD-01: static ModuleDefinitions are versioned and separate from owned ModuleInstances.
- VER-01: unit tests assert command/result and validation surfaces.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test fixtures to domain records | Generated records can be malformed and must be validated before commands apply. |
| content definitions to simulation | Static content drives module behavior but must not store user-owned state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-02-01 | Tampering | `FlowgridSnapshot` records | mitigate | Structured validation issue codes and invariant functions in domain layer. |
| T-01-02-02 | Repudiation | `SyncOperation` shape | mitigate | Stable operation IDs and deterministic command payload metadata. |
| T-01-02-03 | Information Disclosure | domain/content files | accept | No secrets, accounts, or PII are introduced in Phase 1. |
</threat_model>

<verification>
Run `npm run typecheck`, `npm run lint`, and `npm run test -- --run tests/simulation/command-results.test.ts tests/simulation/validation.test.ts`. The full foundation-loop test may remain red until Plan 03 implements executable handlers.
</verification>

<success_criteria>
Domain records, starter content, deterministic env, command/result contracts, events, sync-ready operation shape, and validation issue types exist and compile under strict TypeScript without crossing architecture boundaries.
</success_criteria>

<output>
Create `.planning/phases/01-deterministic-foundation-slice/01-02-SUMMARY.md` when done.
</output>
