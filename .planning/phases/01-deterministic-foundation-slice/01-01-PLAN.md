---
phase: 01-deterministic-foundation-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - eslint.config.js
  - src/domain/index.ts
  - src/content/index.ts
  - src/simulation/index.ts
  - src/app/README.md
  - src/persistence/README.md
  - src/render/README.md
  - src/ui/README.md
  - tests/helpers/fixtures.ts
  - tests/helpers/replay.ts
  - tests/helpers/expect-valid-state.ts
  - tests/simulation/foundation-loop.test.ts
  - tests/simulation/boundaries.test.ts
autonomous: false
requirements:
  - FND-01
  - FND-02
  - VER-01
  - VER-02
user_setup:
  - service: npm package legitimacy
    why: "Phase 1 installs dev tooling; research flagged latest vitest, eslint, and prettier as too-new by package age."
    env_vars: []
    dashboard_config:
      - task: "Executor must ask for human confirmation of exact dev dependency versions before writing package.json or running npm install."
        location: "npmjs.com package pages for typescript, vitest, fast-check, eslint, and prettier if used"
must_haves:
  truths:
    - "Developer can run strict TypeScript, lint, and test scripts for the Flowgrid foundation."
    - "Simulation tests run in a Node-like environment without DOM, React, PixiJS, IndexedDB, persistence, or browser timer dependencies."
    - "A failing foundation-loop test exists before implementation work in dependent plans."
  artifacts:
    - path: "package.json"
      provides: "npm scripts and dev dependency declarations"
      contains: "\"typecheck\""
    - path: "tsconfig.json"
      provides: "strict TypeScript compiler settings"
      contains: "\"strict\": true"
    - path: "tests/simulation/foundation-loop.test.ts"
      provides: "walking skeleton acceptance test for the developer-facing foundation loop"
      contains: "complete_focus_session"
  key_links:
    - from: "tests/simulation/boundaries.test.ts"
      to: "src/simulation"
      via: "recursive source scan"
      pattern: "src/simulation"
---

<objective>
Create the Phase 1 developer walking skeleton harness: strict TypeScript project setup, separated source areas, npm verification commands, and the first failing tests that define the deterministic foundation loop and simulation boundary.

Purpose: Establish the executable environment that all later Phase 1 implementation plans must satisfy without introducing UI, renderer, persistence, browser, or timer dependencies.
Output: Tooling files, source directory entrypoints, boundary README fences, test helpers, and initial tests for the developer-facing MVP skeleton.
</objective>

<execution_context>
@/Users/eggfam/.codex/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
@.planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
@docs/technical-vision-draft.md
@docs/gameplay-spine-draft.md
</context>

## Phase Goal

**As a** developer, **I want to** run a strict, pure TypeScript Flowgrid simulation foundation, **so that** command contracts, starter content, deterministic inputs, and economy invariants are proven before UI, rendering, or persistence work begins.

## Dependency Graph

| Plan | Needs | Creates | Wave |
|---|---|---|---|
| 01 | Phase source artifacts and package legitimacy confirmation | Tooling, source folders, first failing tests | 1 |
| 02 | Plan 01 tooling and failing tests | Domain records, command contracts, starter content | 2 |
| 03 | Plan 02 contracts and content | Executable foundation loop, invariants, property tests | 3 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm dev dependency versions before package files are written</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    AGENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
    .planning/research/STACK.md
  </read_first>
  <action>Before writing `package.json`, present exact proposed versions for `typescript`, `vitest`, `fast-check`, `eslint`, and `prettier` if used. Explain that research marked `vitest`, `eslint`, and `prettier` as package-age suspicious, while `typescript` and `fast-check` were acceptable. Ask the user to approve exact versions or provide replacement pins. Do not run `npm install` until approved.</action>
  <verify>
    <automated>MISSING - blocking human checkpoint must record the approved package versions in the plan summary before package files are created.</automated>
  </verify>
  <acceptance_criteria>
    - The executor records approved exact package versions in `.planning/phases/01-deterministic-foundation-slice/01-01-SUMMARY.md`.
    - `package.json` and `package-lock.json` do not exist before approval unless the user explicitly pre-approved versions in the execution thread.
  </acceptance_criteria>
  <done>Approved exact dev dependency versions are recorded before package files or installs are created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create strict tooling and layer folders</name>
  <files>package.json, package-lock.json, tsconfig.json, vitest.config.ts, eslint.config.js, src/domain/index.ts, src/content/index.ts, src/simulation/index.ts, src/app/README.md, src/persistence/README.md, src/render/README.md, src/ui/README.md</files>
  <read_first>
    AGENTS.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
    .planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
    docs/technical-vision-draft.md
  </read_first>
  <behavior>
    - `npm run typecheck` invokes `tsc --noEmit`.
    - `npm run lint` enforces restricted imports from simulation into `react`, `react-dom`, `pixi.js`, `dexie`, `zustand`, `src/ui`, `src/render`, and `src/persistence`.
    - `npm run test -- --run` uses Vitest and discovers tests under `tests/**/*.test.ts` and `tests/**/*.property.test.ts`.
  </behavior>
  <action>Create the initial package and config surface per FND-01 and FND-02. Use `type: "module"`, scripts named `typecheck`, `lint`, and `test`, strict `tsconfig.json` options including `strict`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, and `noEmit`. Configure Vitest for non-browser unit/property tests. Configure ESLint import restrictions so `src/simulation` cannot import UI, render, persistence, React, PixiJS, Dexie, Zustand, DOM, or browser APIs. Create `src/domain`, `src/content`, `src/simulation`, `src/app`, `src/persistence`, `src/render`, and `src/ui`; only domain/content/simulation get public index exports now, while app/persistence/render/ui README files explicitly state their Phase 1 out-of-scope boundary.</action>
  <verify>
    <automated>npm install && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains scripts `typecheck`, `lint`, and `test`.
    - `tsconfig.json` has `strict`, `exactOptionalPropertyTypes`, and `noUncheckedIndexedAccess` set to `true`.
    - `src/domain`, `src/content`, `src/simulation`, `src/app`, `src/persistence`, `src/render`, and `src/ui` all exist.
    - `npm run typecheck` and `npm run lint` pass against the initial scaffold.
  </acceptance_criteria>
  <done>Strict tooling, source areas, and boundary READMEs exist and pass typecheck/lint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add walking skeleton tests and boundary scanner</name>
  <files>tests/helpers/fixtures.ts, tests/helpers/replay.ts, tests/helpers/expect-valid-state.ts, tests/simulation/foundation-loop.test.ts, tests/simulation/boundaries.test.ts</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
    .planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
    docs/gameplay-spine-draft.md
  </read_first>
  <behavior>
    - Foundation test initially fails until Plans 02 and 03 implement `createStarterFlowgridState`, `createTestSimulationEnv`, and `runSimulationCommand`.
    - Boundary test fails if simulation source imports forbidden packages/layers or references browser globals/timers.
    - Test helpers expose deterministic operation IDs, ISO timestamps, local dates, and RNG seeds without using ambient time.
  </behavior>
  <action>Create the first developer-facing MVP test harness per D-01, D-03, D-05, D-08, VER-01, and VER-02. `foundation-loop.test.ts` must assert starter state can run a `complete_focus_session` command with injected `now`, `localDate`, and RNG seed, then produce applied status, integer Current, XP, Bloom, Activation, Output route to Core, Energy, Core Charge, operations, visual events, and empty validation issues. `boundaries.test.ts` must recursively inspect `src/simulation/**/*.ts` and reject imports or source references to DOM/browser globals, React, PixiJS, IndexedDB, persistence, real timers, `Date.now`, `setTimeout`, and `setInterval`. Keep these tests as the executable contract for dependent plans.</action>
  <verify>
    <automated>npm run test -- --run tests/simulation/boundaries.test.ts</automated>
    <automated>npm run test -- --run tests/simulation/foundation-loop.test.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `tests/simulation/boundaries.test.ts` passes against the scaffold.
    - `tests/simulation/foundation-loop.test.ts` exists and fails only because the implementation symbols from Plans 02 and 03 do not exist yet.
    - No test calls `Date.now()`, `setTimeout`, `setInterval`, `window`, `document`, `localStorage`, or `indexedDB`.
  </acceptance_criteria>
  <done>Boundary scanner passes and the red foundation-loop test encodes the Phase 1 executable contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- Commands: `npm run typecheck`, `npm run lint`, `npm run test -- --run`.
- Config: `package.json`, `package-lock.json`, `tsconfig.json`, `vitest.config.ts`, `eslint.config.js`.
- Source areas: `src/domain`, `src/content`, `src/simulation`, `src/app`, `src/persistence`, `src/render`, `src/ui`.
- Test helpers: `createTestIds`, `createTestSimulationEnv`, `expectValidState`, `expectReplayEqual`.
- Tests: `foundation-loop.test.ts`, `boundaries.test.ts`.

<must_haves>
- FND-01: separated strict TypeScript project areas exist and scripts run.
- FND-02: boundary scanner and lint config prevent simulation from importing UI, rendering, persistence, DOM, React, PixiJS, IndexedDB, browser timers, or browser APIs.
- VER-01 and VER-02: first unit/property test harness exists, with implementation completed in dependent plans.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry to local dev environment | Dev dependency code enters the project during install. |
| test fixtures to simulation | Test-created commands mimic user/domain input and must not bypass invariants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01-SC | Tampering | npm installs | mitigate | Blocking package legitimacy checkpoint before writing package files or running install. |
| T-01-01-01 | Tampering | `src/simulation` imports | mitigate | ESLint restricted imports plus boundary source scanner. |
| T-01-01-02 | Repudiation | test command outputs | accept | Phase 1 has no user data; npm script exit codes are sufficient proof. |
</threat_model>

<verification>
Run `npm run typecheck`, `npm run lint`, and `npm run test -- --run tests/simulation/boundaries.test.ts`. The foundation-loop test is expected to be red until Plans 02 and 03 complete its referenced contracts and handler.
</verification>

<success_criteria>
The repository has a strict TypeScript/Vitest/ESLint harness, separated source areas, package versions approved through the checkpoint, and an executable failing foundation-loop test that encodes D-01, D-03, D-05, and D-08.
</success_criteria>

<output>
Create `.planning/phases/01-deterministic-foundation-slice/01-01-SUMMARY.md` when done.
</output>
