---
phase: 01-deterministic-foundation-slice
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/domain/invariants.ts
  - src/domain/validation.ts
  - src/simulation/commands/complete-focus-session.ts
  - src/simulation/commands/set-core-allocation.ts
  - src/simulation/commands/not-implemented.ts
  - src/simulation/systems/bloom.ts
  - src/simulation/systems/core-allocation.ts
  - src/simulation/systems/current.ts
  - src/simulation/systems/modules.ts
  - src/simulation/systems/routes.ts
  - src/simulation/engine.ts
  - src/simulation/selectors.ts
  - src/simulation/index.ts
  - tests/helpers/fixtures.ts
  - tests/helpers/replay.ts
  - tests/helpers/expect-valid-state.ts
  - tests/simulation/foundation-loop.test.ts
  - tests/simulation/validation.test.ts
  - tests/simulation/command-results.test.ts
  - tests/properties/deterministic-replay.property.test.ts
  - tests/properties/economy-safety.property.test.ts
  - tests/properties/allocation.property.test.ts
  - tests/properties/ownership.property.test.ts
autonomous: true
requirements:
  - FND-02
  - FND-04
  - FND-05
  - SIM-08
  - VER-01
  - VER-02
must_haves:
  truths:
    - "Developer can execute the foundation loop from starter state through focus completion, Bloom, Output-to-Core routing, default allocation, and validation."
    - "Domain-invalid command input returns typed validation issues with unchanged state."
    - "Property tests cover deterministic replay, resource safety, allocation totals, duplicate prevention, token safety, and forge count monotonicity where represented."
  artifacts:
    - path: "src/simulation/engine.ts"
      provides: "runSimulationCommand dispatcher"
      exports: ["runSimulationCommand"]
    - path: "src/simulation/commands/complete-focus-session.ts"
      provides: "foundation loop command handler"
      contains: "complete_focus_session"
    - path: "tests/properties/deterministic-replay.property.test.ts"
      provides: "exact replay property tests"
      contains: "fc.assert"
  key_links:
    - from: "src/simulation/engine.ts"
      to: "src/domain/invariants.ts"
      via: "validate after applied command"
      pattern: "validateFlowgridSnapshot"
    - from: "src/simulation/commands/complete-focus-session.ts"
      to: "src/content/starter-state.ts"
      via: "starter module IDs and formulas"
      pattern: "STARTER_MODULE_DEFINITIONS|focus"
---

<objective>
Implement the deterministic foundation loop and invariant test bar that make Phase 1 a working pure simulation slice.

Purpose: Prove that Flowgrid can transform real effort into visible, useful, deterministic signal while preserving architecture boundaries and economy safety before persistence or UI exists.
Output: Pure simulation dispatcher, complete focus session command, Core allocation command, typed not-implemented handlers, invariant validators, unit tests, property tests, and passing Phase 1 verification commands.
</objective>

<execution_context>
@/Users/eggfam/.codex/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
@.planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
@docs/technical-vision-draft.md
@docs/gameplay-spine-draft.md
@.planning/phases/01-deterministic-foundation-slice/01-01-SUMMARY.md
@.planning/phases/01-deterministic-foundation-slice/01-02-SUMMARY.md
</context>

## Dependency Graph

| Plan | Needs | Creates | Wave |
|---|---|---|---|
| 01 | Phase source artifacts and package legitimacy confirmation | Tooling, source folders, first failing tests | 1 |
| 02 | Plan 01 tooling and failing tests | Domain records, command contracts, starter content | 2 |
| 03 | Plan 02 contracts and content | Executable foundation loop, invariants, property tests | 3 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement complete focus session and command dispatcher</name>
  <files>src/simulation/commands/complete-focus-session.ts, src/simulation/commands/set-core-allocation.ts, src/simulation/commands/not-implemented.ts, src/simulation/systems/bloom.ts, src/simulation/systems/core-allocation.ts, src/simulation/systems/current.ts, src/simulation/systems/modules.ts, src/simulation/systems/routes.ts, src/simulation/engine.ts, src/simulation/selectors.ts, src/simulation/index.ts, tests/simulation/foundation-loop.test.ts, tests/simulation/command-results.test.ts</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
    .planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
    docs/gameplay-spine-draft.md
    src/domain/result.ts
    src/domain/records.ts
    src/content/starter-state.ts
  </read_first>
  <behavior>
    - `runSimulationCommand` returns `applied`, `rejected`, or `not_implemented`.
    - `complete_focus_session` executes starter state -> focus completion -> Bloom -> Output route to Core -> default allocation -> validation per D-01 and D-04.
    - `log_rejuvenation`, `run_forge`, and `install_module` return clear typed `not_implemented` results per D-02.
  </behavior>
  <action>Implement FND-04, SIM-08, D-01, D-02, D-03, D-04, D-07, and D-08. `runSimulationCommand(previousState, command, env)` must dispatch exhaustively. `complete_focus_session` validates `cellId`, positive `durationSeconds`, `startedAt`, `endedAt`, starter module presence, and Core allocation before mutation. For valid input, create an append-shaped focus `SessionRecord`, add integer Cell XP and Current, update daily progress, fire Bloom once for `env.localDate` when the milestone completes, set Activation, route starter Output Current to Core, split Current through integer `convertAllocationPercent` and `storeAllocationPercent`, update Energy, lifetime Energy, and Core Charge, emit economy events, visual events, one sync-ready operation using command `operationId`, run post-command validation, and return empty validation issues. For normal invalidity, return `rejected`, preserve state by deep equality, and emit typed validation issues with no economy events, visual events, or operations. `set_core_allocation` accepts integer convert/store values only when they total 100; otherwise reject unchanged. The later command handlers return `not_implemented` with unchanged state and typed issues or status metadata, not thrown errors.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run test -- --run tests/simulation/foundation-loop.test.ts tests/simulation/command-results.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `complete_focus_session` produces non-negative integer XP, Current, Energy, Core Charge, and lifetime Energy.
    - The same starter state, command, `now`, `localDate`, and RNG seed produce deep-equal result objects.
    - Invalid `cellId`, non-positive duration, reversed time range, missing starter module, or invalid Core allocation returns `rejected` with unchanged state.
    - Later command types return `status: "not_implemented"` with all `SimulationResult` fields present.
  </acceptance_criteria>
  <done>The command dispatcher executes the foundation loop deterministically and returns stable rejected/not-implemented shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement invariant validators and validation tests</name>
  <files>src/domain/invariants.ts, src/domain/validation.ts, tests/helpers/expect-valid-state.ts, tests/simulation/validation.test.ts, tests/simulation/foundation-loop.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
    .planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
    src/domain/records.ts
    src/domain/operation-records.ts
  </read_first>
  <behavior>
    - Validators detect negative resources, invalid references, duplicate installs, invalid route allocations, invalid Core allocation totals, token regressions, and forge count regressions.
    - Applied command results include a state validation event only when post-command validation passes.
    - Normal domain invalidity returns validation issues instead of throwing.
  </behavior>
  <action>Implement FND-05 and D-05/D-07. Export `validateNoNegativeResources`, `validateReferences`, `validateNoDuplicateInstalls`, `validateRouteAllocations`, `validateCoreAllocation`, `validateMonotonicCounters`, `validateOperationShape`, and `validateFlowgridSnapshot`. Use structured `ValidationIssueCode` values such as `negative_resource`, `invalid_reference`, `duplicate_module_install`, `invalid_route_allocation`, `invalid_core_allocation_total`, `token_regression`, `forge_count_regression`, and `invalid_operation_shape`. Validate Cell charge, XP, Momentum, Core Energy, Core Charge, Integration, Module Tokens, forge count, routes, module definition references, module owner Cell references, installed slots, session Cell references, route endpoint references, singleton starter installs, and operation records. Tests must mutate fixture snapshots to prove each issue code is emitted and that validators do not silently repair invalid state.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run test -- --run tests/simulation/validation.test.ts tests/simulation/foundation-loop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each required validator is exported from `src/domain/invariants.ts`.
    - `tests/simulation/validation.test.ts` asserts every listed `ValidationIssueCode`.
    - Rejected command tests prove `previousState` and `nextState` are deep-equal.
    - Applied foundation-loop result has no validation issues and includes `state_validated` economy metadata or equivalent durable validation event.
  </acceptance_criteria>
  <done>Invariant validators emit the required issue codes and applied commands validate cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add property tests for replay, safety, allocation, and ownership</name>
  <files>tests/helpers/fixtures.ts, tests/helpers/replay.ts, tests/helpers/expect-valid-state.ts, tests/properties/deterministic-replay.property.test.ts, tests/properties/economy-safety.property.test.ts, tests/properties/allocation.property.test.ts, tests/properties/ownership.property.test.ts, tests/simulation/boundaries.test.ts</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-deterministic-foundation-slice/01-CONTEXT.md
    .planning/phases/01-deterministic-foundation-slice/01-RESEARCH.md
    src/domain/records.ts
    src/domain/result.ts
    src/content/starter-state.ts
    src/simulation/engine.ts
  </read_first>
  <behavior>
    - Property tests use bounded `fast-check` runs so Phase 1 verification remains fast.
    - Exact replay compares `nextState`, `economyEvents`, `visualEvents`, `operations`, and `validationIssues`.
    - Boundary tests still pass after all simulation implementation is present.
  </behavior>
  <action>Implement VER-02 and D-06/D-08. Add property tests with bounded `fc.assert(..., { numRuns: 100 })` for deterministic replay over generated valid durations, timestamps, operation IDs, local dates, and allocation splits; resource non-negativity after focus completion; allocation acceptance/rejection where accepted state always totals 100; duplicate module install rejection; monotonic Module Tokens, lifetime Energy, and forge count for represented state; and idempotent operation shape when the same operation ID is replayed. Keep generated commands deterministic by injecting env and operation IDs. Ensure `tests/simulation/boundaries.test.ts` passes against the completed simulation tree.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `deterministic-replay.property.test.ts`, `economy-safety.property.test.ts`, `allocation.property.test.ts`, and `ownership.property.test.ts` all pass.
    - `npm run typecheck`, `npm run lint`, and `npm run test -- --run` pass together.
    - Boundary scanner reports no forbidden simulation imports or browser global/timer references.
    - Property failures, if introduced intentionally, shrink to readable command/state inputs.
  </acceptance_criteria>
  <done>Full Phase 1 verification passes: typecheck, lint, unit tests, property tests, and boundary tests.</done>
</task>

</tasks>

## Artifacts this phase produces

- Engine: `runSimulationCommand`, `complete_focus_session` handler, `set_core_allocation` handler, `not_implemented` handlers.
- Systems: Bloom, Current generation, starter module lookup, Output routing, Core allocation split.
- Validators: `validateNoNegativeResources`, `validateReferences`, `validateNoDuplicateInstalls`, `validateRouteAllocations`, `validateCoreAllocation`, `validateMonotonicCounters`, `validateOperationShape`, `validateFlowgridSnapshot`.
- Tests: `foundation-loop.test.ts`, `validation.test.ts`, `command-results.test.ts`, `deterministic-replay.property.test.ts`, `economy-safety.property.test.ts`, `allocation.property.test.ts`, `ownership.property.test.ts`, `boundaries.test.ts`.
- Verification commands: `npm run typecheck`, `npm run lint`, `npm run test -- --run`.

<must_haves>
- FND-02: simulation implementation remains pure and boundary tests pass.
- FND-04: every command path returns the stable result shape.
- FND-05: validators catch all required safety failures.
- SIM-08: replay is exact with injected time, local date, RNG, and operation IDs.
- VER-01: unit tests cover the foundation command, typed not-implemented commands, and validation failures.
- VER-02: property tests cover replay, non-negativity, allocation, duplicate prevention, token safety, and forge count monotonicity where represented.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| command input to simulation | User/domain input crosses into economy mutation logic. |
| simulation to renderer/persistence/sync | Result events and operations are consumed by later phases and must not carry ambiguous truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-03-01 | Tampering | command handlers | mitigate | Validate before mutation; reject normal invalidity unchanged; validate after applied commands. |
| T-01-03-02 | Repudiation | operation records | mitigate | Use caller-provided operation IDs and deterministic payload timestamps from `SimulationEnv`. |
| T-01-03-03 | Denial of Service | property tests | mitigate | Bound `fast-check` runs to 100 cases per property in Phase 1 verification. |
| T-01-03-04 | Elevation of Privilege | renderer/persistence imports | mitigate | Boundary scanner and lint prevent simulation from depending on UI, render, persistence, DOM, browser timers, React, PixiJS, Dexie, and Zustand. |
</threat_model>

<verification>
Run the full Phase 1 gate: `npm run typecheck`, `npm run lint`, and `npm run test -- --run`. All unit tests, property tests, and boundary tests must pass.
</verification>

<success_criteria>
The deterministic foundation loop is executable and exactly replayable; invalid commands return typed validation issues with unchanged state; invariant validators and property tests cover the safety-core tier; and no simulation code crosses the architecture boundary.
</success_criteria>

<output>
Create `.planning/phases/01-deterministic-foundation-slice/01-03-SUMMARY.md` when done.
</output>
