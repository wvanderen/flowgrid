---
phase: 03-playable-generator-flowgrid
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/routes.tsx
  - src/app/store/dispatch.ts
  - src/app/main.tsx
  - src/app/repository.ts
  - src/ui/cell-board/CellBoard.tsx
  - src/ui/cell-board/ModuleTile.tsx
  - src/ui/cell-board/CellInspector.tsx
  - src/ui/cell-board/GeneratorTile.tsx
  - src/ui/cell-board/CreateCellForm.tsx
  - src/ui/cell-board/EditCellForm.tsx
  - src/ui/cell-board/CellActions.tsx
  - src/ui/cell-board/SessionTimer.tsx
  - src/ui/cell-board/ResumeSessionPrompt.tsx
  - src/ui/session-summary/SessionSummary.tsx
  - src/ui/session-summary/nextAction.ts
  - src/ui/flowgrid-home/FlowgridHome.tsx
  - src/ui/flowgrid-home/ArchivedCellsFilter.tsx
  - tests/ui/cell-board.test.tsx
  - tests/ui/session-summary.test.tsx
  - tests/ui/create-cell-form.test.tsx
autonomous: true
requirements:
  - CELL-01
  - CELL-02
  - CELL-03
  - CELL-04
  - SESS-01
  - SESS-05
  - UI-05

must_haves:
  truths:
    - "User can create a Cell with name, color, optional icon, and daily target from the Cell Board UI (per CELL-01, D-09)"
    - "User can inspect a Cell's XP, Momentum, Charge, milestone progress, Activation state, installed modules, and recent sessions (per CELL-02)"
    - "User can edit Cell name, color, icon, and daily target without losing history (per CELL-03, D-11)"
    - "User can archive a Cell so it is hidden from the Flowgrid Home while preserving history (per CELL-04, D-12)"
    - "User can start a focus session from the Generator tile with one primary action (per SESS-01, D-05)"
    - "User can finish a session and see a completion summary with duration, Current, XP, milestone progress, Bloom/Activation effects, and next useful action (per SESS-05)"
    - "User can cancel a session without creating accidental rewards (per SESS-03, D-07)"
    - "Cell Board shows the four starter modules as labeled tiles (per UI-05, CELL-05)"
    - "The active-session timer uses cosmetic setInterval decoupled from durable truth (per D-06)"
    - "App open runs reconcileDayRollover before first render so the Flowgrid is correct immediately (per D-13)"
    - "main.tsx calls initApp(repository) before createRoot so the Zustand store transitions loading→ready and React never mounts against a null snapshot (per D-13)"
    - "Interrupted sessions (activeSessionStartedAt present on reload) surface a resume-or-discard prompt (per D-05)"
  artifacts:
    - path: "src/ui/cell-board/CellBoard.tsx"
      provides: "Cell Board route component at /cells/:cellId with module tiles, inspector, and Generator session actions"
      contains: "useParams"
    - path: "src/ui/cell-board/GeneratorTile.tsx"
      provides: "The protected primary action: Start/Finish/Cancel focus session from one tile"
      contains: "start_focus_session"
    - path: "src/ui/cell-board/CreateCellForm.tsx"
      provides: "Form dispatching create_cell command with name, color, icon, dailyTargetSeconds"
      contains: "create_cell"
    - path: "src/ui/session-summary/SessionSummary.tsx"
      provides: "Inline completion panel showing duration, Current, XP, milestone, Bloom/Activation, next action"
      contains: "SessionSummary"
    - path: "src/ui/session-summary/nextAction.ts"
      provides: "Pure selectors function computing the next useful action text"
      contains: "export function nextUsefulAction"
  key_links:
    - from: "src/ui/cell-board/GeneratorTile.tsx"
      to: "src/app/store/dispatch.ts"
      via: "dispatches start_focus_session, complete_focus_session, and cancel_focus_session commands"
      pattern: "dispatch"
    - from: "src/ui/cell-board/CellBoard.tsx"
      to: "src/simulation/selectors.ts"
      via: "reads getCellById, getStarterModuleInstanceForCell, getRecentSessions via selectors"
      pattern: "getCellById"
    - from: "src/app/store/dispatch.ts"
      to: "src/simulation/systems/day-rollover.ts"
      via: "app-open sequence calls reconcileDayRollover after loadSnapshot before first render"
      pattern: "reconcileDayRollover"
    - from: "src/app/main.tsx"
      to: "src/app/store/dispatch.ts"
      via: "calls await initApp(repository) before createRoot so flowgridStore transitions loading→ready before React mounts"
      pattern: "initApp"
    - from: "src/app/main.tsx"
      to: "src/app/repository.ts"
      via: "imports the repository singleton and passes it to initApp"
      pattern: "repository"
---

<objective>
Deliver the Cell Board route — the primary inspector and interaction surface where users create Cells, see starter modules, start/finish/cancel focus sessions, view completion summaries, edit/archive Cells, and resume interrupted sessions. This plan completes the protected first loop: open app → see Flowgrid → tap Cell → start session → finish → see rewards.

Purpose: This is the final vertical slice of Phase 3. After this plan, a user can complete the entire protected loop in a browser: create a Cell, start a Generator session, finish for proportional rewards (or cancel safely), see the completion summary, and return. The daily-rollover reconciliation runs on app open so the Flowgrid is always correct.
Output: Cell Board UI with module tiles, inspector, session actions, create/edit/archive forms, session completion summary, resume prompt, and archived-Cell filter.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-playable-generator-flowgrid/03-CONTEXT.md
@.planning/phases/03-playable-generator-flowgrid/03-RESEARCH.md

# Prior plan summaries (the layers this plan consumes)
@.planning/phases/03-playable-generator-flowgrid/03-01-PLAN.md
@.planning/phases/03-playable-generator-flowgrid/03-02-PLAN.md

# Contracts consumed by the Cell Board UI
@src/domain/records.ts
@src/domain/result.ts
@src/simulation/selectors.ts
@src/simulation/systems/bloom.ts
@src/simulation/systems/day-rollover.ts
@src/content/formulas.ts
@src/content/starter-modules.ts
@src/app/store/flowgrid-store.ts
@src/app/store/dispatch.ts
@src/app/env.ts
@src/persistence/errors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cell Board with module tiles, inspector, Generator session actions, Cell CRUD forms, and CreateCellForm mounting on Flowgrid Home</name>
  <files>
    src/app/routes.tsx,
    src/ui/cell-board/CellBoard.tsx,
    src/ui/cell-board/ModuleTile.tsx,
    src/ui/cell-board/CellInspector.tsx,
    src/ui/cell-board/GeneratorTile.tsx,
    src/ui/cell-board/CreateCellForm.tsx,
    src/ui/cell-board/EditCellForm.tsx,
    src/ui/cell-board/CellActions.tsx,
    src/ui/cell-board/SessionTimer.tsx,
    src/ui/flowgrid-home/FlowgridHome.tsx,
    tests/ui/cell-board.test.tsx,
    tests/ui/create-cell-form.test.tsx
  </files>
  <read_first>
    src/domain/records.ts (CellRecord with D-10 fields — color, icon, archivedAt, activeSessionStartedAt, lastBloomLocalDate for Activation display; SessionRecord for summary fields; ModuleInstance for tile rendering),
    src/domain/result.ts (CreateCellCommand, EditCellCommand, ArchiveCellCommand, UnarchiveCellCommand, StartFocusSessionCommand, CancelFocusSessionCommand, CompleteFocusSessionCommand — the command interfaces the UI dispatches; SimulationEnv for env construction),
    src/simulation/selectors.ts (getCellById, getCore, getStarterModuleInstanceForCell, getRecentSessions — the selector functions the Cell Board reads through),
    src/content/starter-modules.ts (GENERATOR_MODULE_DEFINITION_ID, CHARGE_CORE_MODULE_DEFINITION_ID, OUTPUT_MODULE_DEFINITION_ID, BLOOM_MODULE_DEFINITION_ID — the module kinds to render as tiles),
    src/content/formulas.ts (DEFAULT_SESSION_LENGTH_SECONDS, DEFAULT_DAILY_TARGET_SECONDS, ACTIVATION_CURRENT_BONUS_PERCENT — defaults for the create form and Activation display),
    src/app/store/flowgrid-store.ts (Plan 03-02 — useFlowgridStore selector hook, FlowgridState interface with snapshot and activeSession),
    src/app/store/dispatch.ts (Plan 03-02 — dispatch function signature and useFlowgridStore hook),
    src/app/env.ts (Plan 03-02 — makeEnv function for constructing SimulationEnv with deriveLocalDate),
    src/app/routes.tsx (Plan 03-02 — current route table to replace the Cell Board placeholder),
    src/ui/flowgrid-home/FlowgridHome.tsx (Plan 03-02 — the Home component to extend with a "New Cell" button + Radix Dialog mounting CreateCellForm, closing the CELL-01 reachability gap),
    .planning/phases/03-playable-generator-flowgrid/03-CONTEXT.md (SESS-05 content list at line 50; D-05 resume prompt; D-06 cosmetic timer; D-07 cancel writes nothing; D-08 sub-second finish; D-11 edit identity only; D-12 archive)
  </read_first>
  <behavior>
    - Test: CellBoard renders the cell name from getCellById when a valid cellId is in the URL params
    - Test: CellBoard renders four ModuleTile components labeled Generator, Charge Core, Output, Bloom
    - Test: CellInspector displays XP, Momentum, Charge, daily milestone progress (as seconds/total), and Activation state (Activated today / Not activated)
    - Test: GeneratorTile shows a Start button when no session is active; dispatches start_focus_session on click
    - Test: GeneratorTile shows Finish and Cancel buttons when a session is active on this cell
    - Test: Finish dispatches complete_focus_session with startedAt, endedAt (now), durationSeconds (floor delta)
    - Test: Cancel dispatches cancel_focus_session (which writes nothing durable per D-07)
    - Test: CreateCellForm dispatches create_cell with name, color (hex), icon (nullable), dailyTargetSeconds
    - Test: CreateCellForm rejects empty name and invalid color format before dispatch
    - Test: EditCellForm dispatches edit_cell with identity fields only (never economy fields)
    - Test: CellActions archive button dispatches archive_cell; the cell disappears from the Flowgrid Home on next render
    - Test: SessionTimer displays elapsed time updating via setInterval (cosmetic, D-06)
    - Test: FlowgridHome renders a "New Cell" button that opens a Radix Dialog containing CreateCellForm (CELL-01 reachability — the form must be mounted, not just defined)
  </behavior>
  <action>
    Update src/app/routes.tsx to replace the Cell Board placeholder div with `<CellBoard />` imported from `../ui/cell-board/CellBoard`. Keep the FlowgridHome route at `/` unchanged. The route path `/cells/:cellId` stays the same (React Router param name cellId).

    Create src/ui/cell-board/CellBoard.tsx as the route component for `/cells/:cellId`. It reads `cellId` from `useParams()` (react-router). It reads the snapshot from `useFlowgridStore(s => s.snapshot)`. If snapshot is null or `getCellById(snapshot, cellId)` returns undefined, render a "Cell not found" message with a link back to `/`. If the cell exists, render a semantic `<section>` with: `<h1>{cell.name}</h1>`, the `<CellInspector cell={cell} snapshot={snapshot} />`, the four `<ModuleTile>` components (one per starter module kind), and the `<GeneratorTile cell={cell} />`. Also render `<CellActions cellId={cell.id} />` for archive/edit access. Import useParams and Link from 'react-router'. Import getCellById from src/simulation/selectors. Import useFlowgridStore from src/app/store/dispatch. Per UI-05 spec.

    Create src/ui/cell-board/ModuleTile.tsx exporting a `ModuleTile` component taking props: `kind: ModuleDefinitionKind`, `label: string`, `description: string`. Render an accessible `<div role="group" aria-label={label}>` with the module name and a short description. Use lucide-react icons mapped by kind (e.g., Zap for Generator, Battery for Charge Core, ArrowRight for Output, Flower for Bloom). This is a presentational component (UI-05 — starter modules as hex tiles conceptually, rendered as accessible semantic tiles in React).

    Create src/ui/cell-board/CellInspector.tsx exporting a `CellInspector` component taking props: `cell: CellRecord`, `snapshot: FlowgridSnapshot`. Render accessible `<dl>` (description list) with: XP (cell.xp), Momentum (cell.momentum), Charge (cell.charge), Daily Milestone (cell.dailyMilestoneProgressSeconds / cell.dailyMilestoneTargetSeconds as "Xm / Ym" or percentage), Activation state ("Activated today" if cell.lastBloomLocalDate === derived localDate from snapshot.settings, else "Not activated"). Import CellRecord and FlowgridSnapshot types. Read recent sessions via getRecentSessions(snapshot, 5) and list them as a compact history (startedAt date, durationSeconds, currentGenerated). Per CELL-02.

    Create src/ui/cell-board/GeneratorTile.tsx exporting a `GeneratorTile` component taking props: `cell: CellRecord`. It reads the active session from `useFlowgridStore(s => s.activeSession)`. If no active session exists for this cell, render a `<button>` labeled "Start Focus Session" that dispatches `start_focus_session` via the dispatch function. Construct the command with `{ type: 'start_focus_session', operationId: crypto.randomUUID(), cellId: cell.id }`. Construct env via `makeEnv(new Date().toISOString(), { localDayBoundary: snapshot.settings.localDayBoundary }, 'flowgrid-session-seed')`. If an active session exists for this cell (activeSession.cellId === cell.id), render the `<SessionTimer startedAt={activeSession.startedAt} />` plus two buttons: "Finish" and "Cancel". Finish constructs a `complete_focus_session` command with startedAt = activeSession.startedAt, endedAt = new Date().toISOString(), durationSeconds = Math.floor((endedAt - startedAt) / 1000). If durationSeconds is 0 or negative (sub-second finish per D-08), route through the cancel path instead (dispatch cancel_focus_session, show "Session too short to record"). Cancel dispatches `cancel_focus_session` with `{ type: 'cancel_focus_session', operationId: crypto.randomUUID(), cellId: cell.id }`. After Finish, the SessionSummary (Task 2) appears. Import dispatch and useFlowgridStore from src/app/store/dispatch. Import makeEnv from src/app/env. Import the command types from src/domain/result. Per SESS-01, SESS-02, SESS-03, D-05, D-06, D-07, D-08. This is the protected primary action.

    Create src/ui/cell-board/SessionTimer.tsx exporting a `SessionTimer` component taking props: `startedAt: string`. It uses `useState` and `useEffect` with `setInterval(updateDisplay, 1000)` to show elapsed time as MM:SS or HH:MM:SS. The interval is cleared on unmount. This timer is purely cosmetic (D-06) — it never touches durable state; the actual duration is computed as floor((endedAt - startedAt) / 1000) at Finish time. Display the timer in a `<time>` element with an aria-live="polite" for accessibility.

    Create src/ui/cell-board/CreateCellForm.tsx exporting a `CreateCellForm` component with controlled inputs for name (text), color (color picker or hex text input defaulting to '#6b7280'), icon (optional text for lucide name or emoji, nullable), and dailyTargetSeconds (number input defaulting to DEFAULT_DAILY_TARGET_SECONDS from formulas). On submit, validate: name non-empty after trim, color matches hex format, dailyTargetSeconds positive integer. If valid, construct a `create_cell` command with `{ type: 'create_cell', operationId: crypto.randomUUID(), cellId: 'flowgrid:cell:' + crypto.randomUUID(), name, color, icon, dailyTargetSeconds }` and dispatch it. After successful dispatch, navigate to `/cells/${newCellId}` via useNavigate. Use Radix UI primitives for accessible form controls where appropriate (labels, error messages). Import DEFAULT_DAILY_TARGET_SECONDS from src/content/formulas. Per CELL-01, D-09.

    Create src/ui/cell-board/EditCellForm.tsx exporting an `EditCellForm` component taking props: `cell: CellRecord`. Pre-fill name, color, icon, dailyTargetSeconds from the existing cell. On submit, construct an `edit_cell` command with only identity fields — NEVER include economy fields (D-11). Dispatch and close the form. The economy fields (xp, current, charge, momentum, etc.) are structurally impossible to send via this form. Per CELL-03, D-11.

    Create src/ui/cell-board/CellActions.tsx exporting a `CellActions` component taking props: `cellId: string`. Render an archive button (if cell.archivedAt is null) that dispatches `archive_cell`, or an unarchive button (if archivedAt is set) that dispatches `unarchive_cell`. Also render an edit button that opens the EditCellForm in a Radix Dialog. On archive, navigate back to `/` since the Cell Board for an archived cell should redirect. Per CELL-04, D-12.

    Extend src/ui/flowgrid-home/FlowgridHome.tsx (created by Plan 03-02) to mount CreateCellForm so users can actually create Cells (CELL-01 reachability — the form must be reachable from the primary surface, not just defined). Add a "New Cell" button inside the ready-state `<section>` that opens a Radix Dialog (`@radix-ui/react-dialog`, already installed by Plan 03-02) containing `<CreateCellForm />`. Wire CreateCellForm's successful-submit path to close the dialog (pass an `onCreated(newCellId)` callback that calls `setOpen(false)` then `navigate('/cells/' + newCellId)` — the form itself already navigates, so the dialog close is the only addition; alternatively pass an `onDone` prop the form calls after its navigate). If no active cells exist (all archived or none created), the existing empty-state message from Plan 03-02 stays, but the "New Cell" button is now always visible alongside it. Import CreateCellForm from `../cell-board/CreateCellForm`. Import Dialog and related primitives from `@radix-ui/react-dialog`. Use the controlled-open pattern (`useState<boolean>` for open state). This closes the key_links_planned blocker: CreateCellForm was previously created but never mounted, making CELL-01 and the phase goal unreachable.

    Create tests/ui/cell-board.test.tsx as a happy-dom RTL test. Mock the dispatch function and the store. Test: CellBoard renders the cell name and four module tiles when a valid cellId is provided. Test: CellInspector shows XP, Momentum, and Activation state. Test: GeneratorTile Start button is present when no session is active. Test: clicking Start calls dispatch with a start_focus_session command. Use render, screen, fireEvent/userEvent from @testing-library/react. Mock useNavigate if needed.

    Create tests/ui/create-cell-form.test.tsx testing the form validation and dispatch: submitting with an empty name prevents dispatch, submitting with invalid color prevents dispatch, submitting valid data dispatches create_cell with the correct fields.

    Extend tests/ui/flowgrid-home.test.tsx (created by Plan 03-02) with a test asserting the "New Cell" button is present in the ready state and clicking it opens a Dialog containing the CreateCellForm (assert the dialog role and a form field become visible). This proves the CELL-01 mounting wiring is reachable. If extending the existing file conflicts with Plan 03-02 ownership, instead add a new test block to tests/ui/cell-board.test.tsx that renders FlowgridHome and asserts the New Cell button + Dialog.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/ui/cell-board.test.tsx tests/ui/create-cell-form.test.tsx tests/ui/flowgrid-home.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/app/routes.tsx Cell Board route element is `<CellBoard />` (placeholder div replaced)
    - src/ui/cell-board/CellBoard.tsx reads cellId via useParams and renders CellInspector, ModuleTiles, and GeneratorTile
    - src/ui/cell-board/GeneratorTile.tsx dispatches start_focus_session on Start, complete_focus_session on Finish, cancel_focus_session on Cancel
    - src/ui/cell-board/GeneratorTile.tsx routes sub-second finishes through cancel (D-08)
    - src/ui/cell-board/CreateCellForm.tsx validates name non-empty and color hex before dispatching create_cell (CELL-01)
    - src/ui/cell-board/EditCellForm.tsx sends ONLY identity fields (name, color, icon, dailyTargetSeconds) — never economy fields (D-11)
    - src/ui/cell-board/CellActions.tsx dispatches archive_cell/unarchive_cell (CELL-04, D-12)
    - src/ui/cell-board/SessionTimer.tsx uses setInterval cosmetically and clears on unmount (D-06)
    - src/ui/cell-board/CellInspector.tsx displays XP, Momentum, Charge, milestone progress, Activation state, and recent sessions (CELL-02)
    - src/ui/flowgrid-home/FlowgridHome.tsx renders a "New Cell" button that opens a Radix Dialog containing CreateCellForm (CELL-01 reachability — CreateCellForm is mounted, not just defined)
    - `npx vitest run tests/ui/cell-board.test.tsx` passes all tests
    - `npx vitest run tests/ui/create-cell-form.test.tsx` passes all tests
    - `npx vitest run tests/ui/flowgrid-home.test.tsx` passes including the New Cell Dialog assertion
    - `npx tsc --noEmit` passes
    - `npx eslint src/ui/cell-board/ src/ui/flowgrid-home/` passes
  </acceptance_criteria>
  <done>
    Cell Board route delivers the full inspection + session lifecycle surface. Users can create Cells (CELL-01) via the New Cell button on Flowgrid Home, inspect them (CELL-02), edit identity (CELL-03), archive (CELL-04), start sessions from the Generator tile (SESS-01), finish for rewards (SESS-02), cancel safely (SESS-03), and see four starter module tiles (UI-05/CELL-05). Session timer is cosmetic (D-06). Sub-second finishes route through cancel (D-08). CreateCellForm is mounted and reachable (CELL-01 key-link closed). All tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session completion summary, resume prompt, day-rollover invocation, archived-Cell filter, and mounting wiring on CellBoard + FlowgridHome</name>
  <files>
    src/ui/session-summary/SessionSummary.tsx,
    src/ui/session-summary/nextAction.ts,
    src/ui/cell-board/ResumeSessionPrompt.tsx,
    src/ui/cell-board/CellBoard.tsx,
    src/ui/flowgrid-home/ArchivedCellsFilter.tsx,
    src/ui/flowgrid-home/FlowgridHome.tsx,
    src/app/store/dispatch.ts,
    src/app/repository.ts,
    src/app/main.tsx,
    tests/ui/session-summary.test.tsx,
    tests/ui/cell-board.test.tsx,
    tests/ui/flowgrid-home.test.tsx,
    tests/app/init-app.test.ts
  </files>
  <read_first>
    src/domain/records.ts (SessionRecord fields at lines 93-106 — durationSeconds, xpGained, currentGenerated, bloomFired, activationGranted, energyGained, coreChargeGained; CellRecord for milestone and Activation context; SettingsRecord for localDayBoundary),
    src/domain/result.ts (CompleteFocusSessionCommand — the command that produces the session being summarized; SimulationResult — carries the result to extract the session from),
    src/simulation/selectors.ts (getCellById, getRecentSessions — to read the cell and session context for the summary),
    src/simulation/systems/day-rollover.ts (Plan 03-01 — reconcileDayRollover signature and behavior; deriveLocalDate for computing local date in nextAction),
    src/content/formulas.ts (DEFAULT_DAILY_MILESTONE_TARGET_SECONDS, DEFAULT_LOCAL_DAY_BOUNDARY — for milestone progress display and next-action computation),
    src/app/store/flowgrid-store.ts (Plan 03-02 — FlowgridState interface, activeSession field for resume prompt detection, lastCompletedSession field added below),
    src/app/store/dispatch.ts (Plan 03-02 — current dispatch function to extend with app-open sequence and session-summary result capture),
    src/app/main.tsx (Plan 03-02 — current Vite entry point to modify so it calls await initApp(repository) before createRoot),
    src/persistence/database.ts (FlowgridDatabase class — instantiated once in the new src/app/repository.ts singleton),
    src/persistence/repository.ts (FlowgridRepository class — wraps FlowgridDatabase, instantiated once in the singleton; open/loadSnapshot/applyResult methods called by initApp and dispatch),
    src/persistence/seeding.ts (first-run seed — initApp relies on loadSnapshot returning a seeded snapshot on a fresh database),
    src/app/env.ts (Plan 03-02 — makeEnv for constructing the env passed to reconcileDayRollover),
    src/persistence/repository.ts (loadSnapshot — the app-open read path; applyResult — the dispatch write path),
    src/ui/cell-board/CellBoard.tsx (Plan 03-03 Task 1 — Cell Board route component to extend with SessionSummary rendering when lastCompletedSession is non-null for this cell),
    src/ui/flowgrid-home/FlowgridHome.tsx (Plan 03-03 Task 1 — already extended with CreateCellForm Dialog; this task adds ResumeSessionPrompt banner and ArchivedCellsFilter section),
    .planning/phases/03-playable-generator-flowgrid/03-CONTEXT.md (SESS-05 content list line 50; D-05 resume prompt; D-12 archived cells hidden from Flowgrid Home but visible behind a filter; D-13 belt-and-suspenders day rollover; Agent's Discretion "Next useful action is a small content/selectors function" line 50)
  </read_first>
  <behavior>
    - Test: nextUsefulAction returns "Bloom ready — complete N more minutes to bloom" when milestone progress is below target
    - Test: nextUsefulAction returns "Activated — start another session for bonus Current" when the cell is Activated today
    - Test: nextUsefulAction returns "Day resets at HH:MM" incorporating the localDayBoundary setting
    - Test: SessionSummary displays duration (formatted as Xm Ys or HH:MM:SS), Current generated, XP gained, milestone progress, Bloom status (fired/not), Activation status, Energy/Core Charge outcome
    - Test: SessionSummary displays the next useful action text from nextUsefulAction
    - Test: CellBoard renders SessionSummary when the store's lastCompletedSession is non-null AND its cellId matches the current cell (SESS-05 reachability — the summary must be visible, not just defined)
    - Test: CellBoard does NOT render SessionSummary when lastCompletedSession is null or belongs to a different cell
    - Test: FlowgridHome renders ResumeSessionPrompt when any cell has non-null activeSessionStartedAt after initApp (D-05 reachability)
    - Test: FlowgridHome renders ArchivedCellsFilter in a collapsible section (D-12 management surface)
    - Test: ResumeSessionPrompt "Discard" button dispatches cancel_focus_session
    - Test: ArchivedCellsFilter toggle controls whether archived cells appear in a list
    - Test: dispatch app-open sequence calls reconcileDayRollover after loadSnapshot
    - Test: initApp(repository) transitions flowgridStore.getState().status from 'loading' to 'ready', sets snapshot to the reconciled FlowgridSnapshot, and leaves lastError null on success
    - Test: initApp(repository) sets status 'error' and a non-null lastError when repository.open or loadSnapshot throws (failure path)
    - Test: main.tsx awaits initApp(repository) before invoking createRoot so React mounts only after the store is 'ready' (verified by source inspection + the init-app.test.ts bootstrap assertion)
  </behavior>
  <action>
    Create src/ui/session-summary/nextAction.ts exporting a pure `nextUsefulAction(cell: CellRecord, settings: SettingsRecord, localDate: string): string` function. This is a selectors/content function, NOT an AI suggestion (per CONTEXT Agent's Discretion line 50). Logic: if cell.lastBloomLocalDate === localDate, return a string like "Activated — start another session for +10% Current" (referencing ACTIVATION_CURRENT_BONUS_PERCENT). If the daily milestone is not complete, compute remaining seconds (cell.dailyMilestoneTargetSeconds - cell.dailyMilestoneProgressSeconds) and return "N more minutes to Bloom today". If the milestone is complete but Bloom has not fired today (edge case), return "Bloom is ready". Otherwise, return "Day resets at {settings.localDayBoundary}" (formatting the boundary as HH:MM). This function is pure and testable without React. Import CellRecord and SettingsRecord types from src/domain. Import ACTIVATION_CURRENT_BONUS_PERCENT from src/content/formulas for the bonus text.

    Create src/ui/session-summary/SessionSummary.tsx exporting a `SessionSummary` component taking props: `session: SessionRecord`, `cell: CellRecord`, `settings: SettingsRecord`, `localDate: string`. Render an accessible `<section role="status" aria-live="polite">` with a heading "Session Complete". Display the SESS-05 content list: duration formatted from session.durationSeconds (e.g., "25m 0s"), Current generated (session.currentGenerated), XP gained (session.xpGained), milestone progress (cell.dailyMilestoneProgressSeconds / cell.dailyMilestoneTargetSeconds as a fraction or percentage), Bloom status (session.bloomFired ? "Bloom fired!" : "No Bloom yet"), Activation status (session.activationGranted ? "Cell Activated" : "Not activated"), Energy/Core Charge outcome (session.energyGained and session.coreChargeGained — these may be 0 in Phase 3 since Core routing is Phase 4, display them but note "Core routing arrives in Phase 4" if both are 0). At the bottom, display the next useful action by calling `nextUsefulAction(cell, settings, localDate)`. Use semantic HTML (dl/dt/dd for the stats). This is an inline panel on the Cell Board (not a modal — per RESEARCH Open Question Q3 recommendation, an inline panel does not block the Generator). Import SessionRecord, CellRecord, SettingsRecord types. Import nextUsefulAction.

    Create src/ui/cell-board/ResumeSessionPrompt.tsx exporting a `ResumeSessionPrompt` component taking props: `cellId: string`, `cellName: string`, `startedAt: string`. It displays: "You had a focus session in progress for {cellName}. Resume or discard?" with two buttons. "Resume" uses `useNavigate` to navigate to `/cells/${cellId}` where the GeneratorTile picks up the active session naturally (the SessionTimer resumes from activeSessionStartedAt). "Discard" dispatches `cancel_focus_session` with `{ type: 'cancel_focus_session', operationId: crypto.randomUUID(), cellId }` to clear the marker (D-07 — cancel writes nothing durable). Import dispatch from src/app/store/dispatch and useNavigate from 'react-router'. Per D-05.

    Create src/ui/flowgrid-home/ArchivedCellsFilter.tsx exporting an `ArchivedCellsFilter` component that renders a toggle (Radix Switch or a simple button with aria-pressed) controlling whether archived cells are visible. This component reads a local boolean state and, when toggled on, displays a list of archived cells (cells where archivedAt is not null) with their names and an "Unarchive" action that dispatches `unarchive_cell`. When toggled off (default), the archived-cells list is hidden (D-12 — archived cells hidden from normal active use but manageable behind this filter). This does NOT affect the Pixi canvas (which already filters archived cells); it is a React-side list for managing archived cells. Import useFlowgridStore and dispatch.

    Update src/app/store/dispatch.ts to add an `initApp(repository: FlowgridRepository)` async function that implements the app-open sequence (D-13): call `await repository.open()`, call `const snapshot = await repository.loadSnapshot()`, construct env via `makeEnv(new Date().toISOString(), { localDayBoundary: snapshot.settings.localDayBoundary }, 'flowgrid-app-seed')`, call `const reconciled = reconcileDayRollover(snapshot, env)`, and set the store to `{ snapshot: reconciled, status: 'ready' }`. If any step throws, set `status: 'error'` and `lastError: mapDomException(e)`. Also update the `dispatch` function to capture the latest complete_focus_session result so the CellBoard can render SessionSummary: after a successful complete_focus_session dispatch, set a store field `lastCompletedSession` with the session record from `result.nextState.sessions` (the newly-appended session — find it by matching the operationId or taking the last entry). Import reconcileDayRollover from src/simulation/systems/day-rollover. Import makeEnv from src/app/env. Import mapDomException from src/persistence/errors. Extend the FlowgridState interface in flowgrid-store.ts to include `lastCompletedSession: SessionRecord | null` (initialize to null). The initApp function is exported from dispatch.ts so main.tsx can import and invoke it.

    Create src/app/repository.ts exporting a module-level singleton `repository` (and a sibling `database`). Import `FlowgridDatabase` from `../persistence/database` and `FlowgridRepository` from `../persistence/repository`. Instantiate `const database = new FlowgridDatabase()` and `export const repository = new FlowgridRepository(database)`. Re-export `database` for tests that need direct table access. This singleton is the production repository instance — both main.tsx (for initApp) and UI dispatch callers (GeneratorTile, CreateCellForm, EditCellForm, CellActions, ResumeSessionPrompt, ArchivedCellsFilter) import it. Module-level instantiation is safe because Dexie lazily opens the database on first interaction (the FlowgridDatabase constructor registers schema versions but does not perform I/O). This closes the BLOCKER: without a production repository instance, initApp would have nothing to call and the store would stay 'loading' forever.

    Modify src/app/main.tsx (created in Plan 03-02 Task 1) to call `await initApp(repository)` BEFORE `createRoot(...)`. Replace the existing entry-point body with an async IIFE: import `initApp` from `./store/dispatch` and `repository` from `./repository`, then `void (async () => { try { await initApp(repository); } catch (e) { console.error('Flowgrid bootstrap failed', e); } createRoot(document.getElementById('root')!).render(<App />); })()`. The try/catch ensures createRoot still runs even if initApp throws (the store will be in 'error' status and FlowgridHome will render the ErrorBanner). Keep the existing `import '../style.css'` and `import { App } from './App'` lines. This guarantees the Zustand store has transitioned from 'loading' to 'ready' (snapshot loaded, reconcileDayRollover applied per D-13) before React mounts, so FlowgridHome and CellBoard never render against a null snapshot. The brief async gap before createRoot is acceptable (Dexie open + loadSnapshot is fast on local IndexedDB; a future Phase 6 splash screen could render during this window if perceived latency becomes an issue). This closes the BLOCKER: initApp is now defined AND invoked on app entry, so D-13 day-rollover runs and dispatch no longer returns early on a null snapshot.

    Update all UI dispatch callers (GeneratorTile.tsx, CreateCellForm.tsx, EditCellForm.tsx, CellActions.tsx, ResumeSessionPrompt.tsx, ArchivedCellsFilter.tsx) to import `repository` from `src/app/repository` and pass it as the third argument to `dispatch(command, env, repository)`. The dispatch signature (defined in Plan 03-02 Task 1) is unchanged — the singleton simply replaces what would otherwise be a Context-provided or prop-drilled instance. This resolves how UI components obtain the repository: a module-level singleton imported at each dispatch site.

    Create tests/app/init-app.test.ts covering the initApp wiring end-to-end. Use fake-indexeddb setup (tests/helpers/setup-indexeddb.ts), construct an isolated test `FlowgridDatabase` and `FlowgridRepository` (do NOT use the singleton — tests need isolation), seed a starter snapshot via the seeding helper, then call `await initApp(testRepository)`. Assert `flowgridStore.getState().status === 'ready'`, `flowgridStore.getState().snapshot !== null`, and `flowgridStore.getState().lastError === null`. Additionally seed a cell whose `lastBloomLocalDate` is stale (yesterday's local date) and assert that after initApp the snapshot's cell has `dailyMilestoneProgressSeconds === 0` (proving reconcileDayRollover ran). For the failure path, mock `testRepository.loadSnapshot` to reject, call `await initApp(testRepository)`, and assert `flowgridStore.getState().status === 'error'` with non-null `lastError`. Reset the singleton store to `{ status: 'loading', snapshot: null, lastError: null, activeSession: null, pendingVisualEvents: [], lastCompletedSession: null }` in a `beforeEach` so test cases do not leak state. This test closes the key_links_planned BLOCKER: initApp is defined, invoked from main.tsx, and the store reliably reaches 'ready' on app entry.

    Extend src/ui/cell-board/CellBoard.tsx (created in Task 1) to mount SessionSummary so the SESS-05 completion summary is actually visible to the user (closing the key_links_planned blocker). After the existing CellInspector/ModuleTiles/GeneratorTile rendering, read `lastCompletedSession` and `activeSession` from the store. Conditionally render `<SessionSummary session={lastCompletedSession} cell={cell} settings={snapshot.settings} localDate={derivedLocalDate} />` when `lastCompletedSession !== null && lastCompletedSession.cellId === cellId`. Use the same deriveLocalDate helper the env factory uses (import from src/simulation/systems/day-rollover) to compute the localDate for the nextUsefulAction call. When the user navigates away from this cell or starts a new session, lastCompletedSession stays in the store but the cellId mismatch hides the summary on other cells — this is the desired behavior. Import SessionSummary from `../session-summary/SessionSummary`. Import useFlowgridStore to read lastCompletedSession. Import deriveLocalDate from src/simulation/systems/day-rollover.

    Extend src/ui/flowgrid-home/FlowgridHome.tsx (already extended in Task 1 with the CreateCellForm Dialog) to mount ResumeSessionPrompt and ArchivedCellsFilter, closing the remaining key_links_planned blockers. (a) After initApp completes and the store has a non-null snapshot, scan `snapshot.cells` for any cell with a non-null `activeSessionStartedAt`. If found, render `<ResumeSessionPrompt cellId={interruptedCell.id} cellName={interruptedCell.name} startedAt={interruptedCell.activeSessionStartedAt} />` as a banner above the Flowgrid canvas section. Per D-05, this surfaces the resume-or-discard decision after a reload. (b) Below the Flowgrid canvas (or in a sidebar/section), render `<ArchivedCellsFilter />` so users can manage archived cells (D-12 — archived cells are hidden from the canvas but reachable here). Import ResumeSessionPrompt from `../cell-board/ResumeSessionPrompt` and ArchivedCellsFilter from `./ArchivedCellsFilter`. Read snapshot from useFlowgridStore and find the interrupted cell via Array.from(snapshot.cells.values()).find(c => c.activeSessionStartedAt !== null).

    Create tests/ui/session-summary.test.tsx as a happy-dom RTL test. Test nextUsefulAction as a pure function (no React needed — can be in the same file or a separate test). Test SessionSummary renders all SESS-05 fields given a mock SessionRecord. Test that the next useful action text appears. Construct mock SessionRecord, CellRecord, and SettingsRecord objects with deterministic test values.

    Extend tests/ui/cell-board.test.tsx (created in Task 1) with a test asserting CellBoard renders SessionSummary when the store's lastCompletedSession.cellId matches the current cell, and does NOT render it when lastCompletedSession is null or belongs to a different cell. Mock the store to provide lastCompletedSession.

    Extend tests/ui/flowgrid-home.test.tsx (created in Plan 03-02, extended in Task 1) with tests asserting: (a) FlowgridHome renders ResumeSessionPrompt when the store snapshot contains a cell with non-null activeSessionStartedAt; (b) FlowgridHome renders ArchivedCellsFilter (the toggle is present). Mock the store accordingly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/ui/session-summary.test.tsx tests/ui/cell-board.test.tsx tests/ui/flowgrid-home.test.tsx tests/app/init-app.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/session-summary/nextAction.ts exports pure `nextUsefulAction` function returning context-appropriate guidance strings
    - src/ui/session-summary/SessionSummary.tsx displays duration, Current, XP, milestone progress, Bloom status, Activation status, Energy/Core outcome, and next useful action (SESS-05 complete content)
    - src/ui/cell-board/CellBoard.tsx renders SessionSummary when lastCompletedSession is non-null AND its cellId matches the current cell (SESS-05 reachability — summary mounted, not just defined)
    - src/ui/cell-board/ResumeSessionPrompt.tsx offers Resume/Discard and is mounted on FlowgridHome when a cell has activeSessionStartedAt (D-05 reachability)
    - src/ui/flowgrid-home/ArchivedCellsFilter.tsx toggles archived-cell visibility and is mounted on FlowgridHome (D-12 reachability)
    - src/ui/flowgrid-home/FlowgridHome.tsx renders ResumeSessionPrompt banner when an interrupted session exists and the ArchivedCellsFilter section
    - src/app/store/dispatch.ts exports `initApp` that calls reconcileDayRollover after loadSnapshot (D-13)
    - src/app/store/dispatch.ts dispatch function captures lastCompletedSession after complete_focus_session
    - src/app/repository.ts exports a singleton `repository` (instantiated from a single `FlowgridDatabase` + `FlowgridRepository`) usable by both initApp and UI dispatch callers
    - src/app/main.tsx awaits `initApp(repository)` before `createRoot(...)` so React mounts only after the store is 'ready' (BLOCKER fix — initApp is invoked, not just defined)
    - After `await initApp(repository)` succeeds, `flowgridStore.getState().status === 'ready'`, `snapshot !== null`, and `lastError === null`
    - After `initApp(repository)` fails (repository throws), `flowgridStore.getState().status === 'error'` and `lastError` is non-null
    - flowgrid-store.ts FlowgridState includes lastCompletedSession field
    - `npx vitest run tests/ui/session-summary.test.tsx` passes all tests including nextUsefulAction pure function tests
    - `npx vitest run tests/ui/cell-board.test.tsx` passes including the SessionSummary-mount assertion
    - `npx vitest run tests/ui/flowgrid-home.test.tsx` passes including ResumeSessionPrompt and ArchivedCellsFilter assertions
    - `npx vitest run tests/app/init-app.test.ts` passes including the loading→ready transition and failure-path assertions
    - `npx tsc --noEmit` passes
    - `npx eslint src/ui/session-summary/ src/ui/cell-board/ResumeSessionPrompt.tsx src/ui/cell-board/CellBoard.tsx src/ui/flowgrid-home/ArchivedCellsFilter.tsx src/ui/flowgrid-home/FlowgridHome.tsx` passes
  </acceptance_criteria>
  <done>
    Session completion summary is mounted on CellBoard and displays all SESS-05 content with a pure next-action selector (not AI). Resume prompt is mounted on FlowgridHome and handles interrupted sessions (D-05). Archived-cells filter is mounted on FlowgridHome and manages visibility (D-12). App-open sequence runs reconcileDayRollover for correct state (D-13). The BLOCKER is closed: src/app/repository.ts provides a production repository singleton and src/app/main.tsx awaits initApp(repository) before createRoot, so the store reliably transitions loading→ready, dispatch no longer returns early on a null snapshot, and D-13 day-rollover runs on every app entry. All three previously-unmounted components (SessionSummary, ResumeSessionPrompt, ArchivedCellsFilter) are now reachable from primary surfaces, closing the key_links_planned blockers. The full protected loop is playable: create → start → finish → see summary → return.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form input → command dispatch | Untrusted text input (Cell name, color, icon) crosses here; form validation + simulation validation are defense-in-depth |
| app open → day-rollover | Stale per-day state crosses here; reconcileDayRollover resets display state deterministically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | Cell name/icon XSS via form input | mitigate | React escapes all rendered text by default; icon is a lucide name or emoji string rendered as text content, never as innerHTML; Cell name displayed in `<h1>` and `<dd>` elements that escape |
| T-03-11 | Tampering | Edit form economy-field injection | mitigate | EditCellForm structurally constructs edit_cell with ONLY identity fields; the command interface does not accept economy fields (D-11); simulation validates cellId existence |
| T-03-12 | Denial of Service | Session timer interval leak | mitigate | SessionTimer useEffect cleanup function calls clearInterval on unmount; tested via RTL unmount |
| T-03-13 | Repudiation | Cancel leaving no audit trail | accept | D-07 intentionally writes no operation/session for cancel; this is a privacy feature, not a gap (user explicitly chose to discard) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx eslint .` passes including all layer boundary rules
- `npx vitest run` passes all tests across node and happy-dom environments
- Manual browser check via `npm run dev`: create a Cell → see it on Flowgrid → tap to open Cell Board → start session → wait → finish → see summary → navigate back
- Manual browser check: cancel a session and verify no session record appears in history
- Manual browser check: archive a Cell and verify it disappears from Flowgrid Home
</verification>

<success_criteria>
- User can create a Cell with name, color, icon, daily target (CELL-01)
- User can inspect Cell XP, Momentum, Charge, milestone, Activation, modules, sessions (CELL-02)
- User can edit Cell identity without losing history (CELL-03)
- User can archive/unarchive a Cell (CELL-04)
- User can start a session from the Generator tile with one action (SESS-01)
- User can finish and see a completion summary with all SESS-05 content (SESS-05)
- User can cancel without accidental rewards (SESS-03)
- Cell Board shows four starter module tiles (UI-05)
- Session timer is cosmetic, decoupled from durable truth (D-06)
- App open runs day rollover for correct state (D-13)
- main.tsx invokes initApp(repository) on entry so the store reaches 'ready' before React mounts (BLOCKER fix)
- Interrupted sessions surface a resume/discard prompt (D-05)
- The protected first loop is fully playable end-to-end
</success_criteria>

<output>
Create `.planning/phases/03-playable-generator-flowgrid/03-03-SUMMARY.md` when done
</output>

<artifacts>
## Artifacts this phase produces

### New exported symbols (src/ui/cell-board)
- `CellBoard` React component (CellBoard.tsx)
- `ModuleTile` React component (ModuleTile.tsx)
- `CellInspector` React component (CellInspector.tsx)
- `GeneratorTile` React component (GeneratorTile.tsx)
- `CreateCellForm` React component (CreateCellForm.tsx)
- `EditCellForm` React component (EditCellForm.tsx)
- `CellActions` React component (CellActions.tsx)
- `SessionTimer` React component (SessionTimer.tsx)
- `ResumeSessionPrompt` React component (ResumeSessionPrompt.tsx)

### New exported symbols (src/ui/session-summary)
- `SessionSummary` React component (SessionSummary.tsx)
- `nextUsefulAction(cell, settings, localDate)` pure function (nextAction.ts)

### New exported symbols (src/ui/flowgrid-home)
- `ArchivedCellsFilter` React component (ArchivedCellsFilter.tsx)

### New exported symbols (src/app/store)
- `initApp(repository)` async function (dispatch.ts — app-open sequence with day rollover)

### New exported symbols (src/app)
- `repository` singleton (repository.ts — production FlowgridRepository instance used by initApp and UI dispatch callers)
- `database` singleton (repository.ts — production FlowgridDatabase instance backing the repository)

### Modified existing symbols
- `dispatch` function in dispatch.ts — now captures lastCompletedSession after complete_focus_session
- `FlowgridState` interface in flowgrid-store.ts — now includes `lastCompletedSession: SessionRecord | null`
- Route table in routes.tsx — Cell Board placeholder replaced with `<CellBoard />`
- `main.tsx` entry point — now awaits `initApp(repository)` before `createRoot(...)` so the store transitions loading→ready before React mounts (BLOCKER fix)

### New file paths
- src/ui/cell-board/CellBoard.tsx
- src/ui/cell-board/ModuleTile.tsx
- src/ui/cell-board/CellInspector.tsx
- src/ui/cell-board/GeneratorTile.tsx
- src/ui/cell-board/CreateCellForm.tsx
- src/ui/cell-board/EditCellForm.tsx
- src/ui/cell-board/CellActions.tsx
- src/ui/cell-board/SessionTimer.tsx
- src/ui/cell-board/ResumeSessionPrompt.tsx
- src/ui/session-summary/SessionSummary.tsx
- src/ui/session-summary/nextAction.ts
- src/ui/flowgrid-home/ArchivedCellsFilter.tsx
- src/app/repository.ts
- tests/ui/cell-board.test.tsx
- tests/ui/session-summary.test.tsx
- tests/ui/create-cell-form.test.tsx
- tests/app/init-app.test.ts
</artifacts>
