---
phase: 03-playable-generator-flowgrid
plan: 04
type: execute
wave: 4
depends_on: [03-02, 03-03]
files_modified:
  - src/style.css
  - src/ui/flowgrid-home/FlowgridHome.tsx
  - src/ui/flowgrid-home/FlowgridCanvas.tsx
  - src/ui/flowgrid-home/ArchivedCellsFilter.tsx
  - src/ui/cell-board/CellBoard.tsx
  - src/ui/cell-board/CellInspector.tsx
  - src/ui/cell-board/GeneratorTile.tsx
  - src/ui/cell-board/ModuleTile.tsx
  - src/ui/cell-board/CreateCellForm.tsx
  - src/ui/cell-board/EditCellForm.tsx
  - src/ui/cell-board/CellActions.tsx
  - src/ui/cell-board/SessionTimer.tsx
  - src/ui/cell-board/ResumeSessionPrompt.tsx
  - src/ui/session-summary/SessionSummary.tsx
  - src/ui/shared/ErrorBanner.tsx
autonomous: true
gap_closure: true
requirements: [UI-01, UI-05]
must_haves:
  truths:
    - "Every Phase 3 user-facing component renders with Tailwind utility classes (dark theme), not raw unstyled HTML"
    - "The @theme design tokens (flowgrid-bg, flowgrid-surface, core, error, cell-activated) are consumed by at least one component each"
    - "The app reads as a cohesive dark-themed product: dark slate page background, surface cards/panels, gold Core accents, visible buttons, usable forms, centered Radix Dialogs with a backdrop overlay"
    - "All existing semantic markup (h1/h2/dl/ol/time), ARIA attributes (role, aria-label, aria-live, aria-pressed, role=alertdialog), event handlers, store selectors, and dispatch calls are byte-for-byte unchanged — only className attributes are added or modified"
    - "npx tsc --noEmit, npx eslint ., npx vitest run (170 tests), and npm run build all succeed after styling"
  artifacts:
    - path: "src/style.css"
      provides: "Base dark-theme body layer consuming bg-flowgrid-bg so the whole viewport is dark"
      contains: "bg-flowgrid-bg"
    - path: "src/ui/flowgrid-home/FlowgridHome.tsx"
      provides: "Styled home shell (page container, h1) + Create Cell Radix Dialog chrome (overlay backdrop + centered panel)"
      contains: "bg-flowgrid-surface"
    - path: "src/ui/flowgrid-home/FlowgridCanvas.tsx"
      provides: "Pixi canvas container with explicit viewport height so the scene is visible"
      contains: "h-[60vh]"
    - path: "src/ui/cell-board/CellBoard.tsx"
      provides: "Styled cell board layout with responsive module grid"
      contains: "max-w-5xl"
    - path: "src/ui/cell-board/GeneratorTile.tsx"
      provides: "Styled protected primary-action tile with gold Core accent and visible Start/Finish/Cancel buttons"
      contains: "text-core"
    - path: "src/ui/cell-board/CreateCellForm.tsx"
      provides: "Styled form fields (labels, inputs, field errors, submit) for the create flow"
      contains: "bg-slate-900/50"
    - path: "src/ui/cell-board/CellActions.tsx"
      provides: "Styled actions row + Edit Cell Radix Dialog chrome"
      contains: "bg-flowgrid-surface"
    - path: "src/ui/session-summary/SessionSummary.tsx"
      provides: "Styled completion panel consuming the core token for reward feedback"
      contains: "text-core"
  key_links:
    - from: "src/style.css"
      to: "all 14 styled components"
      via: "@theme tokens exposed automatically as Tailwind v4 color utilities (bg-flowgrid-bg, bg-flowgrid-surface, text-core, bg-core, text-error, text-cell-activated)"
      pattern: "(bg-flowgrid-bg|bg-flowgrid-surface|text-core|bg-core|text-error|text-cell-activated)"
    - from: "src/ui/flowgrid-home/FlowgridHome.tsx"
      to: "src/ui/cell-board/CreateCellForm.tsx"
      via: "CreateCellForm rendered inside FlowgridHome's Radix Dialog.Content — Task 1 styles the dialog shell, Task 3 styles the form fields"
      pattern: "CreateCellForm"
    - from: "src/ui/cell-board/CellActions.tsx"
      to: "src/ui/cell-board/EditCellForm.tsx"
      via: "EditCellForm rendered inside CellActions's Radix Dialog.Content — Task 3 styles both the dialog shell and the form fields together"
      pattern: "EditCellForm"
---

<objective>
Close the Phase 3 UAT styling gap (03-UAT.md test 2, severity major): every Phase 3 user-facing component currently renders as raw unstyled HTML because no component applies Tailwind utility classes. Tailwind v4 is correctly configured (@tailwindcss/vite plugin, @import 'tailwindcss', @theme palette tokens) but the tokens are never consumed by components.

This plan applies a cohesive first-pass dark-theme styling pass to all 14 Phase 3 UI files (12 unstyled components + FlowgridCanvas container + the style.css base layer), consuming the @theme design tokens so the app reads as a styled product.

Purpose: Make the protected `open app -> tap Cell -> start session` loop visually usable so UAT tests 2-15 can resume. Functional behavior is already correct (170 tests pass); this is purely additive presentational styling.

Output: A styled, buildable, fully-tested Phase 3 UI. No logic, semantic, accessibility, dispatch, simulation, persistence, domain, content, or render-layer changes.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playable-generator-flowgrid/03-UAT.md
@.planning/phases/03-playable-generator-flowgrid/03-02-SUMMARY.md
@.planning/phases/03-playable-generator-flowgrid/03-03-SUMMARY.md

# Files being styled (read each before editing — style IN PLACE):
@src/style.css
@src/ui/flowgrid-home/FlowgridHome.tsx
@src/ui/flowgrid-home/FlowgridCanvas.tsx
@src/ui/flowgrid-home/ArchivedCellsFilter.tsx
@src/ui/cell-board/CellBoard.tsx
@src/ui/cell-board/CellInspector.tsx
@src/ui/cell-board/GeneratorTile.tsx
@src/ui/cell-board/ModuleTile.tsx
@src/ui/cell-board/CreateCellForm.tsx
@src/ui/cell-board/EditCellForm.tsx
@src/ui/cell-board/CellActions.tsx
@src/ui/cell-board/SessionTimer.tsx
@src/ui/cell-board/ResumeSessionPrompt.tsx
@src/ui/session-summary/SessionSummary.tsx
@src/ui/shared/ErrorBanner.tsx
</context>

<style_vocabulary>
## Shared class vocabulary — use these EXACT utility strings

The @theme tokens defined in src/style.css (flowgrid-bg, flowgrid-surface, core, cell-default, cell-activated, cell-route, error) become Tailwind v4 color utilities automatically. Built-in Tailwind colors (slate, amber, black) provide the text/accent hierarchy. All classes below are plain strings to drop into className.

**Page container** (outer route sections):
`mx-auto max-w-5xl px-4 py-6 space-y-6`

**Card / panel** (inspector, tiles, summary, archived, generator):
`rounded-lg border border-slate-700 bg-flowgrid-surface p-4 space-y-3`

**Card with Core accent** (generator tile, session summary, resume prompt):
`rounded-lg border border-core/50 bg-flowgrid-surface p-4 space-y-3`

**Heading h1** (page title):
`text-3xl font-bold text-core`

**Heading h2** (section/tile title):
`text-lg font-semibold text-slate-100`

**Tile label h2** (module tiles, smaller):
`text-base font-semibold text-slate-100`

**Body text** (paragraphs):
`text-slate-300`

**Muted / caption text**:
`text-sm text-slate-400`

**Primary button** (Start Focus Session, Create Cell, Save, Resume):
`inline-flex items-center justify-center rounded-md bg-core px-4 py-2 font-semibold text-flowgrid-bg transition hover:bg-amber-300 focus:outline-none focus-visible:ring-2 focus-visible:ring-core`

**Primary button full-width** (form submit):
`inline-flex w-full items-center justify-center rounded-md bg-core px-4 py-2 font-semibold text-flowgrid-bg transition hover:bg-amber-300 focus:outline-none focus-visible:ring-2 focus-visible:ring-core`

**Secondary button** (Cancel, Close, Edit, Archive, Unarchive, toggle):
`inline-flex items-center justify-center rounded-md border border-slate-600 px-4 py-2 text-slate-200 transition hover:bg-slate-700 focus:outline-none focus-visible:ring-2 focus-visible:ring-slate-400`

**Danger-tinted secondary button** (Discard, Archive):
`inline-flex items-center justify-center rounded-md border border-error/60 px-4 py-2 text-error transition hover:bg-error/10 focus:outline-none focus-visible:ring-2 focus-visible:ring-error`

**Text input**:
`mt-1 block w-full rounded-md border border-slate-600 bg-slate-900/50 px-3 py-2 text-slate-100 placeholder-slate-500 focus:border-core focus:outline-none focus-visible:ring-1 focus-visible:ring-core`

**Label** (wraps label text + input):
`block text-sm font-medium text-slate-300`

**Field error** (role=alert):
`mt-1 text-sm text-error`

**Radix Dialog.Overlay**:
`fixed inset-0 z-40 bg-black/60`

**Radix Dialog.Content**:
`fixed left-1/2 top-1/2 z-50 w-[92vw] max-w-md -translate-x-1/2 -translate-y-1/2 rounded-lg border border-slate-700 bg-flowgrid-surface p-6 shadow-2xl space-y-4`

**Dialog.Title**:
`text-lg font-semibold text-slate-100`

**Stat cell** (dt/dd pair wrapper inside a dl grid):
`rounded-md bg-slate-900/40 p-2`

**Stat label dt**:
`text-xs font-medium uppercase tracking-wide text-slate-400`

**Stat value dd**:
`mt-1 text-lg font-semibold text-slate-100`

**Module grid** (wraps the 4 starter ModuleTiles):
`grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4`

**Stat dl grid** (inspector 5 metrics / summary 8 metrics):
`grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5`  (inspector)
`grid grid-cols-2 gap-3 sm:grid-cols-4`  (summary)

**Canvas container** (replaces current w-full h-full):
`relative h-[60vh] w-full overflow-hidden rounded-lg border border-slate-700 bg-slate-900/40 sm:h-[70vh]`
</style_vocabulary>

<scope_rules>
## HARD RULES for every task (do not violate)

1. **Additive only.** You may add `className="..."` attributes to existing elements and modify the one existing className in FlowgridCanvas.tsx. You MAY NOT add, remove, reorder, or rename any element. You MAY NOT change any text content, any `aria-*` attribute, any `role`, any `data-*` test hook, any `onChange`/`onClick`/`onSubmit` handler, any `useFlowgridStore` selector, any `dispatch(...)` call, any `Dialog.*` structural element, or any import.

2. **One narrow exception — conditional className.** CellInspector.tsx Activation value may switch its dd className between `text-cell-activated` and `text-slate-100` based on the already-computed `activatedToday` boolean. This is a presentational class choice, not logic. Compute the class string inline; do not change the dd text content.

3. **Text content is frozen.** The string inside every heading, paragraph, button, label, dt, dd, li, Dialog.Title must remain identical. Tests assert on these strings.

4. **No new files.** No new components, no new CSS modules, no tailwind.config.js, no changes outside src/ui/** and src/style.css. Specifically: no edits to src/simulation, src/persistence, src/domain, src/content, src/render, src/app.

5. **Consume the tokens.** At least one component must use each of: `bg-flowgrid-bg` (style.css body layer), `bg-flowgrid-surface` (cards), `text-core`/`bg-core` (Core accents, buttons), `text-error`/`border-error` (errors, danger buttons), `text-cell-activated` (Activation state). The cell-default and cell-route tokens are Pixi-scene colors; they need not appear in React components.

6. **Read before edit.** Read each file once immediately before editing it; apply the className additions in a single Edit pass per element to avoid churn.
</scope_rules>

<tasks>

<task type="auto">
  <name>Task 1: Style foundation + Flowgrid Home surface (style.css, home shell, canvas, banners, archived filter)</name>
  <files>src/style.css, src/ui/flowgrid-home/FlowgridHome.tsx, src/ui/flowgrid-home/FlowgridCanvas.tsx, src/ui/flowgrid-home/ArchivedCellsFilter.tsx, src/ui/shared/ErrorBanner.tsx, src/ui/cell-board/ResumeSessionPrompt.tsx</files>
  <action>
Establish the dark-theme foundation and style the Flowgrid Home route plus the ambient feedback banners. Use the exact class strings from the &lt;style_vocabulary&gt; section.

1. src/style.css — append a base layer AFTER the existing `@theme { ... }` block (keep `@import 'tailwindcss';` and the `@theme` block untouched). Add:
   - An `@layer base { ... }` block.
   - Inside it, a `body` rule that applies `bg-flowgrid-bg` and `text-slate-300` (via `@apply bg-flowgrid-bg text-slate-300;`), sets `min-height: 100vh;`, and a system sans-serif `font-family` stack (`ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, sans-serif`).
   - Also give `#root` `min-height: 100vh;` so the route section fills the viewport.
   This single rule consumes the flowgrid-bg token centrally so every route section inherits the dark background without repeating it.

2. src/ui/flowgrid-home/FlowgridHome.tsx — style all THREE returned section variants (error, loading, ready) plus the Create Cell Radix Dialog chrome. The component has three `return` branches; apply the page-container class to each outer `<section aria-label="Flowgrid home">`.
   - Every outer `<section aria-label="Flowgrid home">` → add the **page container** class string.
   - Every `<h1>Flowgrid</h1>` → add the **heading h1** class (text-core).
   - Loading `<p role="status">Loading Flowgrid…</p>` → add **muted text**.
   - Ready-branch empty-state `<p role="status">No active Cells yet…</p>` → add `rounded-lg border border-dashed border-slate-600 bg-flowgrid-surface p-6 text-center text-slate-400`.
   - Ready-branch active-count `<p aria-live="polite">` → add **muted text**.
   - The "New Cell" `<Dialog.Trigger asChild>` button → add the **primary button** class.
   - `<Dialog.Overlay />` → add the **Radix Dialog.Overlay** class.
   - `<Dialog.Content aria-label="Create a new Cell">` → add the **Radix Dialog.Content** class (keep aria-label).
   - `<Dialog.Title>Create a Cell</Dialog.Title>` → add the **Dialog.Title** class.
   - The Dialog.Close `<button aria-label="Close create dialog">` → add the **secondary button** class (keep aria-label and the "Close" text).
   Do NOT move, rename, or remove the ResumeSessionPrompt, FlowgridCanvas, or ArchivedCellsFilter elements — they are styled in their own files below.

3. src/ui/flowgrid-home/FlowgridCanvas.tsx — replace the container div's existing `className="w-full h-full"` with the **canvas container** class string (`relative h-[60vh] w-full overflow-hidden rounded-lg border border-slate-700 bg-slate-900/40 sm:h-[70vh]`). Keep `ref={containerRef}`, `aria-label="Flowgrid canvas"`, and `role="img"` exactly as-is. This gives the Pixi scene a concrete viewport height (h-full had no effect because no ancestor had a height).

4. src/ui/flowgrid-home/ArchivedCellsFilter.tsx — style the collapsible management section.
   - Outer `<section aria-label="Archived Cells">` → add the **card / panel** class.
   - Toggle `<button aria-pressed={expanded}>` → add the **secondary button** class (keep aria-pressed and onClick).
   - "No archived Cells." `<p>` → add **muted text**.
   - `<ul>` → add `divide-y divide-slate-700`.
   - Each `<li>` → add `flex items-center justify-between gap-3 py-2`.
   - The cell-name `<span>` → add `text-slate-200`.
   - Each "Unarchive" `<button>` → add the **secondary button** class (compact is fine).

5. src/ui/shared/ErrorBanner.tsx — style the PersistenceError banner.
   - `<div role="alert" data-kind={error.kind}>` → add `rounded-lg border border-error/50 bg-error/10 p-4 space-y-1` (keep role and data-kind).
   - `<strong>Flowgrid couldn't save that change.</strong>` → add `block font-semibold text-error`.
   - Both `<p>` children (message + recoverable hint) → add `text-sm text-slate-300`.

6. src/ui/cell-board/ResumeSessionPrompt.tsx — style the interrupted-session alert banner (rendered on Flowgrid Home).
   - `<section role="alertdialog" aria-label={...}>` → add the **card with Core accent** class (keep role and aria-label).
   - The explanatory `<p>` → add `text-slate-200`.
   - "Resume" `<button>` → add the **primary button** class.
   - "Discard" `<button>` → add the **danger-tinted secondary button** class.
   The two buttons are adjacent siblings of the section, so the section's `space-y-3` provides spacing between the paragraph and the buttons; add `mt-2` is unnecessary.

Commit message: `style(03-04): dark-theme foundation + Flowgrid Home surface (closes UAT test 2 styling gap, part 1/3)`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npx vitest run</automated>
  </verify>
  <done>
    - src/style.css has an @layer base body rule consuming bg-flowgrid-bg; the whole viewport is dark when the app loads.
    - FlowgridHome's three section variants use the page-container layout; h1 is gold (text-core); the Create Cell Dialog has a black/60 overlay and a centered surface panel.
    - FlowgridCanvas container has an explicit h-[60vh] height so the Pixi scene is visible.
    - ErrorBanner, ResumeSessionPrompt, and ArchivedCellsFilter render as distinct styled cards/buttons.
    - tsc, eslint, and all 170 vitest tests still pass (no text/role/handler changed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Cell Board read surfaces (board layout, inspector, module tiles, generator tile, session summary, session timer)</name>
  <files>src/ui/cell-board/CellBoard.tsx, src/ui/cell-board/CellInspector.tsx, src/ui/cell-board/ModuleTile.tsx, src/ui/cell-board/GeneratorTile.tsx, src/ui/session-summary/SessionSummary.tsx, src/ui/cell-board/SessionTimer.tsx</files>
  <action>
Style everything the user sees on the Cell Board route (`/cells/:cellId`) except the Create/Edit forms and CellActions (those land in Task 3). Use the exact class strings from the &lt;style_vocabulary&gt; section.

1. src/ui/cell-board/CellBoard.tsx — style all three return branches (snapshot-null, cell-not-found, happy path).
   - Every outer `<section aria-label="Cell Board">` / `<section aria-label={...}>` → add the **page container** class.
   - Every `<h1>` → happy-path `<h1>{cell.name}</h1>` gets `text-3xl font-bold text-slate-100`; the two "Cell not found" `<h1>` get `text-2xl font-bold text-slate-100`.
   - "This Cell does not exist or has been removed." `<p>` → add **muted text**.
   - Both `<Link to="/">Return to Flowgrid</Link>` → add `inline-flex items-center font-medium text-core transition hover:underline` (a link styled as a gold link, not a button — it is navigation, keep its text).
   - Modules wrapper `<section aria-label="Modules">` → add the **module grid** class (`grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4`) so the four ModuleTiles lay out responsively.
   - Leave the `<CellInspector>`, `<SessionSummary>`, `<GeneratorTile>`, and `<CellActions>` child placements exactly where they are.

2. src/ui/cell-board/CellInspector.tsx — style the read-only economy panel.
   - Outer `<section aria-label={...}>` → add the **card / panel** class.
   - `<dl>` → add the **stat dl grid** class for inspector (`grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5`).
   - Each `<div>` wrapping a dt/dd pair → add the **stat cell** class.
   - Every `<dt>` → add the **stat label dt** class.
   - Every `<dd>` → add the **stat value dd** class, EXCEPT the Activation dd: use the conditional-class exception — compute `activatedToday ? 'mt-1 text-lg font-semibold text-cell-activated' : 'mt-1 text-lg font-semibold text-slate-100'` and apply it to the Activation dd. Do not change the dd's ternary text content ("Activated today" / "Not activated").
   - Recent-sessions `<ol aria-label="Recent sessions">` → add `space-y-1 text-sm text-slate-400` (keep aria-label).
   - "No sessions yet." `<p>` → add **muted text**.

3. src/ui/cell-board/ModuleTile.tsx — style the four starter-module cards.
   - `<div role="group" aria-label={label}>` → add the **card / panel** class (keep role and aria-label).
   - `<h2>{label}</h2>` → add the **tile label h2** class.
   - `<Icon aria-hidden="true" data-testid={...}>` → add `h-8 w-8 text-core` (keep aria-hidden and data-testid).
   - `<p>{description}</p>` → add `text-sm text-slate-400`.

4. src/ui/cell-board/GeneratorTile.tsx — style the protected primary-action tile. The component has two return branches (idle Start state, and active Finish/Cancel state); BOTH `<section aria-label="Generator">` get the same classes.
   - Both `<section aria-label="Generator">` → add the **card with Core accent** class.
   - Both `<h2>Generator</h2>` → add `text-lg font-semibold text-core`.
   - "Tap to start a focus session." `<p>` and the "Session in progress:" `<p>` → add **body text** class.
   - "Start Focus Session" `<button>` → add the **primary button** class.
   - "Finish" `<button>` → add the **primary button** class.
   - "Cancel" `<button>` → add the **secondary button** class.
   - "Session too short to record." `<p role="status">` → add `text-sm text-error` (keep role).
   Leave the `<SessionTimer>` child and all dispatch/handleStart/handleFinish/handleCancel logic untouched.

5. src/ui/session-summary/SessionSummary.tsx — style the inline completion panel (reward feedback).
   - `<section role="status" aria-live="polite" aria-label="Session summary">` → add the **card with Core accent** class (keep all three a11y attributes).
   - `<h2>Session Complete</h2>` → add `text-lg font-semibold text-core`.
   - `<dl>` → add the summary **stat dl grid** (`grid grid-cols-2 gap-3 sm:grid-cols-4`).
   - Each dt/dd `<div>` → add the **stat cell** class.
   - Every `<dt>` → add the **stat label dt** class.
   - Every `<dd>` → add `mt-1 text-base font-semibold text-slate-100` (summary values slightly smaller than inspector's text-lg — 8 metrics fit better).
   - "Core routing arrives in Phase 4." `<p>` → add **muted text**.
   - `<p aria-label="Next useful action">` → add `rounded-md bg-slate-900/40 px-3 py-2 text-sm text-core` (keep aria-label).

6. src/ui/cell-board/SessionTimer.tsx — style the cosmetic elapsed-time display.
   - `<time aria-live="polite" dateTime={...} data-testid="session-timer">` → add `font-mono text-lg font-semibold text-core` (keep aria-live, dateTime, data-testid).

Commit message: `style(03-04): Cell Board read surfaces — inspector, tiles, generator, summary (closes UAT test 2 styling gap, part 2/3)`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npx vitest run</automated>
  </verify>
  <done>
    - CellBoard route uses the page-container layout with a responsive 4-up module grid.
    - CellInspector renders as a surface card with a 5-up stat grid; Activation value turns amber (text-cell-activated) when activated today.
    - ModuleTile cards show a gold Core icon; GeneratorTile is a gold-accented card with a visible gold Start/Finish button and a secondary Cancel button.
    - SessionSummary renders as a gold-accented reward panel with an 8-metric grid and a gold next-action hint.
    - SessionTimer renders in a gold monospace face.
    - tsc, eslint, and all 170 vitest tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cell management forms + Radix Edit Dialog chrome (CreateCellForm, EditCellForm, CellActions)</name>
  <files>src/ui/cell-board/CreateCellForm.tsx, src/ui/cell-board/EditCellForm.tsx, src/ui/cell-board/CellActions.tsx</files>
  <action>
Style the two identity forms and the CellActions row (which also owns the Edit Cell Radix Dialog chrome). This completes the styled Create + Edit dialog flows. Use the exact class strings from the &lt;style_vocabulary&gt; section.

1. src/ui/cell-board/CreateCellForm.tsx — style the create form (rendered inside FlowgridHome's Dialog, whose shell was styled in Task 1).
   - `<form onSubmit={handleSubmit} aria-label="Create Cell form">` → add `space-y-4` (keep onSubmit and aria-label).
   - Each of the four `<label>` wrappers (Cell name, Color, Icon, Daily target) → add the **label** class. Each label currently contains bare text followed by an `<input>`; the class goes on the label element itself.
   - Each `<input>` (text name, text color, text icon, number daily target) → add the **text input** class. Keep every input's `type`, `value`, `onChange`, `aria-label`, `autoComplete`, `min`, `step` attributes unchanged.
   - Each field-error `<p role="alert" data-error="...">` → add the **field error** class (keep role and data-error).
   - The submit `<button type="submit">Create Cell</button>` → add the **primary button full-width** class.

2. src/ui/cell-board/EditCellForm.tsx — style the edit form identically to the create form (it is rendered inside CellActions's Dialog, styled in step 3 below).
   - `<form onSubmit={handleSubmit} aria-label={...}>` → add `space-y-4` (keep onSubmit and aria-label).
   - Each of the four `<label>` wrappers → add the **label** class.
   - Each `<input>` → add the **text input** class. Keep all input attributes unchanged.
   - The submit `<button type="submit">Save</button>` → add the **primary button full-width** class.
   (EditCellForm has no inline field-error paragraphs — it silently no-ops on invalid input, which is existing behavior; do not add error UI.)

3. src/ui/cell-board/CellActions.tsx — style the actions row AND the Edit Cell Radix Dialog chrome (the form inside it was styled in step 2).
   - Outer `<section aria-label={...}>` → add `flex flex-wrap items-center gap-2` (keep aria-label).
   - The "Edit" `<Dialog.Trigger asChild>` button → add the **secondary button** class.
   - `<Dialog.Overlay />` → add the **Radix Dialog.Overlay** class.
   - `<Dialog.Content aria-label={...}>` → add the **Radix Dialog.Content** class (keep aria-label).
   - `<Dialog.Title>` → add the **Dialog.Title** class.
   - The Dialog.Close `<button aria-label="Close edit dialog">` → add the **secondary button** class (keep aria-label and "Close" text).
   - The "Archive" `<button>` (rendered when `cell.archivedAt === null`) → add the **danger-tinted secondary button** class.
   - The "Unarchive" `<button>` (rendered otherwise) → add the **secondary button** class.
   Leave handleArchive/handleUnarchive, the `navigate('/')`, and the EditCellForm child placement untouched.

Commit message: `style(03-04): cell management forms + Edit Dialog chrome (closes UAT test 2 styling gap, part 3/3)`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npx vitest run && npm run build</automated>
  </verify>
  <done>
    - CreateCellForm and EditCellForm render styled labels, slate inputs with a core focus ring, red field errors, and a full-width gold submit button.
    - CellActions renders as a horizontal actions row; the Edit Dialog has a backdrop overlay and centered surface panel; Archive is danger-tinted, Unarchive is secondary.
    - The full pipeline is green: tsc --noEmit clean, eslint clean, all 170 vitest tests pass, and `npm run build` produces a production bundle.
    - Every @theme token (flowgrid-bg, flowgrid-surface, core, error, cell-activated) is consumed by at least one component.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan crosses NO new trust boundaries. It adds presentational `className` strings to already-validated, already-accessible React components. No user input is newly accepted, no network call is added, no package is installed, no persistence/simulation/dispatch code is touched. The existing trust boundary (browser DOM -> React UI -> dispatch -> simulation) is unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | All 14 styled components | accept | className values are static author-controlled strings, not user/external input. No injection surface exists. |
| T-03-04-02 | Elevation of Privilege | Form className additions (Create/Edit) | accept | Styling cannot alter the dispatched command payloads; D-11 identity-only constraint is enforced structurally in EditCellForm.tsx logic, which is untouched. |
| T-03-04-03 | Repudiation | n/a | accept | No audit-relevant behavior changes; styling is presentational only. |
| T-03-04-04 | Information Disclosure | ErrorBanner / SessionSummary | accept | These already render their content; styling does not expose new data. Dark theme improves legibility, not disclosure. |
| T-03-04-05 | Denial of Service | n/a | accept | No new loops, intervals, or effects; the only existing setInterval (SessionTimer) is unchanged. |
| T-03-04-06 | Spoofing | n/a | accept | No authentication/identity surface in scope. |
| T-03-04-SC | Tampering | (no package installs in this plan) | accept | Zero npm install tasks; only utility classes from the already-approved tailwindcss@4.3.1 are used. No Package Legitimacy Gate applicable. |
</threat_model>

<verification>
After all three tasks commit, run the full gate (this is Task 3's automated verify, repeated here as the phase-level check):

1. `npx tsc --noEmit` — typecheck clean (className is a string; JSX must still parse).
2. `npx eslint .` — lint clean (no new violations; import-boundary rules unaffected since no imports change).
3. `npx vitest run` — all 170 tests pass. Tests assert on text/roles/labels/data-testid, none of which this plan changes. If any test fails, a forbidden text/role/structure edit slipped in — revert it.
4. `npm run build` — Vite production build succeeds with the Tailwind v4 pipeline emitting utility classes (not just Preflight).
5. Manual smoke (out of autonomous scope, done by resuming UAT): `npm run dev`, open the app, confirm Flowgrid Home renders as a dark-themed page with a gold "Flowgrid" heading, a visible "New Cell" button, and (when a Cell exists) a sized canvas; open the Create Cell dialog and confirm a centered panel over a dark backdrop with styled form fields. This unblocks UAT tests 2-15.
</verification>

<success_criteria>
- [ ] All 14 Phase 3 UI files (12 previously-unstyled components + FlowgridCanvas + style.css) carry Tailwind utility classes.
- [ ] The @theme tokens flowgrid-bg, flowgrid-surface, core, error, and cell-activated are each consumed by at least one component (verifiable by ripgrep across src/ui and src/style.css).
- [ ] No component lost any semantic element, ARIA attribute, role, text string, event handler, store selector, or dispatch call.
- [ ] `npx tsc --noEmit`, `npx eslint .`, `npx vitest run` (170/170), and `npm run build` all succeed.
- [ ] UAT test 2 ("Open App — Flowgrid Home Renders") is unblocked: the app reads as a styled product, not raw HTML.
- [ ] Three atomic commits land (one per task) on the gap-closure branch.
</success_criteria>

<output>
Create `.planning/phases/03-playable-generator-flowgrid/03-04-SUMMARY.md` when done. Update `03-UAT.md` test 2 result from `issue` to `pass` only after the manual smoke confirms the styling renders (the autonomous agent marks the build green; the human/UAT-resume confirms the visual).
</output>
