---
phase: 03-playable-generator-flowgrid
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - vite.config.ts
  - index.html
  - src/style.css
  - src/app/main.tsx
  - src/app/App.tsx
  - src/app/routes.tsx
  - src/app/store/flowgrid-store.ts
  - src/app/store/dispatch.ts
  - src/app/env.ts
  - src/app/rng.ts
  - src/render/flowgrid/hex-layout.ts
  - src/render/flowgrid/scene.ts
  - src/render/flowgrid/adapter.ts
  - src/ui/flowgrid-home/FlowgridHome.tsx
  - src/ui/flowgrid-home/FlowgridCanvas.tsx
  - src/ui/shared/ErrorBanner.tsx
  - tests/render/hex-layout.test.ts
  - tests/ui/flowgrid-home.test.tsx
  - tests/app/dispatch.test.ts
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "User can open the app in a browser and see a Core-centered hex Flowgrid with Cells arranged in rings (per D-01, D-04, UI-01)"
    - "The PixiJS 8 canvas initializes asynchronously via Application.init() and appends app.canvas to the DOM (per D-01, Pitfall 1)"
    - "Tapping a Cell hex navigates to /cells/:cellId via React Router (per D-03)"
    - "The Zustand vanilla store holds the latest FlowgridSnapshot and emits to React selectors (per Agent's Discretion: app shell coordination)"
    - "The dispatch path flows UI command → runSimulationCommand → repository.applyResult → store emit (per RESEARCH Pattern 1)"
    - "Visual events emitted by the simulation are received by the renderer but dropped/logged, not animated (per D-02)"
    - "PersistenceError values from the repository are rendered in a visible error banner (Phase 2 typed contract consumed)"
    - "The app loads the snapshot from IndexedDB on open via FlowgridRepository.loadSnapshot (Phase 2 contract reused)"
    - "Active (non-archived) Cells appear on the Flowgrid; archived Cells are hidden (per D-12)"
    - "Activated Cells (lastBloomLocalDate === localDate) show a distinct visual state on the hex (per D-02)"
  artifacts:
    - path: "src/app/main.tsx"
      provides: "Vite entry point — ReactDOM.createRoot mounting App"
      contains: "createRoot"
    - path: "src/app/store/flowgrid-store.ts"
      provides: "Zustand vanilla store holding snapshot, activeSession, visualEvents, status, lastError"
      contains: "createStore"
    - path: "src/app/store/dispatch.ts"
      provides: "Command dispatch path: UI → runSimulationCommand → repository.applyResult → store emit"
      contains: "export async function dispatch"
    - path: "src/render/flowgrid/hex-layout.ts"
      provides: "Pure axial/cube hex math: axialToPixel, pixelToAxial, axialRound, ringCells"
      contains: "export function axialToPixel"
    - path: "src/render/flowgrid/scene.ts"
      provides: "PixiJS 8 async Application.init() scene builder and destroyer"
      contains: "Application"
    - path: "src/ui/flowgrid-home/FlowgridHome.tsx"
      provides: "Home route component mounting Pixi canvas and wiring tap navigation"
      contains: "FlowgridCanvas"
  key_links:
    - from: "src/app/store/dispatch.ts"
      to: "src/simulation/engine.ts"
      via: "calls runSimulationCommand(prev, command, env) from the simulation barrel"
      pattern: "runSimulationCommand"
    - from: "src/ui/flowgrid-home/FlowgridHome.tsx"
      to: "src/render/flowgrid/scene.ts"
      via: "mounts FlowgridCanvas which calls buildFlowgridScene(app, snapshot, onCellTap)"
      pattern: "buildFlowgridScene"
    - from: "src/app/store/flowgrid-store.ts"
      to: "src/render/flowgrid/adapter.ts"
      via: "adapter subscribes to store for snapshot updates; store emits nextState after dispatch"
      pattern: "flowgridStore.subscribe"
---

<objective>
Install the locked technology stack (React 19, Vite 8, React Router 7, Tailwind 4, PixiJS 8, Zustand 5, Radix, lucide-react, Testing Library, happy-dom), configure the build and test environment for DOM/JSX, create the app shell (main entry, router, Zustand store, command dispatch, env factory), build the PixiJS 8 stub scene renderer with custom hex math, and deliver the Flowgrid Home route where users see their Cells on a Core-centered hex lattice and tap to navigate.

Purpose: This plan proves the renderer/simulation boundary (D-01/D-02), the Pixi v8 async-init path, and the React app shell that Plan 03-03's Cell Board plugs into. After this plan, a user can open the app, see the Flowgrid, and tap a Cell to navigate (even though the Cell Board itself lands in Plan 03-03).
Output: Full app shell, PixiJS renderer with hex math, Flowgrid Home UI, and config for the entire frontend stack.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-playable-generator-flowgrid/03-CONTEXT.md
@.planning/phases/03-playable-generator-flowgrid/03-RESEARCH.md
@.planning/phases/03-playable-generator-flowgrid/03-PATTERNS.md

# Plan 03-01 summary (the simulation truth layer this plan consumes)
@.planning/phases/03-playable-generator-flowgrid/03-01-PLAN.md

# Phase 1/2 contracts consumed by the app shell
@src/domain/result.ts
@src/domain/records.ts
@src/simulation/engine.ts
@src/simulation/selectors.ts
@src/persistence/index.ts
@src/persistence/repository.ts
@src/persistence/errors.ts
@src/content/formulas.ts
@tests/helpers/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install locked stack, configure build/test environment, and create the app shell</name>
  <files>
    package.json,
    tsconfig.json,
    vitest.config.ts,
    vite.config.ts,
    index.html,
    src/style.css,
    src/app/main.tsx,
    src/app/App.tsx,
    src/app/routes.tsx,
    src/app/store/flowgrid-store.ts,
    src/app/store/dispatch.ts,
    src/app/env.ts,
    src/app/rng.ts,
    tests/app/dispatch.test.ts
  </files>
  <read_first>
    package.json (current dependencies and scripts at lines 1-28 — add the locked stack),
    tsconfig.json (current compilerOptions at lines 1-26 — add DOM lib, jsx, and tsx include per Pitfall 4),
    vitest.config.ts (current node-only config at lines 1-10 — restructure as workspace with happy-dom env for UI tests per Pitfall 3),
    src/persistence/index.ts (the persistence barrel the app shell imports from — repository, errors, database exports),
    src/persistence/repository.ts (FlowgridRepository class: open, loadSnapshot, applyResult — the dispatch path calls these),
    src/persistence/errors.ts (PersistenceError type — the store holds lastError of this type),
    src/domain/result.ts (SimulationCommand, SimulationResult, SimulationEnv types — the dispatch function signature),
    src/simulation/engine.ts (runSimulationCommand — the dispatch function calls this),
    src/simulation/selectors.ts (getCellById, getCore, getSettings — UI reads via selectors, never reaches into snapshot),
    src/content/formulas.ts (DEFAULT_LOCAL_DAY_BOUNDARY — env factory uses settings.localDayBoundary with deriveLocalDate),
    tests/helpers/fixtures.ts (createRng at lines 85-107 using xmur3+mulberry32 — extract to src/app/rng.ts for production use; createTestSimulationEnv at lines 116-125 — the template for src/app/env.ts),
    .planning/phases/03-playable-generator-flowgrid/03-RESEARCH.md (Standard Stack versions at lines 106-139, install block at lines 149-159, Pattern 1 Zustand store at lines 293-340, Pitfall 2 React Router mode at lines 624-628, Pitfall 3 vitest env at lines 630-633, Pitfall 4 tsconfig at lines 636-639, Code Examples env factory at lines 701-721),
    .planning/phases/03-playable-generator-flowgrid/03-PATTERNS.md (F1 env factory role-match at lines 476-493, E1-E6 config files at lines 460-473)
  </read_first>
  <action>
    Install the locked stack via npm. Dependencies: react@^19.2.7, react-dom@^19.2.7, react-router@^7.18.0 (NOT v8 — STACK.md locks v7; declarative createBrowserRouter mode, NOT framework mode per Pitfall 2 — do NOT install @react-router/dev), zustand@^5.0.14, tailwindcss@^4.3.1, @tailwindcss/vite@^4.3.1, pixi.js@^8.19.0, @radix-ui/react-dialog, @radix-ui/react-popover, @radix-ui/react-tooltip, @radix-ui/react-tabs, @radix-ui/react-menu, lucide-react@^1.21.0. Dev dependencies: @vitejs/plugin-react@^6.0.3, @testing-library/react@^16.3.2, @testing-library/user-event@^14.6.1, @testing-library/jest-dom@^6.9.1, happy-dom@^20.10.6, @types/react@^19.2.17, @types/react-dom@^19.2.3. Use the exact install commands from RESEARCH lines 149-158. Add scripts to package.json: "dev": "vite", "build": "vite build", "preview": "vite preview". Keep existing "typecheck", "lint", "test" scripts.

    Update tsconfig.json compilerOptions (Pitfall 4): change `lib` from `["ES2022"]` to `["ES2022", "DOM", "DOM.Iterable"]`, add `"jsx": "react-jsx"`. Change `include` from `["src/**/*.ts", ...]` to also include `"src/**/*.tsx"` and `"tests/**/*.tsx"`. Add `"types": ["node", "vitest/globals", "@testing-library/jest-dom"]` to replace the existing types line. Keep all strict flags (exactOptionalPropertyTypes, noUncheckedIndexedAccess, etc.) and all other options unchanged. Simulation purity stays enforced by ESLint no-restricted-imports (eslint.config.js lines 31-67), NOT by tsconfig.

    Restructure vitest.config.ts to support two test environments (Pitfall 3). Replace the single config with a Vitest workspace that has two project configs: one with `environment: 'node'` for `tests/simulation/**` and `tests/persistence/**` (existing tests unchanged), and one with `environment: 'happy-dom'` for `tests/ui/**`, `tests/app/**`, and `tests/render/**`. Both keep `setupFiles: ['tests/helpers/setup-indexeddb.ts']` and `globals: false`. Use Vitest's `defineWorkspace` or the `test.projects` / `test.workspaces` API. Ensure `include` globs cover both `.test.ts` and `.test.tsx` files. The existing node-environment simulation and persistence tests MUST keep passing unchanged.

    Create vite.config.ts exporting `defineConfig` with `plugins: [react({ include: ['**/*.tsx', '**/*.ts'] }), tailwindcss()]`. Import `@vitejs/plugin-react` as `react` and `@tailwindcss/vite` as `tailwindcss`. Set `resolve.extensions` to include `.tsx`, `.ts`, `.js`, `.jsx`. Set `root: '.'`.

    Create index.html at the project root (Vite requirement). It contains `<!DOCTYPE html>`, `<html lang="en">`, `<head>` with `<meta charset="UTF-8" />`, `<meta name="viewport" content="width=device-width, initial-scale=1.0" />`, `<title>Flowgrid</title>`, and `<body>` with `<div id="root"></div>` and `<script type="module" src="/src/app/main.tsx"></script>`.

    Create src/style.css with `@import "tailwindcss";` at the top (Tailwind v4 CSS-first per RESEARCH State of the Art line 728). Add a small `@theme` block defining CSS variables for Flowgrid cell colors (e.g., `--color-flowgrid-bg: #0f172a`, `--color-core: #fbbf24`) so Pixi and React share the same values.

    Create src/app/rng.ts exporting a `createRng(seed: string): Rng` function. Copy the xmur3+mulberry32 implementation from tests/helpers/fixtures.ts lines 61-107 verbatim (it is pure TS with no test dependencies). Import the Rng type from src/domain/result.ts. This makes the RNG available to the production env factory without importing test helpers. Per PATTERNS F1 line 492.

    Create src/app/env.ts exporting `makeEnv(now: string, settings: { localDayBoundary: string }, seed: string): SimulationEnv`. It returns `{ now, localDate: deriveLocalDate(now, settings.localDayBoundary), rng: createRng(seed), contentVersion: 'flowgrid:starter:v1' }`. Import deriveLocalDate from src/simulation/systems/day-rollover.ts (Plan 03-01) and createRng from src/app/rng.ts. Import SimulationEnv type from src/domain/result.ts. Per RESEARCH Code Examples lines 701-721.

    Create src/app/store/flowgrid-store.ts using Zustand v5 vanilla `createStore` (NOT the React `create` hook — RESEARCH Pattern 1 lines 293-311). Import `createStore` from `zustand/vanilla`. Define interface FlowgridState with: `snapshot: FlowgridSnapshot | null`, `activeSession: { cellId: string; startedAt: string } | null`, `pendingVisualEvents: readonly VisualEvent[]`, `status: 'loading' | 'ready' | 'error'`, `lastError: PersistenceError | null`. Export `flowgridStore` as the createStore result with initial values: snapshot null, activeSession null, pendingVisualEvents [], status 'loading', lastError null. Also export the FlowgridState type. Per RESEARCH Pattern 1 lines 297-311.

    Create src/app/store/dispatch.ts with two exports. First, a React-bound hook `useFlowgridStore` using `useStore` from `zustand` bound to `flowgridStore` (for React selectors). Second, an async `dispatch(command: SimulationCommand, env: SimulationEnv, repository: FlowgridRepository)` function that: reads `flowgridStore.getState().snapshot`, returns early if null, calls `runSimulationCommand(prev, command, env)`, if `result.status === 'applied'` calls `await repository.applyResult(result)`, if `apply.ok === false` sets `lastError` and returns, otherwise calls `flowgridStore.setState` updating snapshot to `result.nextState`, appending `result.visualEvents` to pendingVisualEvents, and syncing activeSession from the cell's activeSessionStartedAt field. Import runSimulationCommand from src/simulation/engine.ts, FlowgridRepository from src/persistence, types from src/domain. Per RESEARCH Pattern 1 lines 318-340.

    Create src/app/routes.tsx exporting a `router` created via `createBrowserRouter([{ path: '/', element: <FlowgridHome /> }, { path: '/cells/:cellId', element: <div>Cell Board (Plan 03-03)</div> }])`. Import createBrowserRouter from 'react-router'. Import FlowgridHome from '../ui/flowgrid-home/FlowgridHome'. Use the declarative data mode (NOT framework mode — Pitfall 2). The Cell Board route is a placeholder div for now; Plan 03-03 replaces it.

    Create src/app/App.tsx exporting an `App` component that renders `<RouterProvider router={router} />` wrapped in an error boundary that catches and renders PersistenceError. Import RouterProvider from 'react-router' and the router from routes.tsx.

    Create src/app/main.tsx as the Vite entry point: `import { createRoot } from 'react-dom/client'`, `import { App } from './App'`, `import '../style.css'`, then `createRoot(document.getElementById('root')!).render(<App />)`. Per RESEARCH Pattern 2.

    Create tests/app/dispatch.test.ts covering the dispatch function: given a starter snapshot loaded into the store, dispatching a create_cell command produces an applied result, the repository writes it, and the store emits the new snapshot. Use fake-indexeddb setup, FlowgridDatabase, FlowgridRepository, and the store. This is a node-environment test (no React rendering). Verify dispatch is async and the store state updates.

    Run typecheck, lint, and all tests to verify the new configuration is correct and existing tests are unaffected.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include react, react-dom, react-router (^7.18.0), zustand, tailwindcss, @tailwindcss/vite, pixi.js, @radix-ui/react-dialog, lucide-react
    - package.json devDependencies include @vitejs/plugin-react, @testing-library/react, @testing-library/user-event, @testing-library/jest-dom, happy-dom, @types/react, @types/react-dom
    - package.json NOT depend on @react-router/dev (declarative mode, not framework mode per Pitfall 2)
    - tsconfig.json compilerOptions.lib includes "DOM" and "DOM.Iterable", compilerOptions.jsx equals "react-jsx"
    - vitest.config.ts defines separate environments: node for tests/simulation and tests/persistence, happy-dom for tests/ui and tests/app
    - vite.config.ts exists with plugin-react and tailwindcss plugins
    - index.html exists at project root with `<div id="root">` and script src `/src/app/main.tsx`
    - src/style.css contains `@import "tailwindcss"`
    - src/app/store/flowgrid-store.ts exports `flowgridStore` created via `createStore` from `zustand/vanilla`
    - src/app/store/dispatch.ts exports async `dispatch` function and `useFlowgridStore` hook
    - src/app/env.ts exports `makeEnv` that calls `deriveLocalDate` for localDate computation
    - src/app/routes.tsx exports a router via `createBrowserRouter` with routes for `/` and `/cells/:cellId`
    - `npx tsc --noEmit` passes with zero errors
    - `npx vitest run tests/simulation/ tests/persistence/` passes (existing tests unaffected by config changes)
    - `npx vitest run tests/app/dispatch.test.ts` passes
  </acceptance_criteria>
  <done>
    Locked stack installed and configured. App shell created: main entry, router with Home and Cell Board routes, Zustand vanilla store, command dispatch path, env factory with deriveLocalDate. Build and test configs support both node and happy-dom environments. All existing tests pass unchanged. Dispatch test proves the UI→simulation→repository→store loop works.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Custom hex math module, PixiJS 8 stub scene, and Flowgrid Home UI</name>
  <files>
    src/render/flowgrid/hex-layout.ts,
    src/render/flowgrid/scene.ts,
    src/render/flowgrid/adapter.ts,
    src/ui/flowgrid-home/FlowgridHome.tsx,
    src/ui/flowgrid-home/FlowgridCanvas.tsx,
    src/ui/shared/ErrorBanner.tsx,
    tests/render/hex-layout.test.ts,
    tests/ui/flowgrid-home.test.tsx
  </files>
  <read_first>
    src/domain/records.ts (CellRecord with D-10 fields from Plan 03-01 — color, archivedAt, lastBloomLocalDate for Activation rendering),
    src/simulation/selectors.ts (getCellById, getCore — the renderer reads via selectors),
    src/app/store/flowgrid-store.ts (Plan 03-02 Task 1 — the store the adapter subscribes to for snapshot updates),
    src/app/store/dispatch.ts (Plan 03-02 Task 1 — useFlowgridStore hook for React selectors),
    src/persistence/errors.ts (PersistenceError — ErrorBanner renders this type),
    .planning/phases/03-playable-generator-flowgrid/03-RESEARCH.md (Pattern 2 Pixi v8 async init at lines 343-384, Pattern 3 hex math at lines 386-444, Architecture diagram at lines 188-247, Pitfall 1 app.canvas vs app.view at lines 618-622),
    .planning/phases/03-playable-generator-flowgrid/03-PATTERNS.md (hex-layout pattern-ref at lines 84-86, scene pattern-ref at lines 84, adapter at lines 86, FlowgridHome at lines 92)
  </read_first>
  <behavior>
    - Test: axialToPixel({q:0, r:0}, size) returns {x:0, y:0} (the Core at origin)
    - Test: axialToPixel({q:1, r:0}, size) returns a point to the right of origin
    - Test: pixelToAxial is the inverse of axialToPixel (round-trip property via fast-check)
    - Test: ringCells(0) returns exactly one coordinate [{q:0, r:0}]
    - Test: ringCells(1) returns exactly six coordinates forming a hex ring
    - Test: ringCells(2) returns exactly twelve coordinates
    - Test: axialRound resolves fractional coordinates to the nearest integer axial coordinate
    - Test: FlowgridHome renders without crashing in happy-dom (RTL smoke test)
    - Test: FlowgridHome shows a loading state when store status is 'loading'
    - Test: FlowgridHome renders an accessible heading "Flowgrid" (semantic UI pairing per PROJECT.md accessibility rule)
  </behavior>
  <action>
    Create src/render/flowgrid/hex-layout.ts as a pure TypeScript module with zero dependencies (STACK.md mandates owning this code). Export: interface `AxialCoord { readonly q: number; readonly r: number }`, function `axialToPixel(coord: AxialCoord, size: number): { x: number; y: number }` using pointy-top projection (x = size * (sqrt(3)*q + sqrt(3)/2*r), y = size * (3/2*r)), function `pixelToAxial(x: number, y: number, size: number): AxialCoord` (inverse projection + axialRound), function `axialRound(coord: { q: number; r: number }): AxialCoord` using cube rounding (compute s = -q-r, round all three, drop the one with largest diff), and function `ringCells(radius: number): readonly AxialCoord[]` returning all coordinates in hex ring N around origin (radius 0 returns just origin; radius N walks six edges of N steps each). Follow RESEARCH Pattern 3 lines 396-444 verbatim for the algorithm. This module has no Pixi imports — it is pure math that tests, layout, and hit-detection all share.

    Create src/render/flowgrid/scene.ts exporting `buildFlowgridScene(app: Application, snapshot: FlowgridSnapshot, onCellTap: (cellId: string) => void): void` and `destroyFlowgridScene(app: Application): void`. The buildFlowgridScene function: creates a Graphics object for the Core at the center (axialToPixel({q:0,r:0}, HEX_SIZE)), iterates over non-archived cells from snapshot.cells (filter archivedAt === null per D-12), assigns each cell a position using ringCells rings (ring 1 for the first 6 cells, ring 2 for cells 7-18, etc. — assign sequentially), draws each as a hex shape (Graphics.hex or polygon) at its pixel position with fill color from cell.color. Cells with `cell.lastBloomLocalDate === env.localDate` (Activated today — D-02) get a distinct border/halo. Draw static route lines from each cell to the Core (D-02 — routes as static lines, no animation). Set each cell Graphics to `interactive: true` and register a `pointertap` event that calls `onCellTap(cell.id)` (D-03 — Pixi hit-detection resolves cellId, React Router navigates). Use `app.stage` to add children. Follow RESEARCH Pattern 2 lines 347-384 for the Application lifecycle. Define a module-level constant HEX_SIZE = 48 (tunable). Import Application from 'pixi.js'. Import FlowgridSnapshot from src/domain. Import axialToPixel, ringCells from hex-layout.ts. Import getCore, cell iteration from selectors or direct Map iteration.

    Create src/render/flowgrid/adapter.ts exporting `connectFlowgridAdapter(app: Application, store: typeof flowgridStore): () => void`. It subscribes to the store via `store.subscribe` and, on snapshot changes, rebuilds the scene display objects (clear and redraw — Phase 3 has no tweening per D-02). It drains pendingVisualEvents from the store by reading and clearing them (D-02 — visual events are received but dropped/logged, not animated). Returns an unsubscribe function. This exercises the "drop visual events freely" property from day one (D-02 honors UI-04's eventual safety contract). The adapter must NOT import React (render layer imports Pixi + domain selectors only per S6 boundary rules).

    Create src/ui/flowgrid-home/FlowgridCanvas.tsx exporting a `FlowgridCanvas` component that takes `onCellTap: (cellId: string) => void` prop. It uses `useRef<HTMLDivElement>` for a container and `useEffect` to asynchronously initialize Pixi: create `new Application()`, `await app.init({ background: 0x0f172a, resizeTo: containerRef.current ?? undefined, preference: 'webgl', antialias: true })` (WebGL explicitly — RESEARCH Environment Availability line 779), then `containerRef.current.appendChild(app.canvas)` (NOT app.view — Pitfall 1), then call buildFlowgridScene with the current store snapshot, then connectFlowgridAdapter. The cleanup function sets a cancelled flag, calls destroyFlowgridScene, and `app.destroy(true)`. Follow RESEARCH Pattern 2 lines 347-384.

    Create src/ui/flowgrid-home/FlowgridHome.tsx exporting the `FlowgridHome` route component. It uses `useFlowgridStore` to read status, snapshot, and lastError. When status is 'loading', render a loading message. When status is 'error' or lastError is non-null, render `<ErrorBanner error={lastError} />`. When status is 'ready' and snapshot exists, render a semantic `<section>` with `<h1>Flowgrid</h1>` (accessible heading — PROJECT.md accessibility rule), the `<FlowgridCanvas onCellTap={handleCellTap} />` component, and a text summary of active cells count. The `handleCellTap` callback uses `useNavigate` from react-router to navigate to `/cells/${cellId}` (D-03). Import useNavigate from 'react-router'. If no active cells exist (all archived or none created), render a message prompting the user to create a Cell (the create form lands in Plan 03-03, so this is a text hint for now).

    Create src/ui/shared/ErrorBanner.tsx exporting an `ErrorBanner` component that takes `error: PersistenceError | null` and renders a visible, accessible error message using the error.kind and error.message fields. Use role="alert" for screen reader announcement. Import PersistenceError type from src/persistence/errors.ts. This is the Phase 2 typed PersistenceError contract rendered (the contract was shipped in Phase 2, consumed here).

    Create tests/render/hex-layout.test.ts with pure unit tests for axialToPixel, pixelToAxial, axialRound, and ringCells. Include a fast-check property test asserting pixelToAxial is the approximate inverse of axialToPixel (within rounding tolerance). This is a node-environment test (no DOM needed for pure math). Assert ringCells(0) has length 1, ringCells(1) has length 6, ringCells(2) has length 12.

    Create tests/ui/flowgrid-home.test.tsx as a happy-dom-environment RTL test. Test that FlowgridHome renders an accessible h1 heading with text "Flowgrid". Test the loading state renders when store status is 'loading'. Mock the Pixi Application if needed (happy-dom does not have WebGL — mock buildFlowgridScene or guard the canvas initialization). The test should not crash on import or render. Use render and screen from @testing-library/react.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/render/hex-layout.test.ts tests/ui/flowgrid-home.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/render/flowgrid/hex-layout.ts exports `axialToPixel`, `pixelToAxial`, `axialRound`, `ringCells` with zero non-domain imports
    - src/render/flowgrid/scene.ts exports `buildFlowgridScene` and `destroyFlowgridScene`; uses `app.canvas` NOT `app.view` (Pitfall 1)
    - src/render/flowgrid/scene.ts sets cell Graphics `interactive: true` and registers `pointertap` calling onCellTap (D-03)
    - src/render/flowgrid/scene.ts filters archived cells (archivedAt === null check per D-12)
    - src/render/flowgrid/adapter.ts subscribes to the store and drains pendingVisualEvents (D-02 drop)
    - src/ui/flowgrid-home/FlowgridCanvas.tsx uses `await app.init()` (v8 async init) and `app.canvas` (not app.view)
    - src/ui/flowgrid-home/FlowgridHome.tsx renders semantic `<h1>Flowgrid</h1>` and calls `useNavigate().navigate(/cells/${cellId})` on cell tap
    - src/ui/shared/ErrorBanner.tsx renders PersistenceError with role="alert"
    - `npx vitest run tests/render/hex-layout.test.ts` passes including the fast-check inverse property
    - `npx vitest run tests/ui/flowgrid-home.test.tsx` passes (FlowgridHome renders without crashing in happy-dom)
    - `npx tsc --noEmit` passes
    - `npx eslint src/render/ src/ui/` passes (layer boundary rules: render does not import React/Dexie, ui does not import Pixi/Dexie directly)
  </acceptance_criteria>
  <done>
    Custom hex math module tested with property-based inverse assertions. PixiJS 8 stub scene renders Core, non-archived Cells in rings, routes as static lines, and Activation halos. Tap hit-detection resolves cellId and triggers Router navigation to /cells/:cellId (D-03). Visual events dropped by the adapter (D-02). Flowgrid Home renders with accessible heading, loading state, and error banner. All tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → Pixi canvas | User pointer events cross here; Pixi hit-detection resolves cellId from pixel coordinates |
| Zustand store → React UI | Snapshot emit crosses here; UI reads selectors, never mutates |
| repository → store | PersistenceError values cross here on write/load failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | Pixi pointer event → cellId resolution | mitigate | pixelToAxial + axialRound use cube rounding to eliminate floating-point tap misses; cellId comes from the snapshot Map, not from user input |
| T-03-07 | Tampering | Visual event drop changing durable state | mitigate | D-02 adapter drains and drops visual events; durable truth lives in the Zustand snapshot which only changes via dispatch → runSimulationCommand (UI-04 contract exercised from day one) |
| T-03-08 | Denial of Service | Pixi WebGL context loss | accept | Phase 3 stub scene has no animation loop; context loss is recoverable on reload; Phase 6 hardening owns full resilience |
| T-03-09 | Information Disclosure | PersistenceError message rendering | accept | Error messages are user-facing local-first strings; no sensitive data in PersistenceError payloads |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with DOM/JSX config active
- `npx eslint .` passes including layer boundary rules (render no React/Dexie, ui no Pixi/Dexie)
- `npx vitest run` passes all tests across both environments (node + happy-dom)
- `npm run dev` starts the Vite dev server without errors
- Flowgrid Home renders in a browser with the Pixi canvas showing Core + Cells (manual check via dev server)
</verification>

<success_criteria>
- Locked stack installed: React 19, react-router 7, Tailwind 4, PixiJS 8, Zustand 5, Radix, lucide-react
- Build/test config supports DOM/JSX and dual test environments (node + happy-dom)
- App shell created: main entry, router, Zustand store, dispatch path, env factory
- PixiJS 8 stub scene renders Core-centered hex Flowgrid with static state (D-01, D-02, D-04)
- Tap navigates to Cell Board route via React Router (D-03)
- Archived Cells hidden from Flowgrid Home (D-12)
- Activated Cells show distinct visual state (D-02)
- Visual events dropped by adapter, proving renderer/simulation safety boundary (D-02)
- PersistenceError rendered in accessible error banner
</success_criteria>

<output>
Create `.planning/phases/03-playable-generator-flowgrid/03-02-SUMMARY.md` when done
</output>

<artifacts>
## Artifacts this phase produces

### New exported symbols (src/render/flowgrid)
- `AxialCoord` interface (hex-layout.ts)
- `axialToPixel(coord, size)` function (hex-layout.ts)
- `pixelToAxial(x, y, size)` function (hex-layout.ts)
- `axialRound(coord)` function (hex-layout.ts)
- `ringCells(radius)` function (hex-layout.ts)
- `buildFlowgridScene(app, snapshot, onCellTap)` function (scene.ts)
- `destroyFlowgridScene(app)` function (scene.ts)
- `connectFlowgridAdapter(app, store)` function (adapter.ts)

### New exported symbols (src/app)
- `flowgridStore` Zustand vanilla store instance (store/flowgrid-store.ts)
- `FlowgridState` interface (store/flowgrid-store.ts)
- `dispatch(command, env, repository)` async function (store/dispatch.ts)
- `useFlowgridStore` React-bound selector hook (store/dispatch.ts)
- `makeEnv(now, settings, seed)` function (env.ts)
- `createRng(seed)` function (rng.ts)
- `router` React Router instance (routes.tsx)
- `App` React component (App.tsx)

### New exported symbols (src/ui)
- `FlowgridHome` React component (flowgrid-home/FlowgridHome.tsx)
- `FlowgridCanvas` React component (flowgrid-home/FlowgridCanvas.tsx)
- `ErrorBanner` React component (shared/ErrorBanner.tsx)

### New file paths
- vite.config.ts
- index.html
- src/style.css
- src/app/main.tsx
- src/app/App.tsx
- src/app/routes.tsx
- src/app/store/flowgrid-store.ts
- src/app/store/dispatch.ts
- src/app/env.ts
- src/app/rng.ts
- src/render/flowgrid/hex-layout.ts
- src/render/flowgrid/scene.ts
- src/render/flowgrid/adapter.ts
- src/ui/flowgrid-home/FlowgridHome.tsx
- src/ui/flowgrid-home/FlowgridCanvas.tsx
- src/ui/shared/ErrorBanner.tsx
- tests/render/hex-layout.test.ts
- tests/ui/flowgrid-home.test.tsx
- tests/app/dispatch.test.ts
</artifacts>
