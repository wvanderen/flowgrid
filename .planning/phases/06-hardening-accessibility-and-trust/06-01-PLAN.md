---
phase: 06-hardening-accessibility-and-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/records.ts
  - src/domain/result.ts
  - src/simulation/engine.ts
  - src/simulation/commands/update-settings.ts
  - src/simulation/operation-events.ts
  - src/persistence/database.ts
  - src/persistence/validation-schemas.ts
  - src/persistence/export-json.ts
  - src/persistence/seeding.ts
  - src/app/routes.tsx
  - src/ui/settings/SettingsPanel.tsx
  - src/ui/settings/reduce-motion.ts
  - src/ui/flowgrid-home/FlowgridHome.tsx
  - tests/simulation/update-settings.test.ts
  - tests/properties/update-settings.property.test.ts
  - tests/persistence/migration-harness.test.ts
autonomous: true
requirements: [UI-06]
user_setup: []

must_haves:
  truths:
    - "User can open /settings and see current default session length, daily target, local day boundary, and a reduced-motion toggle"
    - "User can edit settings defaults (session length, daily target, day boundary, reduceMotion) and the values persist across reload"
    - "User can export full local state as a JSON file download and sessions as a CSV file download from /settings"
    - "User can import a JSON archive that replaces local state after an explicit confirmation dialog"
    - "Changing default session length or daily target in Settings does NOT reshape any existing Cell (D-12)"
    - "An existing v4 Dexie database migrates to v5 with reduceMotion defaulting to false"
  artifacts:
    - path: "src/simulation/commands/update-settings.ts"
      provides: "update_settings command handler (validate → apply → emit operation → return)"
      contains: "export function updateSettings"
    - path: "src/ui/settings/SettingsPanel.tsx"
      provides: "/settings route component reading snapshot.settings, dispatching update_settings, export/import buttons"
      contains: "SettingsPanel"
    - path: "src/persistence/database.ts"
      provides: "Dexie version(5) block with upgradeSettingsV4ToV5 transform"
      contains: "version(5)"
    - path: "src/domain/records.ts"
      provides: "SettingsRecord with reduceMotion field"
      contains: "reduceMotion"
  key_links:
    - from: "src/app/routes.tsx"
      to: "src/ui/settings/SettingsPanel.tsx"
      via: "createBrowserRouter entry with path /settings"
      pattern: "path: '/settings'"
    - from: "src/ui/settings/SettingsPanel.tsx"
      to: "src/simulation/commands/update-settings.ts"
      via: "dispatch(update_settings command) through the app store"
      pattern: "type: 'update_settings'"
    - from: "src/ui/settings/SettingsPanel.tsx"
      to: "src/persistence/export-json.ts"
      via: "exportJson(db) call on the Export JSON button"
      pattern: "exportJson"
    - from: "src/simulation/engine.ts"
      to: "src/simulation/commands/update-settings.ts"
      via: "dispatcher switch case for update_settings"
      pattern: "case 'update_settings'"
---

## Phase Goal

**As a** Flowgrid user, **I want to** configure my app defaults, toggle reduced motion, and back up or restore my state, **so that** I can trust Flowgrid as a personalized, recoverable daily tool.

## Objective

Deliver the complete Settings vertical slice (UI-06): the `update_settings` simulation command, the new `SettingsRecord.reduceMotion` field with a Dexie v4→v5 migration, the `/settings` route with a SettingsPanel (D-10 — defaults editing + reduced-motion toggle + export/import), and full test coverage (unit + property + migration harness). After this plan, a user can open /settings, change defaults that persist, toggle reduced motion, export JSON/CSV, and import-replace — all through durable simulation commands and the established repository path.

Purpose: This is the truth-layer-plus-UI foundation for Phase 6. The `reduceMotion` field and `update_settings` command are prerequisites for Plan 06-02 (animated renderer reads reduceMotion) and 06-03 (Cell list renders alongside the Settings link). UI-06 is closed by this plan.

Output: `update_settings` command + handler, `SettingsRecord.reduceMotion`, Dexie v5 migration, `/settings` route + SettingsPanel, export/import UI, unit/property/migration tests.

## Execution Context

@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-accessibility-and-trust/06-CONTEXT.md
@.planning/phases/06-hardening-accessibility-and-trust/06-RESEARCH.md
@.planning/phases/06-hardening-accessibility-and-trust/06-PATTERNS.md

@src/simulation/commands/set-core-allocation.ts
@src/domain/records.ts
@src/domain/result.ts
@src/domain/validation.ts
@src/simulation/engine.ts
@src/simulation/operation-events.ts
@src/persistence/database.ts
@src/persistence/validation-schemas.ts
@src/persistence/export-json.ts
@src/persistence/export-csv.ts
@src/persistence/seeding.ts
@src/persistence/index.ts
@src/app/routes.tsx
@src/ui/core-panel/CorePanel.tsx
@src/ui/flowgrid-home/FlowgridHome.tsx
@src/app/store/dispatch.ts
@src/content/formulas.ts

## Artifacts this phase produces

- **Command variant:** `update_settings` (added to `SimulationCommand` union in `src/domain/result.ts`)
- **Command interface:** `UpdateSettingsCommand { type: 'update_settings'; operationId; defaultSessionLengthSeconds; dailyTargetSeconds; localDayBoundary; reduceMotion }`
- **Command handler:** `updateSettings(previousState, command, env)` in `src/simulation/commands/update-settings.ts`
- **Domain field:** `SettingsRecord.reduceMotion: boolean` in `src/domain/records.ts`
- **Persistence export:** `upgradeSettingsV4ToV5` pure transform + `SETTINGS_V5_DEFAULTS` const + `version(5)` Dexie block in `src/persistence/database.ts`
- **Schema:** `settingsSchema.reduceMotion: z.boolean().default(false)` + `_settingsSchemaCheck` drift guard in `src/persistence/validation-schemas.ts`
- **UI helper:** `effectiveReduceMotion(setting: boolean): boolean` in `src/ui/settings/reduce-motion.ts`
- **Route:** `/settings` path in `src/app/routes.tsx`
- **Component:** `SettingsPanel` in `src/ui/settings/SettingsPanel.tsx`
- **Tests:** `tests/simulation/update-settings.test.ts`, `tests/properties/update-settings.property.test.ts`, v4→v5 fixture in `tests/persistence/migration-harness.test.ts`

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: update_settings command + SettingsRecord.reduceMotion + Dexie v5 migration + schemas</name>
  <files>src/domain/records.ts, src/domain/result.ts, src/simulation/engine.ts, src/simulation/commands/update-settings.ts, src/simulation/operation-events.ts, src/persistence/database.ts, src/persistence/validation-schemas.ts, src/persistence/export-json.ts, src/persistence/seeding.ts</files>
  <read_first>
    - src/simulation/commands/set-core-allocation.ts (the EXACT template — singleton-targeting validate→apply→emit→return; reject-with-issues pattern)
    - src/domain/records.ts lines 143-149 (SettingsRecord current shape)
    - src/domain/result.ts lines 44-49 (SetCoreAllocationCommand interface shape) + lines 159-173 (SimulationCommand union)
    - src/domain/validation.ts (ValidationIssueCode union — use invalid_operation_shape for malformed settings input)
    - src/simulation/engine.ts (dispatcher switch — add case)
    - src/simulation/operation-events.ts lines 17-81 (entityTypeForCommand + entityIdForCommand switches — add settings case)
    - src/persistence/database.ts lines 105-135 (FORGE_HISTORY_V4_DEFAULTS + upgradeForgeHistoryV3ToV4 — the byte-for-byte template) + lines 222-235 (version(4) block — the template for version(5))
    - src/persistence/validation-schemas.ts lines 109-115 (settingsSchema) + lines 69-70 (coreSchema .default() precedent for backward-compat) + lines 200-209 (drift guard pattern)
    - src/persistence/export-json.ts lines 42-45 (ARCHIVE_VERSION + JsonArchive.archiveVersion)
    - src/persistence/seeding.ts lines 61-67 (seeded settings singleton)
    - src/content/formulas.ts (DEFAULT_SESSION_LENGTH_SECONDS, DEFAULT_DAILY_TARGET_SECONDS, DEFAULT_LOCAL_DAY_BOUNDARY)
  </read_first>
  <behavior>
    - Test 1: dispatching update_settings with valid fields writes a SettingsRecord carrying the new reduceMotion value and appends one operation with entityType 'settings'.
    - Test 2: dispatching update_settings with a non-positive defaultSessionLengthSeconds is rejected with code invalid_operation_shape and nextState byte-identical to previousState.
    - Test 3: dispatching update_settings with a malformed localDayBoundary (not HH:MM) is rejected with invalid_operation_shape.
    - Test 4: existing Cells' dailyMilestoneTargetSeconds are unchanged after update_settings (D-12 — defaults affect new Cells only).
    - Test 5: the v4→v5 upgrade transform sets reduceMotion to false on a v4 settings row lacking the field.
  </behavior>
  <action>
    Add `readonly reduceMotion: boolean` to SettingsRecord in records.ts, placed before `updatedAt` (metadata-last convention). Declare `UpdateSettingsCommand` interface in result.ts peer to SetCoreAllocationCommand: fields `type: 'update_settings'`, `operationId: OperationId`, `defaultSessionLengthSeconds: IntSeconds`, `dailyTargetSeconds: IntSeconds`, `localDayBoundary: string`, `reduceMotion: boolean`. Append `| UpdateSettingsCommand` to the SimulationCommand union.

    Create src/simulation/commands/update-settings.ts mirroring set-core-allocation.ts exactly: accumulate typed ValidationIssue[] (no throw). Validate defaultSessionLengthSeconds and dailyTargetSeconds are positive integers; validate localDayBoundary matches the HH:MM shape used by deriveLocalDate (reuse the same shape check or a regex matching two digits, colon, two digits); reject with code invalid_operation_shape, entityType 'settings', entityId previousState.settings.id when invalid. On valid input, build nextState.settings as a new SettingsRecord spreading previousState.settings with the four editable fields overridden and updatedAt set to env.now; bump client.updatedAt; append one operation via operationFromCommand with options.entityId previousState.settings.id and payload carrying the four fields. Return status applied with visualEvents empty (settings has no visual event).

    Add `case 'update_settings': return updateSettings(previousState, command, env);` to the engine.ts dispatcher switch. Add `case 'update_settings': return 'settings';` to entityTypeForCommand and `case 'update_settings': return fallback;` to entityIdForCommand in operation-events.ts (EntityType already includes 'settings' — no union change needed).

    In database.ts, export `SETTINGS_V5_DEFAULTS = { reduceMotion: false } as const` and `upgradeSettingsV4ToV5(row: Record<string, unknown>): Record<string, unknown>` that sets row.reduceMotion to the default when undefined (mirrors upgradeForgeHistoryV3ToV4 line-for-line). Add a `this.version(5).stores({ ... })` block repeating the full 10-store set verbatim from version(4), with `.upgrade(async (tx) => { await tx.table('settings').toCollection().modify(upgradeSettingsV4ToV5); })`. Settings is a singleton so the transform runs once.

    In validation-schemas.ts, add `reduceMotion: z.boolean().default(false)` to settingsSchema (the .default(false) is the load-bearing backward-compat trick per RESEARCH Pitfall 4 / Open Question Q2 — a v2 archive lacking the field parses and defaults, so NO ARCHIVE_VERSION bump is required). Add a `_settingsSchemaCheck` drift guard matching the existing _sessionSchemaCheck pattern. Confirm export-json.ts ARCHIVE_VERSION stays at 2 (do NOT bump — document the decision in a code comment: field-additive via .default follows the Phase 4 coreSchema precedent).

    In seeding.ts, add `reduceMotion: false` to the seeded settings singleton (pure constant — keep seeding DOM-free and testable; D-09 OS-preference honoring happens in the UI layer via Task 2's effectiveReduceMotion + first-load check, not in persistence).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/simulation/update-settings.test.ts tests/persistence/migration-harness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/domain/records.ts SettingsRecord interface contains `reduceMotion: boolean` before updatedAt
    - src/domain/result.ts SimulationCommand union includes UpdateSettingsCommand and an UpdateSettingsCommand interface with type 'update_settings'
    - src/simulation/commands/update-settings.ts exports function updateSettings(previousState, command, env) returning SimulationResult
    - src/simulation/engine.ts contains `case 'update_settings'`
    - src/simulation/operation-events.ts entityTypeForCommand contains `case 'update_settings': return 'settings'` and entityIdForCommand contains `case 'update_settings': return fallback`
    - src/persistence/database.ts contains `version(5)` and `upgradeSettingsV4ToV5` and `SETTINGS_V5_DEFAULTS`
    - src/persistence/validation-schemas.ts settingsSchema contains `reduceMotion: z.boolean().default(false)` and a _settingsSchemaCheck drift guard
    - src/persistence/export-json.ts ARCHIVE_VERSION remains 2 (no bump)
    - src/persistence/seeding.ts seeded settings includes reduceMotion
    - npx tsc --noEmit exits 0
    - tests/simulation/update-settings.test.ts exits 0 (applied + rejected + D-12 cases)
    - tests/persistence/migration-harness.test.ts v4→v5 fixture exits 0
  </acceptance_criteria>
  <done>update_settings command validates and applies settings edits; SettingsRecord carries reduceMotion; Dexie v5 migration defaults the field; schemas + drift guard in place; tsc + unit + migration tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /settings route + SettingsPanel + export/import UI + Settings header link</name>
  <files>src/app/routes.tsx, src/ui/settings/SettingsPanel.tsx, src/ui/settings/reduce-motion.ts, src/ui/flowgrid-home/FlowgridHome.tsx</files>
  <read_first>
    - src/ui/core-panel/CorePanel.tsx (THE route-peer template: imports, store wiring, local-state form fields, clamp helper, buildEnv, dispatch, lastRejection surface, section shell)
    - src/ui/forge-panel/ForgePanel.tsx (second route-peer reference, shorter — header + Home link shell)
    - src/app/routes.tsx (route table — add /settings peer)
    - src/app/store/dispatch.ts (dispatch + useFlowgridStore + lastRejection population on rejection)
    - src/ui/flowgrid-home/FlowgridHome.tsx lines 78-82 (header Link to /core — add Settings sibling)
    - src/persistence/index.ts (barrel — exportJson, exportSessionCsv, importArchive all re-exported here)
    - src/persistence/export-json.ts (exportJson signature) + src/persistence/export-csv.ts (exportSessionCsv signature)
    - src/persistence/import.ts (importArchive replace-mode signature)
    - src/content/formulas.ts (DEFAULT_SESSION_LENGTH_SECONDS etc. for form min/max hints)
    - Task 1's update-settings.ts (the command shape the panel dispatches)
  </read_first>
  <behavior>
    - Test 1: navigating to /settings renders SettingsPanel with the current settings values populated from the store.
    - Test 2: changing the reduced-motion toggle and saving dispatches update_settings and the persisted value reflects on reload.
    - Test 3: the Export JSON button triggers a file download (Blob + anchor) with the exportJson output.
    - Test 4: the Import button opens a confirmation dialog; confirming calls importArchive in replace mode.
    - Test 5: the Home header shows a Settings link next to the Core link.
  </behavior>
  <action>
    Create src/ui/settings/reduce-motion.ts exporting `effectiveReduceMotion(setting: boolean): boolean` that returns the persisted setting (the persisted boolean IS the effective value). Also export `prefersReducedMotion(): boolean` guarded by `typeof window !== 'undefined'` reading window.matchMedia('(prefers-reduced-motion: reduce)').matches with a false fallback — used by the first-load OS-preference honoring (D-09).

    Create src/ui/settings/SettingsPanel.tsx mirroring CorePanel.tsx's structure: loading guard (if snapshot null return a status placeholder), a `<section aria-label="Settings" className="mx-auto max-w-5xl px-4 py-6 space-y-6">` shell, a header `<h2>Settings</h2>` with a `<Link to="/">Home</Link>`. Use local controlled-input state seeded from snapshot.settings for defaultSessionLengthSeconds, dailyTargetSeconds, localDayBoundary (text), and reduceMotion (checkbox). Add a clamp/validate helper mirroring CorePanel's clampPercent for the numeric fields (positive integers). Build a `SETTINGS_SEED = 'flowgrid-settings-seed'` + `buildSettingsEnv(localDayBoundary)` mirroring CorePanel's buildCoreEnv. On save, dispatch an UpdateSettingsCommand `{ type: 'update_settings', operationId: crypto.randomUUID(), defaultSessionLengthSeconds, dailyTargetSeconds, localDayBoundary, reduceMotion }` via the dispatch/makeEnv/repository trio. Render the lastRejection surface (`<p role="status" aria-live="polite">`) verbatim from CorePanel for invalid-input feedback. Add inline hints for invalid session-length/boundary values mirroring the Core allocation inline-sum hint pattern.

    For D-11 export: add two buttons. "Export full state (JSON)" calls exportJson(db) (db via the repository or a FlowgridDatabase instance from the app's database gateway) then triggers a download via a small `triggerDownload(filename, mime, content)` helper (create it inline or in src/ui/shared/download.ts — a Blob + temporary anchor click). "Export sessions (CSV)" calls exportSessionCsv(db) the same way. Both go through the persistence barrel (src/persistence/index.ts), never importing Dexie directly (UI layer rule).

    For D-13 import: add an "Import (replace local state)" button that opens a Radix Dialog (exact idiom from FlowgridHome.tsx lines 110-126) containing a file input, a confirmation warning that local state will be replaced, and a confirm button that reads the file, calls importArchive(db, parsed, 'replace') (replace mode only — merge is NOT exposed per D-13), and triggers an app reload or store re-load on success. Wrap the parse in the existing import-validation path (importValidation/importArchive already runs Zod + reference + resource checks per DATA-06).

    For D-09 first-run OS honoring: in SettingsPanel's mount effect (or the app bootstrap), if prefersReducedMotion() returns true and the current settings.reduceMotion is false AND no prior update_settings operation exists for reduceMotion, dispatch a one-time update_settings to persist reduceMotion true. Keep this simple — a code comment documenting the one-time intent is sufficient; the exact guard (checking operations log vs a client flag) is the executor's discretion but must not re-fire on every load.

    Add the /settings route to src/app/routes.tsx: import SettingsPanel and add `{ path: '/settings', element: <SettingsPanel /> }` as a fifth entry peer to /core and /forge.

    Add a Settings header link to FlowgridHome.tsx: next to the existing `<Link to="/core">` at line 81, add a sibling `<Link to="/settings" className="text-sm text-slate-400 underline">Settings</Link>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --dom happy-dom 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/routes.tsx contains `path: '/settings'` with element SettingsPanel
    - src/ui/settings/SettingsPanel.tsx exports function SettingsPanel and contains aria-label="Settings", a reduceMotion checkbox control, an Export JSON button, an Export CSV button, and an Import button
    - src/ui/settings/SettingsPanel.tsx dispatches a command with type 'update_settings'
    - src/ui/settings/reduce-motion.ts exports effectiveReduceMotion and prefersReducedMotion
    - src/ui/flowgrid-home/FlowgridHome.tsx contains a Link to "/settings"
    - SettingsPanel reads from the persistence barrel (no direct Dexie import)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>/settings route renders SettingsPanel with editable defaults + reduced-motion toggle + working export (JSON/CSV) + import-replace-with-confirm; Settings link in Home header; effectiveReduceMotion helper exists for Plan 06-02.</done>
</task>

<task type="auto">
  <name>Task 3: update_settings unit + property tests + v4→v5 migration harness fixture</name>
  <files>tests/simulation/update-settings.test.ts, tests/properties/update-settings.property.test.ts, tests/persistence/migration-harness.test.ts</files>
  <read_first>
    - tests/simulation/run-forge.test.ts (THE acceptance-test analog: snapshot-with-overrides helpers, one test() per acceptance bullet, buildStarterSnapshot + createTestSimulationEnv from tests/helpers/fixtures.ts, expectValidState + expectReplayEqual)
    - tests/properties/forge-safety.property.test.ts (THE property-test analog: fc.assert(fc.property(arb, ...)) with numRuns 100; rejected-result invariant at lines 92-95)
    - tests/persistence/migration-harness.test.ts (runMigrationFixture entries — the v3→v4 forgeHistory block is the template for the v4→v5 settings block)
    - tests/helpers/fixtures.ts (buildStarterSnapshot, createTestSimulationEnv, expectValidState, expectReplayEqual)
    - src/simulation/commands/update-settings.ts (Task 1 output — the handler under test)
  </read_first>
  <action>
    Create tests/simulation/update-settings.test.ts mirroring run-forge.test.ts structure: import buildStarterSnapshot + createTestSimulationEnv + expectValidState + expectReplayEqual from tests/helpers/fixtures.ts. Write one test() per behavior bullet: (1) valid update_settings applies and nextState.settings reflects all four fields + one operation appended with entityType settings; (2) non-positive session length rejected with invalid_operation_shape, nextState byte-identical to previousState, operations empty; (3) malformed localDayBoundary rejected identically; (4) existing Cells' dailyMilestoneTargetSeconds unchanged after applying update_settings (D-12 — assert the Cell records in nextState.cells equal those in previousState.cells); (5) deterministic replay — expectReplayEqual confirms a second run with the same command+env produces byte-identical results.

    Create tests/properties/update-settings.property.test.ts mirroring forge-safety.property.test.ts: use fast-check fc.assert with numRuns 100. Property 1 (well-formed settings): for arbitrary valid positive-integer session lengths, daily targets, HH:MM boundaries, and boolean reduceMotion, update_settings always returns status applied with a SettingsRecord whose fields equal the command inputs. Property 2 (D-12 durability): for any update_settings applied to a snapshot with existing Cells, every existing Cell's dailyMilestoneTargetSeconds in nextState equals its value in previousState (defaults never back-fill). Property 3 (rejection cleanliness): for arbitrary invalid inputs (non-positive or non-integer lengths, malformed boundary), the result is rejected with nextState === previousState and operations empty.

    Add a v4→v5 settings migration fixture to tests/persistence/migration-harness.test.ts: a new describe('v4 -> v5 settings migration (upgradeSettingsV4ToV5)') block parallel to the v3→v4 forgeHistory block. Define a V4Settings interface (id, defaultSessionLengthSeconds, dailyTargetSeconds, localDayBoundary, updatedAt — no reduceMotion). Run runMigrationFixture with input lacking reduceMotion, upgrade calling upgradeSettingsV4ToV5({...old}), and expected including reduceMotion false. Import upgradeSettingsV4ToV5 from ../../src/persistence/database.js.
  </action>
  <verify>
    <automated>npx vitest run tests/simulation/update-settings.test.ts tests/properties/update-settings.property.test.ts tests/persistence/migration-harness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/simulation/update-settings.test.ts exits 0 with at least 5 test cases (applied, rejected-shape, rejected-boundary, D-12-unchanged, replay)
    - tests/properties/update-settings.property.test.ts exits 0 with at least 3 properties (well-formed, D-12 durability, rejection cleanliness)
    - tests/persistence/migration-harness.test.ts contains a describe block for v4 -> v5 settings migration and exits 0
    - npx vitest run (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>update_settings has unit coverage (applied/rejected/D-12/replay), property coverage (invariants over 100 runs), and the v4→v5 migration is exercised by the harness; full vitest suite green.</done>
</task>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → simulation | Settings form values cross here (session length, day boundary, reduceMotion) |
| imported archive → local state | D-13 replace-mode import crosses here (untrusted JSON) |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | update_settings command input | mitigate | Typed ValidationIssue[] (no throw); invalid_operation_shape rejects non-positive lengths and malformed HH:MM boundary before any state write |
| T-06-02 | Tampering | D-13 import (replace mode) | mitigate | Round-trips Phase 2 DATA-06 validation (Zod schema + reference checks + resource invariants) before any local write; replace mode only (merge not exposed); explicit confirmation dialog |
| T-06-03 | Tampering | DOM-based settings injection | mitigate | Controlled React inputs; values clamped client-side; simulation re-validates independently of the form |
| T-06-04 | Information disclosure | Export downloads | accept | Local-only file download; no network; user-initiated; standard browser save dialog |

## Verification

- `npx tsc --noEmit` passes (exhaustive switch catches the new union variant)
- `npx vitest run` full suite passes (no regressions from the new command/migration/schema)
- `/settings` route renders and persists edits (verified by the component behavior + unit tests; full browser click-flow is Plan 06-04 VER-04)

## Success Criteria

- [ ] User can open /settings and edit all four settings fields (UI-06)
- [ ] Settings persist across reload via update_settings + Dexie v5
- [ ] Export JSON + Export CSV buttons trigger downloads
- [ ] Import-replace flows through DATA-06 validation with confirmation
- [ ] Existing v4 databases migrate cleanly to v5
- [ ] D-12 holds: existing Cells unaffected by default changes (property-tested)

## Output

Create `.planning/phases/06-hardening-accessibility-and-trust/06-01-SUMMARY.md` when done
