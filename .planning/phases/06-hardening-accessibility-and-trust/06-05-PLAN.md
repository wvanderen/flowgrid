---
phase: 06-hardening-accessibility-and-trust
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-02"]
files_modified:
  - src/ui/flowgrid-home/particle-anchors.ts
  - src/ui/flowgrid-home/FlowgridCanvas.tsx
  - src/ui/settings/SettingsPanel.tsx
  - tests/ui/particle-anchors.test.ts
  - tests/ui/settings-reduce-motion.test.tsx
autonomous: false
gap_closure: true
requirements: [UI-03, VER-06]
user_setup: []

must_haves:
  truths:
    - "Particle anchor coordinates are container-local — core at {0,0}, cells at their hex-local positions, routes at their line endpoints — matching the coordinate space the hexes themselves use"
    - "Visiting /settings with OS prefers-reduced-motion enabled does not durably persist reduceMotion=true; the checkbox pre-fills as a non-committal suggestion but no durable write occurs without an explicit Save"
    - "Particle Current trails, Bloom bursts, Core convert/store ripples, Activation pulses, and forge/token flashes are visible inside the canvas viewport during their respective events in a real browser"
  artifacts:
    - path: "src/ui/flowgrid-home/particle-anchors.ts"
      provides: "Pure, pixi.js-free buildParticleAnchors helper returning container-local particle anchors"
      exports: ["buildParticleAnchors"]
      min_lines: 20
    - path: "tests/ui/particle-anchors.test.ts"
      provides: "Regression test asserting anchors are container-local (not stage-local)"
      contains: "toEqual"
    - path: "src/ui/settings/SettingsPanel.tsx"
      provides: "SettingsPanel with session-only OS-preference pre-fill (no durable auto-persist in the mount effect)"
    - path: "tests/ui/settings-reduce-motion.test.tsx"
      provides: "Regression test asserting the mount effect does not dispatch update_settings"
  key_links:
    - from: "src/ui/flowgrid-home/FlowgridCanvas.tsx"
      to: "src/ui/flowgrid-home/particle-anchors.ts"
      via: "import { buildParticleAnchors } from './particle-anchors.js' — replaces the inline function"
      pattern: "from './particle-anchors"
    - from: "src/ui/flowgrid-home/particle-anchors.ts"
      to: "src/render/flowgrid/scene.ts"
      via: "import type SceneRefs — type-only, no runtime pixi.js dependency"
      pattern: "import type.*SceneRefs"
---

<objective>
Close the Phase 06 UAT Test 1 gap (severity: major) — particle animations are invisible because buildParticleAnchors emits coordinates in stage/canvas space while the ParticleContainer is parented inside the centered scene container, causing Pixi to double-apply the centering transform. All particles land off-canvas at approximately (2 × canvasCenterX, 2 × canvasCenterY).

This plan fixes the universal root cause (coordinate-space mismatch) and the environment-conditional secondary contributor (SettingsPanel auto-persisting reduceMotion=true from the OS preference), then adds automated regression guards and a human visual verification checkpoint.

Purpose: Restore the visible animation contract that UI-03 and VER-06 require — Current trails, Bloom bursts, Core ripples, and forge/token flashes must be observable by the user during real gameplay.
Output: Fixed particle anchor math (extracted to a pure testable module), session-only OS-preference honoring in SettingsPanel, two regression tests, and a human-verified visual smoke.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-accessibility-and-trust/06-UAT.md
@.planning/phases/06-hardening-accessibility-and-trust/06-02-SUMMARY.md
@.planning/debug/no-canvas-animation.md

@src/ui/flowgrid-home/FlowgridCanvas.tsx
@src/render/flowgrid/scene.ts
@src/render/flowgrid/particles.ts
@src/ui/settings/SettingsPanel.tsx
@src/ui/settings/reduce-motion.ts
@src/render/flowgrid/hex-layout.ts
@tests/render/hex-layout.test.ts
@tests/ui/cell-list-a11y.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract buildParticleAnchors to a pure module and fix the coordinate-space bug</name>
  <files>src/ui/flowgrid-home/particle-anchors.ts, src/ui/flowgrid-home/FlowgridCanvas.tsx, tests/ui/particle-anchors.test.ts</files>
  <behavior>
    - With a SceneRefs whose container is at stage offset (400, 300) and one CellView at container-local (83, 48): buildParticleAnchors returns core deep-equal { x: 0, y: 0 } and cells.get(cellId) deep-equal { x: 83, y: 48 } (NOT 483/348 which would indicate the container offset was added).
    - With one RouteView at container-local from (0,0) to (83, 48): routes.get(routeId).from deep-equal { x: 0, y: 0 } and .to deep-equal { x: 83, y: 48 } (NOT 400/300 and 483/348).
    - The function never offsets any anchor by the container's stage position — anchors stay in the same space the hexes use (axialToPixel output), because the ParticleContainer inherits the scene container's centering transform.
  </behavior>
  <action>
    ROOT CAUSE (confirmed in .planning/debug/no-canvas-animation.md): the inline buildParticleAnchors in FlowgridCanvas.tsx (lines ~227-249) adds the scene container's stage offset to each hex-local position, producing stage/canvas coordinates. But the ParticleContainer (sceneRefs.particleLayer) is a CHILD of the centered scene container (scene.ts ~line 243: container.addChild(particleLayer)), so Pixi applies the container's centering transform a SECOND time to every particle. The result: every emitted particle (Bloom burst, current-flow trail, Core convert/store ripple, Activation pulse, forge-roll flash, module-upgrade flash, token-granted flash) renders at roughly (2 × canvasCenterX, 2 × canvasCenterY) — the bottom-right off-canvas corner. The emission pipeline, ticker, and per-frame motion update are all healthy; particles simply never appear in the visible canvas region.

    Step 1 — Write the test FIRST (TDD RED). Create tests/ui/particle-anchors.test.ts (plain .ts — no JSX needed; this is a pure-function test). Import { buildParticleAnchors } from ../../src/ui/flowgrid-home/particle-anchors.js. Import { expect, test } from 'vitest'. Construct a minimal SceneRefs-shaped object using inline object literals typed loosely (cast through unknown if needed, since SceneRefs imports Container/ParticleContainer from pixi.js which is unavailable under happy-dom — the function only reads the numeric x/y fields). Set container: { x: 400, y: 300, label: 'flowgrid-scene' } as the container stub (a typical canvas-center offset on an 800x600 canvas); add one CellView stub: cells = new Map([['flowgrid:cell:test', { cellId: 'flowgrid:cell:test', x: 83, y: 48, color: 0x6b7280, activatedToday: false }]]); add one RouteView stub: routes = new Map([['flowgrid:route:0', { routeId: 'flowgrid:route:0', fromX: 0, fromY: 0, toX: 83, toY: 48 }]]). The remaining SceneRefs fields (coreHex, particleLayer, liveParticles, stopMotionTick, app) can be null/undefined stubs — buildParticleAnchors never reads them. Assert: result.core toEqual { x: 0, y: 0 }; result.cells.get('flowgrid:cell:test') toEqual { x: 83, y: 48 }; result.routes.get('flowgrid:route:0').from toEqual { x: 0, y: 0 } and .to toEqual { x: 83, y: 48 }. Run npx vitest run tests/ui/particle-anchors.test.ts — it MUST fail (module does not exist yet).

    Step 2 — Create the pure module (TDD GREEN). Create src/ui/flowgrid-home/particle-anchors.ts exporting `buildParticleAnchors(refs: SceneRefs): ParticleAnchors`. Use TYPE-ONLY imports (import type) for SceneRefs from ../../render/flowgrid/scene.js and ParticleAnchors from ../../render/flowgrid/particles.js — this guarantees zero runtime pixi.js dependency so the module loads under happy-dom without WebGL. The function body: create a cells Map; for each [cellId, view] of refs.cells, set cells.get(cellId) to { x: view.x, y: view.y } UNCHANGED (these are already container-local per scene.ts CellView). Create a routes Map; for each [routeId, route] of refs.routes, set routes.get(routeId) to { from: { x: route.fromX, y: route.fromY }, to: { x: route.toX, y: route.toY } } UNCHANGED. Return { core: { x: 0, y: 0 }, cells, routes }. Do NOT offset any anchor by the container's stage position — the ParticleContainer inherits the container's centering transform (container.addChild(particleLayer) in scene.ts), so adding the stage offset would double-apply it. Add a header comment documenting the coordinate-space contract and referencing .planning/debug/no-canvas-animation.md as the root-cause evidence. Run npx vitest run tests/ui/particle-anchors.test.ts — it MUST pass.

    Step 3 — Rewire FlowgridCanvas.tsx. Remove the inline buildParticleAnchors function definition entirely (the function and its preceding comment block, roughly lines 224-249). Add `import { buildParticleAnchors } from './particle-anchors.js';` to the existing import group near the top of the file. The call site at approximately line 156 (const anchors = buildParticleAnchors(sceneRefs)) stays UNCHANGED — it already passes sceneRefs and consumes the return value. In the import from ../../render/flowgrid/particles.js (line 45), the ParticleAnchors TYPE is no longer referenced directly in this file (the new module owns it) — drop the ParticleAnchors type from that import while keeping emitParticles. Run npx tsc --noEmit to confirm no type errors from the extraction.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/particle-anchors.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    buildParticleAnchors lives in its own pure module (src/ui/flowgrid-home/particle-anchors.ts), returns container-local coordinates (core {0,0}; cells at their hex positions; routes at their line endpoints), the regression test passes asserting these exact values, FlowgridCanvas.tsx imports the function from the new module with no inline function remaining, and tsc is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop SettingsPanel from durably pinning reduceMotion via OS-preference auto-persist</name>
  <files>src/ui/settings/SettingsPanel.tsx, tests/ui/settings-reduce-motion.test.tsx</files>
  <behavior>
    - When SettingsPanel mounts with snapshot.settings.reduceMotion === false AND prefersReducedMotion() === true: NO update_settings command is dispatched (reduceMotion is NOT durably pinned).
    - The local checkbox state pre-fills to true (a visual suggestion honoring the OS preference), but the durable store record stays reduceMotion === false until the user explicitly clicks Save.
  </behavior>
  <action>
    SECONDARY CONTRIBUTOR (per UAT diagnosis): the ref-guarded mount effect in SettingsPanel.tsx (approximately lines 72-96) dispatches an update_settings command to DURABLY persist reduceMotion=true when the OS prefers-reduced-motion and the persisted setting is false. This accidentally pins reduceMotion=true for any user who visits /settings once with OS reduce-motion enabled, which stops the animation ticker and skips particle emission indefinitely — compounding the Task 1 coordinate bug for affected users.

    GAP-CLOSURE REVISION OF D-09: D-09 (locked) states "Reduced-motion auto-detects the OS preference with a manual override." The durable auto-persist was the original implementation, but the UAT diagnosis shows it traps users who did not intend to durably commit. The revised approach honors the OS preference as a SESSION-ONLY visual suggestion (checkbox pre-fill), NOT a durable write. D-09's "manual override" is fully preserved — the user can explicitly toggle the checkbox and click Save to persist their choice either way. This is the "make session-only without writing durable state" fix direction from the approved gap summary.

    Step 1 — Write the test FIRST (TDD RED). Create tests/ui/settings-reduce-motion.test.tsx. Follow the React component test pattern from tests/ui/cell-list-a11y.test.tsx (happy-dom project, vitest + @testing-library/react). vi.mock the dependencies SettingsPanel pulls in at module level: (a) ../../src/app/store/dispatch.js — export a `dispatch` vi.fn() spy (capture all calls) and a `useFlowgridStore` stub that reads from a local snapshot variable (set it before render) returning a snapshot with settings.reduceMotion === false and the other three settings at their seeded defaults (defaultSessionLengthSeconds 1500, dailyTargetSeconds 1800, localDayBoundary '00:00'); (b) ../../src/app/repository.js — stub `repository` and `database` as empty objects; (c) ../../src/ui/settings/reduce-motion.js — stub `effectiveReduceMotion` as identity and `prefersReducedMotion` as () => true; (d) ../../src/persistence/index.js — stub exportJson/exportSessionCsv/importArchive as no-op async fns. Render <SettingsPanel /> inside a createMemoryRouter + RouterProvider (mirror cell-list-a11y.test.tsx's renderHome helper) so the <Link to="/" /> has router context. Wait for mount effects (use waitFor or await act). Assert the dispatch spy was NOT called with any command whose type is 'update_settings'. Run npx vitest run tests/ui/settings-reduce-motion.test.tsx — it MUST FAIL (the current mount effect dispatches on mount when OS pref is true).

    Step 2 — Fix the mount effect (TDD GREEN). In src/ui/settings/SettingsPanel.tsx, locate the ref-guarded mount effect (the useEffect with honoredOsPreference ref, approximately lines 72-96). In the branch where prefersReducedMotion() returns true: REMOVE the buildSettingsEnv call, the UpdateSettingsCommand object construction, AND the `void dispatch(command, env, repository)` call. KEEP the `honoredOsPreference.current = true` assignment and the `setReduceMotion(true)` local-state update — the checkbox still pre-fills to reflect the OS preference as a non-committal session-only suggestion. Update the comment block above the effect (currently documenting D-09 first-run OS-preference honoring) to read: the OS preference now pre-fills the checkbox as a session-only suggestion; durable reduceMotion requires an explicit Save click; this revises the D-09 implementation per gap-closure 06-05 so reduceMotion cannot be accidentally pinned true. Keep buildSettingsEnv, SETTINGS_SEED, makeEnv, dispatch, UpdateSettingsCommand, repository imports — they are still used by handleSave. Do NOT modify effectiveReduceMotion or prefersReducedMotion in reduce-motion.ts. Run npx vitest run tests/ui/settings-reduce-motion.test.tsx — it MUST PASS. Then run the full suite to confirm no regressions.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/settings-reduce-motion.test.tsx && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>
    SettingsPanel's mount effect no longer dispatches update_settings to persist reduceMotion; the checkbox pre-fills from the OS preference (session-only) but the durable store record remains false until explicit Save. The regression test passes, the full vitest suite shows no regressions, and tsc is green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual smoke — confirm all particle animations are visible inside the canvas</name>
  <action>
    WHAT WAS BUILT: Task 1 fixed the particle anchor coordinate space so particles emit in container-local coordinates (matching the hexes) instead of stage coordinates. Task 2 stopped the accidental durable pinning of reduceMotion from the OS preference. Together these restore the visible animation pipeline: the debug session (.planning/debug/no-canvas-animation.md) confirmed the emission logic, ticker, and per-frame motion update were all healthy — only the coordinate space and the reduce-motion gate were broken.

    Full-pixel animation verification is human-only (Pixi v8 preserveDrawingBuffer:false defeats automated readback per VER-06). Run this smoke in a real browser with WebGL:

    1. Start the dev server: `npm run dev`
    2. Open the app in a real browser (Chrome/Firefox/Safari with WebGL enabled). NOTE: if your OS has prefers-reduced-motion enabled, either disable it OS-wide for this test, or open /settings and confirm the Reduce motion checkbox is OFF (it may be pre-filled ON — if so, uncheck and Save).
    3. If no Cells exist, create one via the New Cell flow
    4. Start a focus session on a Cell (tap the Cell hex or use the Cell list link then Start)
    5. WHILE THE SESSION IS ACTIVE: observe the canvas — particle Current trails should visibly flow from the Cell hex toward the Core hex along the route line
    6. Complete the session (Finish): a Bloom burst should radiate outward from the Cell hex; an Activation pulse and Core convert ripple should animate at the Core
    7. Navigate to /core, adjust the convert/store allocation sliders, and Apply: a Core convert/store ripple should animate at the Core hex
    8. Log a rejuvenation: if Module Tokens are granted, a token-granted flash (purple) should animate
    9. Navigate to /forge and run a forge roll: a forge-roll flash (gold) should animate; if a module upgrades, a module-upgrade flash (green) should animate
    10. CONFIRM: all particles appear INSIDE the visible canvas region (centered on/near the Core and Cell hexes), NOT clustered in the bottom-right corner or off-screen

    Expected outcome: every animation type is visible during its respective event. If any particle cluster appears in the bottom-right corner or off-canvas, the coordinate-space fix is incomplete — describe what you see.
  </action>
  <verify>Human confirms all particle animation types (Current trails, Bloom bursts, Core ripples, Activation pulses, forge/token flashes) are visible inside the canvas viewport during their respective events</verify>
  <done>Human types "approved" confirming all animation types are visible inside the canvas, OR describes which events/particles are missing or mispositioned for follow-up</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS preference media query → app UI state | prefers-reduced-motion influences the SettingsPanel checkbox pre-fill (existing boundary; behavior revised from durable auto-persist to session-only suggestion) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | particle-anchors.ts | accept | Pure coordinate derivation from internal SceneRefs state; no untrusted input crosses this boundary — the SceneRefs are built from the app's own simulation snapshot, not from user-supplied data |
| T-06-05-02 | Elevation of privilege | SettingsPanel mount effect | mitigate | Removed durable auto-persist so user state cannot be modified without explicit consent (Save click); the OS preference now only pre-fills a visual suggestion that the user can accept or reject before any durable write |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (no type errors from the extraction or the mount-effect revision)
- `npx vitest run` full suite passes — all existing tests remain green plus the two new regression tests pass
- Human visual smoke (Task 3 checkpoint) confirms all particle animation types are visible inside the canvas viewport during real gameplay events
- No new packages installed; no ARCHIVE_VERSION bump; no Dexie migration needed (SettingsRecord shape is unchanged)
</verification>

<success_criteria>
- Particle anchor coordinates are container-local and unit-tested (regression guard in place)
- SettingsPanel does not durably persist reduceMotion from the OS preference and the behavior is unit-tested
- All particle animation types (Current trails, Bloom bursts, Core ripples, Activation pulses, forge/token flashes) are human-confirmed visible inside the canvas during their respective events
- UAT Test 1 can be re-run and passes (result: pass)
</success_criteria>

<output>
Create `.planning/phases/06-hardening-accessibility-and-trust/06-05-SUMMARY.md` when done
</output>
