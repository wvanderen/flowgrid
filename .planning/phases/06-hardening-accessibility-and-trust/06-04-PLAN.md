---
phase: 06-hardening-accessibility-and-trust
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - playwright.config.ts
  - package.json
  - vitest.config.ts
  - tests/e2e/release-flow.spec.ts
  - tests/e2e/accessibility.spec.ts
  - tests/e2e/canvas-smoke.spec.ts
autonomous: true
requirements: [VER-04, VER-05, VER-06]
user_setup: []

must_haves:
  truths:
    - "A real browser can complete the full Flowgrid flow: create Cell → start/finish focus → see Current/Bloom/Core effects → log rejuvenation → earn Module Token → run Forge → reload with state preserved"
    - "Every critical canvas-backed action is reachable via keyboard only (Tab/Enter) and passes an automated axe WCAG scan"
    - "The Pixi canvas renders nonblank Cells/Core/routes (scene-graph probe + pixel-variance), and reducing/disabling motion leaves durable economy state intact"
    - "E2E runs against the production build (vite build && vite preview), not dev mode"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright config: production-build webServer, SwiftShader WebGL launch args, tests/e2e dir"
      contains: "webServer"
    - path: "tests/e2e/release-flow.spec.ts"
      provides: "VER-04 full-flow E2E with reload-with-state"
      contains: "page.reload"
    - path: "tests/e2e/accessibility.spec.ts"
      provides: "VER-05 keyboard-only flow + axe scan per route"
      contains: "AxeBuilder"
    - path: "tests/e2e/canvas-smoke.spec.ts"
      provides: "VER-06 scene-graph probe + pixel variance + reduced-motion durability"
      contains: "summarizeScene"
  key_links:
    - from: "playwright.config.ts"
      to: "package.json"
      via: "webServer command npm run build && npm run preview; test:e2e script"
      pattern: "npm run preview"
    - from: "tests/e2e/canvas-smoke.spec.ts"
      to: "src/render/flowgrid/scene-inspect.ts"
      via: "page.evaluate window.__flowgridInspect summarizeScene call"
      pattern: "__flowgridInspect"
---

## Phase Goal

**As a** Flowgrid developer releasing v1, **I want to** verify the complete browser flow, accessibility, and canvas rendering against the production build, **so that** I can trust the app is release-ready without manual smoke testing.

## Objective

Deliver the release-readiness E2E gate vertical slice (VER-04 + VER-05 + VER-06): install Playwright + axe-core, configure a production-build webServer with SwiftShader WebGL flags (D-14/D-17), and write three real-browser spec files covering the full critical flow with reload-with-IndexedDB-state (VER-04), keyboard-only navigation + per-route axe WCAG scans (VER-05), and canvas scene-graph probe + pixel-variance + reduced-motion durability (VER-06). This plan absorbs the deferred Phase 5 Task-4 human visual smoke into automation. After this plan, Phase 6 and v1 are verifiable.

Purpose: VER-04/05/06 are the only honest way to cover full click-flow, IndexedDB reload, keyboard, and a real WebGL canvas. This plan depends on 06-02 (renderer + scene-inspect probe exist) and 06-03 (Cell list exists for the keyboard flow). It is the final slice — it tests the complete shipped product.

Output: playwright.config.ts, three E2E specs, package.json scripts, vitest e2e exclusion.

## Execution Context

@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-accessibility-and-trust/06-CONTEXT.md
@.planning/phases/06-hardening-accessibility-and-trust/06-RESEARCH.md
@.planning/phases/06-hardening-accessibility-and-trust/06-PATTERNS.md

@src/render/flowgrid/scene-inspect.ts
@src/ui/flowgrid-home/FlowgridHome.tsx
@src/ui/settings/SettingsPanel.tsx
@vitest.config.ts
@package.json

## Artifacts this phase produces

- **Config:** `playwright.config.ts` (webServer → vite preview, SwiftShader launchOptions args, tests/e2e testDir)
- **Scripts:** `test:e2e` and `test:e2e:ui` in package.json
- **E2E specs:** `tests/e2e/release-flow.spec.ts` (VER-04), `tests/e2e/accessibility.spec.ts` (VER-05), `tests/e2e/canvas-smoke.spec.ts` (VER-06)
- **Window probe wiring:** the app exposes `window.__flowgridInspect` (aggregate scene counts) for VER-06 — verify it is wired from 06-02's scene-inspect.ts (if not wired in 06-02, wire it here in FlowgridCanvas.tsx)

## Tasks

<task type="auto">
  <name>Task 1: Install Playwright + Chromium + write config + scripts + vitest e2e exclusion</name>
  <files>playwright.config.ts, package.json, vitest.config.ts, package-lock.json</files>
  <read_first>
    - package.json (existing scripts — add test:e2e peers; verify vite build/preview scripts exist)
    - vitest.config.ts (existing dual-environment config — exclude tests/e2e so vitest does not pick up Playwright specs)
    - 06-RESEARCH.md Code Example "Playwright config" lines 489-516 (the verified config shape)
    - 06-RESEARCH.md Pitfall 2 "Headless Chromium WebGL needs SwiftShader flags" lines 352-365 (the CRITICAL launchOptions args)
    - 06-RESEARCH.md Package Legitimacy Audit lines 149-165 (both packages approved as canonical false-positives; no human checkpoint required)
    - 06-RESEARCH.md Assumption A2 (vite preview default port 4173)
  </read_first>
  <action>
    Install the two dev dependencies (both approved per RESEARCH Package Legitimacy Audit — canonical Microsoft/Deque repos, 41M/5M weekly downloads; the "too-new" heuristic is a documented false positive): run `npm install -D @playwright/test @axe-core/playwright`. Then install the Chromium browser binary: `npx playwright install chromium` (one-time, ~150MB; on Linux CI add --with-deps). Do NOT install @pixi/tween (does not exist on npm — SLOP) or axe-playwright (community fork — use the official @axe-core/playwright).

    Create playwright.config.ts per the RESEARCH verified example: import defineConfig + devices from @playwright/test. Set testDir to ./tests/e2e, fullyParallel true, forbidOnly !!process.env.CI, retries process.env.CI ? 2 : 0, reporter process.env.CI ? 'github' : 'list'. In use: baseURL 'http://localhost:4173', trace 'on-first-retry', and launchOptions.args containing EXACTLY ['--use-gl=angle', '--use-angle=swiftshader', '--enable-unsafe-swiftshader'] (Pitfall 2 — without these, VER-06 WebGL fails in headless Chromium). Add a single chromium project using devices['Desktop Chrome']. Configure webServer: command 'npm run build && npm run preview -- --strictPort' (D-17 production build), url 'http://localhost:4173', reuseExistingServer !process.env.CI, timeout 120000.

    Add to package.json scripts: "test:e2e": "playwright test" and "test:e2e:ui": "playwright test --ui" (peers to the existing test script).

    Update vitest.config.ts to exclude tests/e2e from the vitest test discovery (add tests/e2e to the existing exclude array, or scope include to not match tests/e2e/**) so vitest run does not attempt to parse Playwright specs. Keep the existing node + happy-dom dual-environment config intact.
  </action>
  <verify>
    <automated>npx vitest run exits 0 && test -f playwright.config.ts && node -e "require('./playwright.config.ts')" 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains @playwright/test and @axe-core/playwright (NOT axe-playwright, NOT @pixi/tween)
    - playwright.config.ts exists with webServer.command containing npm run build and npm run preview, webServer.url http://localhost:4173, and launchOptions.args containing all three SwiftShader flags (--use-gl=angle, --use-angle=swiftshader, --enable-unsafe-swiftshader)
    - package.json contains test:e2e and test:e2e:ui scripts
    - vitest.config.ts excludes tests/e2e from discovery
    - npx vitest run exits 0 (vitest no longer picks up e2e specs; existing suite still green)
  </acceptance_criteria>
  <done>Playwright + axe-core + Chromium installed; config targets the production build with SwiftShader WebGL flags; package.json has E2E scripts; vitest ignores the e2e directory.</done>
</task>

<task type="auto">
  <name>Task 2: VER-04 release-flow E2E + VER-05 keyboard/axe E2E</name>
  <files>tests/e2e/release-flow.spec.ts, tests/e2e/accessibility.spec.ts</files>
  <read_first>
    - 06-RESEARCH.md Code Example "axe-core scan per route" lines 518-532 (the AxeBuilder usage)
    - 06-CONTEXT.md D-15 (keyboard flow + axe scan per route)
    - 06-CONTEXT.md D-17 (production build)
    - src/ui/flowgrid-home/FlowgridHome.tsx (the New Cell button + Cell list + canvas — the E2E entry points)
    - src/ui/cell-board/CellBoard.tsx (Start/Finish session controls — the flow targets)
    - src/ui/core-panel/CorePanel.tsx (allocation + rejuvenation controls)
    - src/ui/forge-panel/ForgePanel.tsx (forge controls)
    - src/ui/settings/SettingsPanel.tsx (export control — the flow end)
  </read_first>
  <action>
    Create tests/e2e/release-flow.spec.ts (VER-04): import test + expect from @playwright/test. Write a single test driving the full critical flow against the production preview: (1) goto '/'; (2) click "New Cell", fill the create form, submit; (3) navigate to the Cell (via the canvas tap OR the semantic Cell list link — use the Cell list link for determinism), Start a focus session, wait a moment, Finish it; (4) assert completion feedback appears (Current/XP/Core outcome text); (5) navigate to /core, set allocation, Apply; (6) log a rejuvenation, assert token progress if a threshold crossed (or assert the rejuvenation summary); (7) if a Module Token is available, navigate to /forge, run a forge roll, pick a choice; (8) page.reload() and assert the Cells, Core state, and forge history persist (IndexedDB durability — VER-04's reload-with-state); (9) navigate to /settings and assert the Export JSON button is present. Use page.getByRole / getByLabel for resilient selectors. Add graceful waits for async store loads.

    Create tests/e2e/accessibility.spec.ts (VER-05): import test + expect from @playwright/test and AxeBuilder from @axe-core/playwright (the OFFICIAL package — not axe-playwright). Write two kinds of tests: (A) a keyboard-only flow test driving the critical actions via page.keyboard.press('Tab') and page.keyboard.press('Enter') through the semantic Cell list → Start/Finish → allocation → rejuvenation → forge → export, asserting each step succeeds without pointer clicks; (B) per-route axe scans: for each of '/', '/cells/:id' (after creating a Cell), '/core', '/forge', '/settings', run `new AxeBuilder({ page }).withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa']).analyze()` and assert results.violations equals []. This catches both interaction gaps (keyboard) and markup/contrast/ARIA violations (axe).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/release-flow.spec.ts tests/e2e/accessibility.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/release-flow.spec.ts exits 0 and includes a page.reload() with post-reload state assertions
    - tests/e2e/accessibility.spec.ts imports AxeBuilder from @axe-core/playwright (NOT axe-playwright), drives the flow via page.keyboard, and runs an axe scan on at least 5 routes asserting violations equals []
    - npx playwright test tests/e2e/release-flow.spec.ts tests/e2e/accessibility.spec.ts exits 0
  </acceptance_criteria>
  <done>VER-04 (full flow + reload-with-state) and VER-05 (keyboard + axe per route) pass against the production build.</done>
</task>

<task type="auto">
  <name>Task 3: VER-06 canvas smoke — scene-graph probe + pixel variance + reduced-motion durability</name>
  <files>tests/e2e/canvas-smoke.spec.ts</files>
  <read_first>
    - 06-RESEARCH.md Code Example "Scene-graph probe" lines 545-560 (the summarizeScene + window probe pattern)
    - 06-RESEARCH.md Pitfall 2 + Assumption A1 (SwiftShader flags required for headless WebGL)
    - 06-CONTEXT.md D-16 (scene-graph probe primary + pixel sanity secondary; reduced-motion leaves economy intact)
    - 06-CONTEXT.md D-03/D-08 (static fallback when reduced motion on; ticker fully off)
    - src/render/flowgrid/scene-inspect.ts (06-02 output — summarizeScene export; verify window.__flowgridInspect is wired in FlowgridCanvas.tsx — if 06-02 did not wire it, wire it here: attach summarizeScene(app) to window.__flowgridInspect in FlowgridCanvas's mount effect, returning aggregate counts only)
  </read_first>
  <action>
    Create tests/e2e/canvas-smoke.spec.ts (VER-06): import test + expect from @playwright/test. Write three assertions:

    (1) Scene-graph structural probe: goto '/', wait for the canvas to mount and the store to load, create a Cell if none exist, then `const summary = await page.evaluate(() => (window as any).__flowgridInspect?.())` and assert summary.cells > 0 and summary.core === true and summary.routes >= 0. If window.__flowgridInspect is undefined (not wired), wire it in FlowgridCanvas.tsx mount effect: attach the summarizeScene(app) call to window.__flowgridInspect returning only aggregate { cells, core, routes } counts (RESEARCH Open Question Q1 option a — safe, no internal refs).

    (2) Pixel-variance sanity: grab the canvas element via page.locator or the data-testid flowgrid-canvas container, read its WebGL pixels via page.evaluate reading canvas.getContext('webgl').readPixels or drawImage to a 2D context and sample a coarse grid, and assert the sampled pixels are NOT all a single uniform color (non-zero variance — the canvas rendered something). This requires WebGL (SwiftShader flags from the config). Mark this assertion with a clear comment that it is the secondary check; if headless WebGL is flaky in CI, it may be guarded with test.skip in CI while the structural probe still runs.

    (3) Reduced-motion durability (D-03/D-08): navigate to /settings, toggle reduceMotion ON (or set it via the form + save), return to '/', dispatch a focus session via the semantic controls, then assert the durable records are unchanged by reading the post-session state and comparing to a baseline captured before — specifically that completing a session with motion OFF still produces the same nextState as motion ON (the renderer cannot affect economy truth). Alternatively/additionally: assert the animation ticker is stopped (the canvas shows static hexes) while economy actions still work. The core assertion is that reducing motion does not break or alter economy state.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/canvas-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/canvas-smoke.spec.ts exits 0
    - the spec asserts summary.cells > 0 and summary.core is true via the window probe
    - the spec includes a pixel-variance assertion (guarded for CI flakiness if needed)
    - the spec asserts reduced-motion leaves durable economy state intact
    - window.__flowgridInspect is wired (either from 06-02 or this task) and returns aggregate counts only
    - npx playwright test (all e2e) exits 0
  </acceptance_criteria>
  <done>VER-06 passes: canvas renders nonblank Cells/Core/routes (structural + pixel), and reduced/disabled motion leaves economy state intact.</done>
</task>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production build → real browser | The shipped bundle runs in Chromium (E2E exercises real bundling, IndexedDB, WebGL) |
| untrusted archive import (VER-04 path) | The flow may exercise export/import round-trip |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | Production bundle dev-code leak | mitigate | D-17 E2E runs against vite build && vite preview (real bundling, no HMR/dev-only code); catches build-time regressions dev mode hides |
| T-06-11 | Denial of service | Headless WebGL unavailability in CI | mitigate | SwiftShader launchOptions flags (Pitfall 2); VER-06 pixel-variance may be test.skip in CI while structural probe still runs; D-07 graceful path keeps VER-04/05 runnable without WebGL |
| T-06-12 | Tampering | Accessibility regression | mitigate | VER-05 axe scan per route catches WCAG violations automatically; keyboard-only flow catches interaction gaps |
| T-06-SC | Tampering | npm installs (@playwright/test, @axe-core/playwright) | mitigate | RESEARCH Package Legitimacy Audit approved both (canonical Microsoft/Deque repos, 41M/5M weekly downloads); Task 0 human-verify gate confirms install; @pixi/tween (SLOP) and axe-playwright (community) explicitly NOT installed |

## Verification

- `npx playwright test` (all E2E specs) passes against the production build
- `npx vitest run` (unit/property suite) still passes (e2e excluded)
- VER-04 full flow + reload-with-state ✓
- VER-05 keyboard + axe per route ✓
- VER-06 scene-graph + pixel + reduced-motion durability ✓

## Success Criteria

- [ ] Real browser completes create Cell → focus → Current/Bloom/Core → rejuvenation → token → forge → reload-with-state (VER-04)
- [ ] Keyboard-only flow + axe WCAG scan pass on all routes (VER-05)
- [ ] Canvas renders nonblank Cells/Core/routes; reduced motion leaves economy intact (VER-06)
- [ ] E2E runs against vite build && vite preview (D-17)
- [ ] Deferred Phase 5 human visual smoke is retired by automation

## Output

Create `.planning/phases/06-hardening-accessibility-and-trust/06-04-SUMMARY.md` when done
