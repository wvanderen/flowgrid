---
phase: 06.1-canvas-always-visible-layout-pivot
plan: 03
type: execute
wave: 3
depends_on: [06.1-01, 06.1-02]
files_modified:
  - tests/e2e/canvas-smoke.spec.ts
  - tests/ui/flowgrid-home.test.tsx
  - tests/ui/cell-list-a11y.test.tsx
  - tests/ui/cell-board.test.tsx
autonomous: false
requirements: [UI-08, UI-03, VER-06]
user_setup: []

must_haves:
  truths:
    - "E2E (production build) confirms the <canvas> stays mounted and window.__flowgridInspect returns non-zero on /, /cells/:id, AND /core — not only on /"
    - "E2E confirms the <canvas> DOM identity is unchanged across /cells/A -> /cells/B (param-only navigation does not unmount the canvas)"
    - "E2E confirms the takeover overlay (/settings) covers the canvas but the canvas stays mounted (probe still returns counts)"
    - "Human smoke confirms particles are perceivable during session finish and Core convert/store with the canvas visible (the re-opened 06-05 Task 3 — UI-03/VER-06 re-closure)"
    - "WebGL-fail keeps the canvas zone identical in size/position (no reflow) — D-09 continuity"
  artifacts:
    - path: "tests/e2e/canvas-smoke.spec.ts"
      provides: "Extended VER-06 canvas smoke — persistence across core routes, param-change identity, takeover-coverage, particles-during-events"
      contains: "__flowgridInspect"
    - path: "tests/ui/flowgrid-home.test.tsx"
      provides: "Layout-route canvas-persistence + WebGL-fail continuity assertions"
      contains: "AppLayout"
  key_links:
    - from: "tests/e2e/canvas-smoke.spec.ts"
      to: "src/ui/flowgrid-home/FlowgridCanvas.tsx"
      via: "spec consumes window.__flowgridInspect probe + asserts <canvas> presence on /cells/:id and /core (canvas now persists there post-Plan-01)"
      pattern: "canvas"
    - from: "tests/ui/flowgrid-home.test.tsx"
      to: "src/ui/shell/AppLayout.tsx"
      via: "test mounts the pathless layout route via createMemoryRouter and asserts the canvas mock persists across Outlet child swaps"
      pattern: "createMemoryRouter"
---

<objective>
Plan 03 is the **acceptance gate** that re-opens and closes Phase 6 Plan 06-05 Task 3 (the human visual smoke that FAILED because the canvas unmounted on every particle-emitting route). With the persistent spine (Plan 01) and the inline dock (Plan 02) in place, the canvas now stays mounted across /, /cells/:id, /core — so particles emit into a visible canvas during active sessions, session finish (Bloom/Activation/Core ripple), Core convert/store, and rejuvenation. This plan extends the E2E canvas-smoke harness to assert the persistence invariant structurally, extends the UI tests to cover the layout route + WebGL-fail continuity (D-09), and runs the re-opened human smoke against the production build (D-17) as the final blocking checkpoint.

This plan re-closes **UI-03** (visuals render from simulation-emitted events DURING their events) and **VER-06** (canvas/WebGL smoke checks pass when particles are visible), and validates **UI-08** end-to-end. It is the verification slice of the phase.

Purpose: Prove structurally and visually that effort is now perceivable as it happens — the structural blocker that kept 06-05 Task 3 open is dissolved.
Output: Extended canvas-smoke.spec.ts, extended UI tests, a passed human-verify checkpoint (the re-opened 06-05 Task 3).
</objective>

## Phase Goal

**As a** Flowgrid player, **I want to** watch my Current trails, Bloom bursts, and Core ripples fire in the canvas while I play, **so that** the signal my effort produces is visible in the moment — and so the trust checks (UI-03, VER-06) finally close.

This plan delivers the verification slice that closes the phase story.

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-CONTEXT.md
@.planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-RESEARCH.md
@.planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-PATTERNS.md
@.planning/phases/06-hardening-accessibility-and-trust/06-05-SUMMARY.md
@.planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-01-SUMMARY.md
@.planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-02-SUMMARY.md

# Plan 01 delivered the persistent spine; Plan 02 delivered the Z-Lift + dock. This plan verifies them.
@tests/e2e/canvas-smoke.spec.ts
@tests/e2e/accessibility.spec.ts
@tests/ui/flowgrid-home.test.tsx
@tests/ui/cell-list-a11y.test.tsx
@tests/ui/cell-board.test.tsx
@src/ui/flowgrid-home/FlowgridCanvas.tsx
@src/ui/shell/AppLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend canvas-smoke.spec.ts — persistence, param-change identity, takeover-coverage, particles-during-events (VER-06, UI-03 re-closure)</name>
  <files>
    tests/e2e/canvas-smoke.spec.ts
  </files>
  <read_first>
    tests/e2e/canvas-smoke.spec.ts (the current structure: createCell helper lines 17-25, waitForScene poller lines 29-40, the three existing VER-06 tests — scene-graph probe, pixel variance, reduced-motion durability; the createCell + runSessionAndReadSummary helpers),
    src/ui/flowgrid-home/FlowgridCanvas.tsx (the window.__flowgridInspect probe wiring; the WebGL-fail branch returning the same className),
    .planning/phases/06-hardening-accessibility-and-trust/06-05-SUMMARY.md (WHY Task 3 failed — canvas unmounted on /cells/:id and /core; this plan asserts the inverse),
    .planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-RESEARCH.md (Pitfall 1 verification step — assert canvas identity unchanged across /cells/A -> /cells/B; Pitfall 5 takeover covers but probe stays live; Environment Availability — production build via vite build && vite preview, SwiftShader WebGL),
    .planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-CONTEXT.md (the 6 success criteria — criteria 1, 2, 6 map to this task)
  </read_first>
  <action>
    Extend tests/e2e/canvas-smoke.spec.ts with new VER-06 / UI-08 / UI-03 tests (keep the existing three tests intact — they still pass post-pivot). All new tests run against the production build (the spec already runs via the playwright config against vite preview per Phase 6 D-17).

    Test A — "canvas persists across core routes": after createCell, navigate / -> /cells/:id -> /core -> / (use page.goto or the in-app links); at EACH route assert page.locator('canvas') is visible AND waitForScene succeeds (window.__flowgridInspect returns cells > 0). This is the structural proof of the Plan-01 spine — the canvas no longer unmounts on /cells/:id or /core (the exact gap that blocked 06-05 Task 3). Cite success criterion 1 + UI-08.

    Test B — "canvas identity unchanged across param-only navigation": create TWO Cells (names A and B); capture the <canvas> element handle on /cells/A; navigate to /cells/B (param-only change); assert the SAME canvas element handle is still attached (e.g. evaluate element.isConnected on the saved reference, or assert the canvas count stays at 1 and the probe cell count is stable). This proves the pathless layout route keeps the AppLayout parent (and thus FlowgridCanvas) mounted across child param changes — RESEARCH Pitfall 1 verification step. Cite Pitfall 1.

    Test C — "takeover covers canvas but probe stays live": navigate to /settings; assert page.locator('canvas') is still in the DOM (count >= 1 — mounted, not unmounted) AND window.__flowgridInspect still returns a result with cells > 0 (the canvas is mounted but covered by the overlay — D-02). Assert the Settings overlay is visible. Navigate back to / and assert the canvas is visible again. Cite D-02.

    Test D — "particles visible during session finish" (the re-opened 06-05 Task 3 structural component): createCell, navigate to /cells/:id (canvas now visible alongside), click Start Focus Session, wait ~1.5s, assert window.__flowgridInspect is non-zero THROUGHOUT (capture before start, during session, after Finish), click Finish, assert the probe is STILL non-zero and the Session summary renders. This is the structural proof that particles emit into a visible canvas during the session lifecycle (the human-perception confirmation is the Task 3 checkpoint below). Cite success criterion 6 + UI-03.

    Reuse the existing createCell + waitForScene helpers. Do NOT rewrite the existing three tests. The pixel-variance test's existing CI skip (canvas-smoke.spec.ts line 76) stays — the structural probe is the load-bearing assertion.
  </action>
  <verify>
    <automated>npm run build && npx playwright test canvas-smoke</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && npx playwright test canvas-smoke` exits 0 (all existing + new tests pass against the production preview build).
    - The spec contains a test asserting <canvas> is visible AND __flowgridInspect is non-zero on /cells/:id and /core (rg 'cells/:|/core' tests/e2e/canvas-smoke.spec.ts within a single test body — Test A).
    - The spec contains a test asserting canvas identity is unchanged across /cells/A -> /cells/B (rg 'isConnected|cells/' Test B pattern — Pitfall 1 verification).
    - The spec contains a test asserting /settings keeps the canvas mounted (DOM count >= 1) + probe live (Test C — D-02).
    - The spec contains a test asserting the probe is non-zero throughout start -> wait -> finish on /cells/:id (Test D — UI-03/success criterion 6).
    - The existing three VER-06 tests (scene-graph probe, pixel variance, reduced-motion durability) are unchanged and still pass.
  </acceptance_criteria>
  <done>canvas-smoke.spec.ts extended with four new tests — persists-across-core-routes, param-change-identity, takeover-coverage, particles-during-session-finish — all green against the production preview build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend UI tests — layout-route canvas persistence + WebGL-fail continuity (D-09)</name>
  <files>
    tests/ui/flowgrid-home.test.tsx,
    tests/ui/cell-list-a11y.test.tsx,
    tests/ui/cell-board.test.tsx
  </files>
  <read_first>
    tests/ui/flowgrid-home.test.tsx (Plan 01 already updated this to mount the layout route — confirm the createMemoryRouter harness wraps AppLayout + children, the FlowgridCanvas vi.mock stub, the beforeEach store reset including selectedCellId/takeoverActive),
    tests/ui/cell-list-a11y.test.tsx (the Cell-list nav a11y assertions — extend to assert the nav is present in the persistent chrome across core routes per D-03),
    tests/ui/cell-board.test.tsx (the dispatch vi.mock + importActual pattern around lines 30-34; the createMemoryRouter with /cells/:cellId initialEntries around lines 99-105),
    src/ui/flowgrid-home/FlowgridCanvas.tsx (the WebGL-fail branch — it returns the same className h-[60vh] ... sm:h-[70vh] for both success and fail; D-09 says the zone stays identical),
    .planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-RESEARCH.md (Pitfall 7 WebGL-fail reflow — the canvas zone must NOT collapse/resize on failure),
    .planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-CONTEXT.md (D-03 chrome persists across core views; D-09 canvas zone identical on WebGL-fail)
  </read_first>
  <behavior>
    - Test 1 (flowgrid-home): the FlowgridCanvas mock (data-testid flowgrid-canvas-mock) STAYS mounted (same element) when navigating / -> /cells/:id -> /core via the layout route (Plan 01 added a version of this — confirm + extend to assert the mock identity is unchanged).
    - Test 2 (cell-list-a11y): the Cell-list <nav aria-label="Cells"> is present in the persistent chrome on / AND /cells/:id AND /core (D-03 chrome persistence).
    - Test 3 (cell-board): CellBoard renders into the layout dock zone alongside the canvas mock; the "Return to Flowgrid" link navigates to / WITHOUT unmounting the canvas mock.
    - Test 4 (flowgrid-home, WebGL-fail continuity): mock FlowgridCanvas to render its D-07 fallback branch (role="status" note); assert the canvas zone retains the same className (h-[60vh] sm:h-[70vh]) — no reflow — and the dock/chrome are still operable. Cite D-09 / Pitfall 7.
  </behavior>
  <action>
    In tests/ui/flowgrid-home.test.tsx, confirm/extend the layout-route canvas-persistence test from Plan 01 (the canvas mock persists across / -> /cells/:id -> /core -> /) and add the WebGL-fail continuity test: parameterize the FlowgridCanvas vi.mock to render the D-07 fallback (a div with role="status" and the SAME className the success branch uses), then assert the canvas zone's className matches the success-branch className (no layout shift) and the chrome (Cell-list nav, ReturnCues, New Cell button) is still present and operable. Cite D-09.

    In tests/ui/cell-list-a11y.test.tsx, extend the harness to mount the layout route (createMemoryRouter with the pathless AppLayout parent + children) and assert the Cell-list <nav aria-label="Cells"> is present when the active route is /, /cells/:id, AND /core (D-03 — the always-visible semantic Cell list persists across core views as the a11y peer to the canvas). Mock FlowgridCanvas to a placeholder (happy-dom has no WebGL).

    In tests/ui/cell-board.test.tsx, extend the createMemoryRouter harness to mount CellBoard as a child of the AppLayout layout route (so the canvas mock is present alongside CellBoard); assert the canvas mock is mounted AND CellBoard content renders; assert clicking the "Return to Flowgrid" link navigates to / and the canvas mock identity is unchanged (no unmount). Keep the existing dispatch vi.mock + importActual pattern.

    All three files: extend the beforeEach store reset to include selectedCellId: null and takeoverActive: false (Plan 01 started this — confirm completeness).
  </action>
  <verify>
    <automated>npx vitest run tests/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/ui/` exits 0 (all existing + new UI tests pass).
    - tests/ui/flowgrid-home.test.tsx contains a test asserting the canvas mock persists across / -> /cells/:id -> /core AND a WebGL-fail continuity test asserting the canvas-zone className is identical between success and fail branches (rg 'h-\[60vh\]' or the shared className appears in both the test and the FlowgridCanvas source — D-09 honored).
    - tests/ui/cell-list-a11y.test.tsx asserts the Cells nav is present on /, /cells/:id, AND /core (rg 'aria-label="Cells"' within the layout-route test).
    - tests/ui/cell-board.test.tsx asserts CellBoard renders alongside the canvas mock and the Return-to-Flowgrid link keeps the mock mounted.
    - All three files' beforeEach store resets include selectedCellId + takeoverActive (rg 'selectedCellId|takeoverActive' returns >= 2 in each reset block).
  </acceptance_criteria>
  <done>flowgrid-home/cell-list-a11y/cell-board UI tests extended for the layout route (canvas-mock persists across core routes, Cells-nav present across views, CellBoard renders alongside canvas) + WebGL-fail continuity (D-09 no reflow) — vitest green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke — re-opened 06-05 Task 3 (particles visible during events; UI-03/VER-06 re-closure)</name>
  <files>none — verification-only checkpoint</files>
  <action>
    Human visual smoke of the re-opened Phase 6 Plan 06-05 Task 3. Plan 01 lifted the FlowgridCanvas into a persistent layout route so it stays mounted across /, /cells/:id, /core, with /settings + /forge as overlays above it (ticker pauses behind overlays). Plan 02 added the Z-Lift visual + a semantic HTML dock beside the canvas. The autonomous agent has already verified: `npm run build` is green, the canvas-smoke E2E (Task 1) asserts the canvas persists on all core routes + the probe is non-zero throughout the session lifecycle, and the UI tests (Task 2) cover the layout route + WebGL-fail continuity. What it cannot verify is that particles are PERCEIVABLE during their emitting events in a real browser — the exact gap that kept 06-05 Task 3 open.

    Steps for the human (run against the production build per Phase 6 D-17):
    1. `npm run build && npm run preview` and open the preview URL in a browser.
    2. Create a Cell (New Cell button) if none exists.
    3. From Home (/), confirm the canvas with the hex lattice is visible.
    4. Tap the Cell hex (or the Cell-list link) — confirm it navigates to /cells/:id AND THE CANVAS STAYS VISIBLE (the spine works; the Z-Lift lifts the selected hex; the HTML dock appears beside it with a Start button).
    5. Click Start Focus Session (on the dock OR the canvas — both should work; two paths one truth). Confirm Current-trail particles emit INTO THE VISIBLE CANVAS during the session (the core UI-03 evidence — this is what 06-05 Task 3 could not show).
    6. Wait ~2-3 seconds, then Finish. Confirm the Bloom burst + Activation pulse + Core ripple particles fire IN THE VISIBLE CANVAS at finish.
    7. Navigate to /core (Core link). Confirm the canvas is STILL visible. Adjust the convert/store allocation sliders (inline dock). Confirm the Core convert/store ripple particle fires in the visible canvas.
    8. (Optional) Trigger a rejuvenation flow and confirm the token-grant flash is visible.
    9. Open /settings (takeover overlay). Confirm the overlay covers the canvas but does NOT re-init it. Close the overlay (back) and confirm the canvas instantly reappears with no flicker/rebuild (build-once preserved across takeovers) and any active-session Current trail resumes.
    10. Confirm the sacred flow feels intact: open app -> tap Cell -> start session is a one-tap path with the canvas never disappearing during core play.

    Expected outcome: particles are perceivable during their emitting events (the 06-05 Task 3 blocker is dissolved). The canvas never unmounts during /, /cells/:id, /core navigation. Takeovers cover but do not destroy the canvas.
  </action>
  <verify>
    <automated>npm run build && npx playwright test canvas-smoke --grep "persists across core routes|identity unchanged|takeover covers|particles visible during"</automated>
    <human-check>Confirm via the 10-step smoke above that particles are perceivable during events and the canvas stays mounted across core routes + takeovers.</human-check>
  </verify>
  <done>Human types "approved" if particles are visible during session finish + Core convert/store with the canvas staying mounted throughout (UI-03/VER-06 re-close; Phase 6.1 success criteria 1, 2, 5, 6 met), or describes what is missing (e.g. "particles still static", "canvas unmounts on /core", "dock misaligned"). If the sacred tap-Cell->start-session flow feels degraded in any way, flag it.</done>
  <acceptance_criteria>
    - The automated canvas-smoke tests (Task 1) pass against the production build.
    - Human confirms (this checkpoint): Current-trail particles visible during an active session; Bloom/Core ripple visible at session finish; Core convert/store ripple visible on /core; canvas stays mounted across /, /cells/:id, /core; /settings takeover covers without re-init and the canvas instantly reappears on return.
    - The sacred open-app -> tap-Cell -> start-session flow is preserved/strengthened (one tap, canvas visible).
    - On approval: UI-03 and VER-06 re-close; Phase 6.1 success criteria 1, 2, 5, 6 are met. (Criteria 3, 4 validated by Plan 02.)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production build | E2E runs against vite preview (D-17); no new trust boundary — verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.1-06 | Repudiation | test coverage gaps | mitigate | Task 1 + Task 2 structurally assert the persistence invariant (canvas mounted on all core routes, identity unchanged across param change, takeover-coverage) so the human smoke is not the sole evidence — the structural probe is load-bearing. |
| T-6.1-SC | Tampering | npm install | accept | No new packages this phase (Playwright + axe already installed in Phase 6). |
</threat_model>

<verification>
- `npx vitest run` passes (all UI tests green including the new layout-route + WebGL-fail continuity tests).
- `npm run build && npx playwright test` passes (canvas-smoke + accessibility + release-flow E2E green against the production preview build).
- Human-verify checkpoint (Task 3) returns "approved" — particles visible during events; canvas mounted across core routes + takeovers.
</verification>

<success_criteria>
- VER-06 re-closes: canvas/WebGL smoke checks structurally assert the canvas persists on /cells/:id + /core (not only /) and the probe is non-zero throughout session lifecycle.
- UI-03 re-closes: particles are perceivable during their emitting events (human-confirmed via Task 3).
- UI-08 validated end-to-end: canvas mounted/visible during all core signal-producing interactions; only Settings/Forge are full-screen takeovers; layout degrades safely on WebGL-fail (D-09) with no reflow.
- All 6 Phase 6.1 ROADMAP success criteria met (1, 2, 5, 6 via this plan; 3, 4 via Plan 02).
- The Phase 6 06-05 Task 3 human smoke (re-opened) PASSES — Phase 6 can fully complete.
</success_criteria>

<output>
Create `.planning/phases/06.1-canvas-always-visible-layout-pivot/06.1-03-SUMMARY.md` when done. On checkpoint approval, update STATE.md to reflect Phase 6.1 complete + 06-05 Task 3 re-closed (UI-03/VER-06 closed).
</output>

## Artifacts this phase produces (Plan 03)

**Modified test symbols:**
- `tests/e2e/canvas-smoke.spec.ts` — four new tests: persists-across-core-routes, param-change-identity, takeover-coverage, particles-during-session-finish (re-opened 06-05 Task 3 structural component).
- `tests/ui/flowgrid-home.test.tsx` — layout-route canvas-persistence test (extended from Plan 01) + WebGL-fail continuity test (D-09).
- `tests/ui/cell-list-a11y.test.tsx` — Cells-nav-present-across-core-routes assertion (D-03 chrome persistence).
- `tests/ui/cell-board.test.tsx` — CellBoard-renders-alside-canvas + Return-to-Flowgrid-keeps-canvas-mounted assertions.

**No new production symbols** — this plan is verification only (it consumes Plans 01 + 02 artifacts).

**Checkpoint artifact:** the re-opened 06-05 Task 3 human-verify result (approved/failed) recorded in the plan SUMMARY + STATE.md.
