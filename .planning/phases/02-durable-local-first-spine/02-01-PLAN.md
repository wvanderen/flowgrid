---
phase: 02-durable-local-first-spine
plan: 01
type: execute
wave: 1
depends_on:
  - 01-03
files_modified:
  - package.json
  - vitest.config.ts
  - eslint.config.js
  - src/persistence/README.md
  - src/persistence/errors.ts
  - src/persistence/ids.ts
  - src/persistence/database.ts
  - src/persistence/seeding.ts
  - src/persistence/diff.ts
  - src/persistence/repository.ts
  - src/persistence/index.ts
  - tests/helpers/setup-indexeddb.ts
  - tests/persistence/boundaries.test.ts
  - tests/persistence/schema.test.ts
  - tests/persistence/repository.test.ts
  - tests/persistence/diff.test.ts
  - tests/persistence/append-only.test.ts
  - tests/persistence/errors.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - SESS-04
  - VER-03
must_haves:
  truths:
    - "D-01: Hybrid records-plus-operation-log model — Reloading after an applied complete_focus_session result preserves the changed Cell, Core, sessions, operations, and client.updatedAt in IndexedDB (entity records are the read path; the operation log is for sync/audit, not replay)."
    - "D-05: One IndexedDB object store per durable entity type — Nine normalized Dexie object stores exist keyed by their stable string IDs."
    - "D-06: ModuleDefinitions are NOT persisted — no moduleDefinitions store is created (only user-owned ModuleInstance records go in IndexedDB; static ModuleDefinitions stay as versioned code content in src/content/starter-modules.ts)."
    - "A first run seeds exactly one ClientRecord, one SettingsRecord, and one CoreRecord with starter defaults; cells/moduleInstances/routes/sessions/operations/forgeHistory start empty."
    - "applyResult writes nothing when result.status is 'rejected' or 'not_implemented'."
    - "Applying the same applied result twice is a no-op on the second write; a session or operation whose ID exists with a different payload is rejected as a typed PersistenceError (session_conflict / operation_conflict)."
    - "All writes for one applied result execute inside a single Dexie transaction spanning all nine stores."
    - "Persistence source imports no simulation runtime (SimulationResult is type-only) and no direct dexie import outside database.ts."
  prohibitions:
    - "No ModuleDefinition records are persisted to IndexedDB (definitions stay as versioned code content)."
    - "No update/delete API exists on FlowgridRepository for sessions, operations, or forgeHistory."
    - "Persistence never calls simulation functions or emits economy/visual events."
    - "No floats are introduced for economy values; records stay integer-branded."
  artifacts:
    - path: "src/persistence/database.ts"
      provides: "Dexie subclass with v1 schema and populate/blocked hooks"
      exports: ["FlowgridDatabase"]
      contains: "version(1)"
    - path: "src/persistence/repository.ts"
      provides: "repository write/read boundary"
      exports: ["FlowgridRepository"]
      contains: "applyResult"
    - path: "src/persistence/diff.ts"
      provides: "pure snapshot diff to write plan"
      exports: ["diffFlowgridSnapshots"]
  key_links:
    - from: "src/persistence/repository.ts"
      to: "src/domain/result.ts"
      via: "type-only SimulationResult consumption"
      pattern: "import type"
    - from: "src/persistence/database.ts"
      to: "src/persistence/seeding.ts"
      via: "on('populate') first-run seed"
      pattern: "seedStarterState"
---

<objective>
Deliver the durable reload slice: a Dexie/IndexedDB repository that consumes Phase 1 applied SimulationResult outputs, persists normalized records plus append-only sessions and operations, and reloads identical state. This is the load-bearing spine DATA-01..DATA-03 and SESS-04 depend on, verified by the schema/repository/append-only tier of VER-03.

Purpose: Prove Flowgrid data survives reload as normalized local-first records before any UI or export surface is built.
Output: Dependencies, config, error contract, Dexie schema + seeding, pure snapshot diff, repository write/read path, barrel, and the core persistence test suite.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-durable-local-first-spine/02-CONTEXT.md
@.planning/phases/02-durable-local-first-spine/02-RESEARCH.md
@.planning/phases/02-durable-local-first-spine/02-PATTERNS.md
@.planning/phases/01-deterministic-foundation-slice/01-03-SUMMARY.md
</context>

## Dependency Graph

| Plan | Needs | Creates | Wave |
|---|---|---|---|
| 02-01 | Phase 1 SimulationResult + durable record contracts | Dexie schema, repository write/read, error contract, reload tests | 1 |
| 02-02 | 02-01 repository read path | JSON + CSV export | 2 |
| 02-03 | 02-01 repository + errors | Import/restore validation, migration harness, storage-error coverage | 2 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependencies, test infrastructure, and persistence boundary rule</name>
  <files>package.json, vitest.config.ts, tests/helpers/setup-indexeddb.ts, eslint.config.js, tests/persistence/boundaries.test.ts</files>
  <read_first>
    AGENTS.md
    package.json
    vitest.config.ts
    tsconfig.json
    eslint.config.js
    tests/simulation/boundaries.test.ts
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
  </read_first>
  <behavior>
    - dexie (runtime), zod (runtime), and fake-indexeddb (dev) are installed with the verified versions.
    - Vitest loads `fake-indexeddb/auto` once via setupFiles so every persistence test has a global IndexedDB.
    - ESLint blocks `src/persistence/**` from importing simulation at runtime (type-only allowed) and from importing `dexie` outside the database module.
    - A persistence boundary scanner test mirrors `tests/simulation/boundaries.test.ts` and fails if any `src/persistence` file runtime-imports simulation, react, pixi, zustand, or references browser globals.
  </behavior>
  <action>Add `"dependencies": { "dexie": "^4.4.4", "zod": "^4.4.3" }` and add `"fake-indexeddb": "^6.2.5"` under `devDependencies` in package.json. Run `npm install`. In vitest.config.ts add `setupFiles: ['tests/helpers/setup-indexeddb.ts']` inside the `test` block; keep `environment: 'node'` and `globals: false`. Create `tests/helpers/setup-indexeddb.ts` containing `import 'fake-indexeddb/auto';`. In eslint.config.js append a new config object with `files: ['src/persistence/**/*.ts']` whose `no-restricted-imports` mirrors the existing simulation block: `paths` forbids bare `{ name: 'dexie', message: 'Import Dexie through the database module, not directly in repositories.' }`; `patterns` forbids simulation relative imports `../simulation`, `../simulation/*`, `../../simulation`, `../../simulation/*`, `@/simulation`, `@/simulation/*` with message "Persistence must not import simulation. Consume SimulationResult types only." Type-only imports stay permitted because `tsconfig.json` has `verbatimModuleSyntax: false`. Create `tests/persistence/boundaries.test.ts` mirroring `tests/simulation/boundaries.test.ts` but scanning `src/persistence` and forbidding: import react/react-dom/pixi.js/zustand/idb/fake-indexeddb, the `window`/`document`/`localStorage`/`navigator` globals, `Date.now`/`performance.now`/`setTimeout`/`setInterval`, and relative imports into `../simulation`, `../app`, `../render`, `../ui`. Do NOT forbid `indexedDB` or `crypto` (persistence legitimately uses them). Do NOT forbid `dexie` in the scanner (the lint rule handles direct-import policy); the scanner targets layer leakage.</action>
  <verify>
    <automated>npm install</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm run test -- --run tests/persistence/boundaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"dexie": "^4.4.4"`, `"zod": "^4.4.3"`, and `"fake-indexeddb": "^6.2.5"`.
    - `npm install` exits 0 and node_modules/dexie, node_modules/zod, node_modules/fake-indexeddb exist.
    - vitest.config.ts contains the literal `setupFiles: ['tests/helpers/setup-indexeddb.ts']`.
    - tests/helpers/setup-indexeddb.ts contains `import 'fake-indexeddb/auto';`.
    - eslint.config.js contains the literal `files: ['src/persistence/**/*.ts']` and a persistence "must not import simulation" message.
    - tests/persistence/boundaries.test.ts exits 0.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Dependencies are installed, fake-indexeddb is wired into Vitest, the persistence boundary is enforced by both ESLint and a scanner test, and all current tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PersistenceError contract and client ID helper</name>
  <files>src/persistence/errors.ts, src/persistence/ids.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/validation.ts
    src/domain/ids.ts
    src/simulation/operation-events.ts
  </read_first>
  <behavior>
    - PersistenceError is a readonly typed contract with an 8-member kind union and a recoverable boolean (DATA-07 surface; Phase 3 renders it).
    - mapDomException maps known DOMException names and the missing-indexedDB case to the correct kind/recoverable classification.
    - generateClientId returns a crypto.randomUUID() string (the only persistence-generated ID).
  </behavior>
  <action>Create src/persistence/errors.ts with a header comment citing DATA-07 and "Phase 3 renders; Phase 2 ships contract only". Define `PersistenceErrorKind` as the exact union: `'quota_exceeded' | 'persistence_denied' | 'blocked_upgrade' | 'write_failure' | 'session_conflict' | 'operation_conflict' | 'indexeddb_unavailable' | 'unknown'`. Define a readonly `PersistenceError` interface with `kind: PersistenceErrorKind`, `message: string`, `recoverable: boolean`, and `cause?: unknown` (no `| undefined` because `exactOptionalPropertyTypes` is on). Export `mapDomException(e: unknown): PersistenceError` mapping `QuotaExceededError`→`quota_exceeded`/recoverable true, `SecurityError`→`persistence_denied`/true, `InvalidStateError`→`blocked_upgrade`/true, `UnknownError`/`DataError`/`OperationError`→`write_failure`/true, else `unknown`/false; when the global `indexedDB` is undefined return `indexeddb_unavailable`/recoverable false. Mirror the discriminated-shape style of src/domain/validation.ts. Create src/persistence/ids.ts exporting `export function generateClientId(): string { return crypto.randomUUID(); }` with a header noting it is the single persistence-generated ID and that operation/session IDs stay command-supplied.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/errors.ts exports `PersistenceError`, `PersistenceErrorKind`, and `mapDomException`.
    - `PersistenceErrorKind` is the exact 8-member union listed above.
    - `mapDomException(new DOMException('m', 'QuotaExceededError'))` returns an object whose `.kind === 'quota_exceeded'` and `.recoverable === true`.
    - src/persistence/ids.ts exports `generateClientId` and its body calls `crypto.randomUUID()`.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The typed storage-error contract and the client-ID helper exist and typecheck cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Dexie schema, first-run seeding, layer docs, and barrel</name>
  <files>src/persistence/database.ts, src/persistence/seeding.ts, src/persistence/README.md, src/persistence/index.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/records.ts
    src/domain/operation-records.ts
    src/domain/ids.ts
    src/content/starter-state.ts
    src/content/formulas.ts
    src/content/content-version.ts
    src/content/index.ts
    src/persistence/errors.ts
    src/persistence/ids.ts
    src/domain/index.ts
  </read_first>
  <behavior>
    - FlowgridDatabase extends Dexie and declares version(1) with the nine-store topology from RESEARCH §1.1.
    - on('populate') seeds exactly the three singletons (client, core, settings) using the populate transaction; other stores start empty.
    - on('blocked') surfaces a PersistenceError kind 'blocked_upgrade'.
    - The barrel re-exports errors, ids, database, seeding surfaces.
    - README documents the realized layer (replacing the Phase 1 placeholder) and the four version axes.
  </behavior>
  <action>Create src/persistence/database.ts: `import Dexie, { type Table } from 'dexie';` (the only file allowed to import dexie directly). Define `export class FlowgridDatabase extends Dexie` whose constructor calls `super(name)` then declares `this.version(1).stores({ client: 'id', cells: 'id', core: 'id', moduleInstances: 'id, ownerCellId', routes: 'id, sourceCellId', sessions: 'id, cellId, startedAt', operations: 'id, status, createdAt', settings: 'id', forgeHistory: 'id, createdAt' })`. Declare typed table handles `client!: Table<ClientRecord, ClientId>;` through `forgeHistory!: Table<ForgeHistoryRecord, ForgeHistoryId>;` importing record/ID types from `../domain/index.js`. In the constructor register `this.on('populate', (tx) => { seedStarterState(tx); })` (must use the `tx` parameter, stay synchronous — constants only) and `this.on('blocked', () => { /* surface PersistenceError kind blocked_upgrade via the error contract */ })`. Header comment documents all four version axes: Dexie schema version, ContentVersion, SyncOperation.payloadVersion, and archiveVersion. Create src/persistence/seeding.ts exporting `seedStarterState(tx: Transaction): void` that inserts into `tx.table('client')`, `tx.table('core')`, and `tx.table('settings')` ONLY. Build the records from the same constants starter-state.ts uses: import `STARTER_CONTENT_VERSION`, `DEFAULT_CONVERT_ALLOCATION_PERCENT`, `DEFAULT_STORE_ALLOCATION_PERCENT`, `DEFAULT_SESSION_LENGTH_SECONDS`, `DEFAULT_DAILY_TARGET_SECONDS`, `DEFAULT_LOCAL_DAY_BOUNDARY` from `../content/index.js`; import `generateClientId` from `./ids.js`. ClientRecord gets `id: generateClientId()`, `contentVersion: STARTER_CONTENT_VERSION`. CoreRecord gets zeroed `energy/coreCharge/lifetimeEnergy/integration/moduleTokens/forgeCount` and the default allocation pair. SettingsRecord gets the three default constants. Generate `now` via `new Date().toISOString()` inside seeding (persistence MAY use ambient time here only). Leave cells/moduleInstances/routes/sessions/operations/forgeHistory untouched (empty). Update src/persistence/README.md: remove the "Phase 1 defers persistence" text; state the layer is realized as nine normalized Dexie stores keyed by stable IDs; keep the sentence that persistence consumes FlowgridSnapshot/SessionRecord/SyncOperation and never runs simulation rules; document the four version axes and reference database.ts/repository.ts. Create src/persistence/index.ts barrel: `export * from './errors.js'; export * from './ids.js'; export * from './database.js'; export * from './seeding.js';` with `.js` extensions and a header comment matching src/domain/index.ts style.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/database.ts contains the literal `this.version(1).stores(` and all nine store names: client, cells, core, moduleInstances, routes, sessions, operations, settings, forgeHistory.
    - src/persistence/database.ts contains `this.on('populate'` and calls `seedStarterState`.
    - No `moduleDefinitions` store is declared anywhere in database.ts.
    - src/persistence/seeding.ts writes only the three singletons (no cells/modules/routes/sessions/operations/forgeHistory writes).
    - src/persistence/seeding.ts imports `STARTER_CONTENT_VERSION` and `generateClientId`.
    - src/persistence/README.md no longer contains the phrase "Phase 1 defers persistence".
    - src/persistence/index.ts exists and re-exports errors, ids, database, seeding with `.js` extensions.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>A Dexie v1 schema with nine normalized stores opens, seeds three singletons on first run, and the layer is documented and barrel-exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement pure snapshot diff</name>
  <files>src/persistence/diff.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/records.ts
    src/domain/invariants.ts
    src/simulation/commands/complete-focus-session.ts
  </read_first>
  <behavior>
    - diffFlowgridSnapshots(previous, next) is pure: no IndexedDB, no dexie, no simulation runtime imports.
    - It returns a typed FlowgridWritePlan describing puts/deletes for map entities, singleton puts, and appends for the three append-only arrays.
    - Identical inputs produce an empty plan.
  </behavior>
  <action>Create src/persistence/diff.ts with a header comment citing D-03 (changed-record detection lives in persistence, not simulation). Define a `FlowgridWritePlan` readonly interface (inline in this module) holding: `puts` grouped by entity kind is acceptable, but concretely use fields `cellPuts: readonly CellRecord[]`, `moduleInstancePuts: readonly ModuleInstance[]`, `routePuts: readonly RouteRecord[]`, `cellDeletes: readonly CellId[]`, `moduleInstanceDeletes: readonly ModuleInstanceId[]`, `routeDeletes: readonly RouteId[]`, `clientPut: ClientRecord | null`, `corePut: CoreRecord | null`, `settingsPut: SettingsRecord | null`, `appendSessions: readonly SessionRecord[]`, `appendOperations: readonly SyncOperation[]`, `appendForgeHistory: readonly ForgeHistoryRecord[]`. Implement `diffFlowgridSnapshots(previous: FlowgridSnapshot, next: FlowgridSnapshot): FlowgridWritePlan`. For map entities (cells/moduleInstances/routes): iterate next map keys; a key absent in previous or whose `JSON.stringify(prev) !== JSON.stringify(next)` is a put; a key in previous but absent in next is a delete. For singletons (client/core/settings): `JSON.stringify(prev) !== JSON.stringify(next)` → put, else null. For arrays (sessions/operations/forgeHistory): elements whose id is not in the previous id-set are appends. Import record/ID types type-only from `../domain/index.js`. Add a header note that JSON-compare is safe under Phase 1 deterministic replay (Phase 1 D-08).</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/diff.ts exports `diffFlowgridSnapshots` and the `FlowgridWritePlan` type.
    - diff.ts has no `import` of `dexie`, `fake-indexeddb`, or any `../simulation` path.
    - `diffFlowgridSnapshots(s, s)` for identical snapshots returns a plan whose `cellPuts`, `moduleInstancePuts`, `routePuts`, all deletes, `appendSessions`, `appendOperations`, `appendForgeHistory` are empty and whose singleton puts are null.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The pure diff function detects changed/deleted/append records from a snapshot pair without touching IndexedDB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement repository write and read path</name>
  <files>src/persistence/repository.ts, src/persistence/index.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/result.ts
    src/domain/records.ts
    src/domain/operation-records.ts
    src/simulation/operation-events.ts
    src/persistence/database.ts
    src/persistence/diff.ts
    src/persistence/errors.ts
    src/simulation/commands/complete-focus-session.ts
  </read_first>
  <behavior>
    - applyResult consumes a SimulationResult type-only; writes nothing for rejected/not_implemented.
    - For applied results it diffs prev/next and writes the plan + appends inside one transaction over all nine stores with idempotent-upsert (D-04) conflict checks.
    - loadSnapshot bulk-reads all stores into a FlowgridSnapshot (Maps for cells/moduleInstances/routes, arrays for sessions/operations/forgeHistory).
    - open/close wrap db.open()/db.close().
  </behavior>
  <action>Create src/persistence/repository.ts. Type-only import `SimulationResult`, `FlowgridSnapshot`, `SessionRecord`, `SyncOperation` from `../domain/index.js` (never runtime). Import `FlowgridDatabase` from `./database.js`, `diffFlowgridSnapshots` from `./diff.js`, `mapDomException`/`PersistenceError` from `./errors.js`. Define `export type ApplyResult = { ok: true } | { ok: false; error: PersistenceError }`. Define `export class FlowgridRepository { constructor(private readonly db: FlowgridDatabase) {} async open(): Promise<void>; async close(): Promise<void>; async applyResult(result: SimulationResult): Promise<ApplyResult>; async loadSnapshot(): Promise<FlowgridSnapshot>; }`. In `applyResult`: if `result.status !== 'applied'` return `{ ok: true }` immediately (writes nothing). Compute the plan via `diffFlowgridSnapshots(result.previousState, result.nextState)`. Execute `await this.db.transaction('rw', this.db.client, this.db.cells, this.db.core, this.db.moduleInstances, this.db.routes, this.db.sessions, this.db.operations, this.db.settings, this.db.forgeHistory, async () => { ... })`. Inside: perform the puts/deletes from the plan; for each appendSession/appendOperation/appendForgeHistory run an idempotent-upsert helper: `const existing = await table.get(record.id);` if existing, compare `JSON.stringify(existing) === JSON.stringify(record)` → skip, else return/throw a `{ kind: 'session_conflict' }` / `{ kind: 'operation_conflict' }` PersistenceError (forgeHistory uses a generic write_failure or an analogous conflict — keep it consistent and documented). Wrap DOMException via mapDomException and return `{ ok: false, error }`. In `loadSnapshot`: read all singletons with `db.client.get(...)` patterns — because noUncheckedIndexedAccess is on, after bulk reads assert presence of the three singletons (they are seeded on first run); build `new Map(...)` for cells/moduleInstances/routes and `[...arr]` for the arrays; return a FlowgridSnapshot matching src/domain/records.ts:122-132. Expose NO updateSession/deleteOperation/deleteSession methods. Update src/persistence/index.ts to also `export * from './repository.js';`. Header comment cites DATA-03 (atomic writes), D-02 (rejected/not_implemented write nothing), D-04 (idempotent upsert), and Phase 1 D-08 (determinism makes JSON-compare safe).</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm run test -- --run tests/persistence/boundaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/repository.ts exports `FlowgridRepository` and `ApplyResult`; its simulation import is `import type`.
    - `applyResult` returns `{ ok: true }` without writes when `result.status !== 'applied'`.
    - `applyResult` wraps writes in a single `this.db.transaction('rw', ...)` listing all nine tables.
    - The idempotent-upsert path compares existing vs incoming via JSON.stringify and produces a `session_conflict`/`operation_conflict` PersistenceError on mismatch.
    - `loadSnapshot` returns an object satisfying `FlowgridSnapshot` with `Map` cells/moduleInstances/routes.
    - No `updateSession`, `deleteSession`, or `deleteOperation` method exists on FlowgridRepository.
    - tests/persistence/boundaries.test.ts still exits 0 (no runtime simulation import added).
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The repository writes applied results atomically with conflict checks and reloads them into an identical FlowgridSnapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Core VER-03 tests — schema, repository reload, diff, append-only, errors</name>
  <files>tests/persistence/schema.test.ts, tests/persistence/repository.test.ts, tests/persistence/diff.test.ts, tests/persistence/append-only.test.ts, tests/persistence/errors.test.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    tests/helpers/fixtures.ts
    tests/helpers/replay.ts
    tests/simulation/foundation-loop.test.ts
    src/persistence/database.ts
    src/persistence/repository.ts
    src/persistence/diff.ts
    src/persistence/errors.ts
    src/simulation/engine.ts
  </read_first>
  <behavior>
    - Each test uses a fresh IDBFactory (`new IDBFactory()` from fake-indexeddb in beforeEach) and a unique database name so tests are isolated.
    - Schema test asserts nine stores exist and that a fresh open seeds the three singletons with starter values; the other six stores are empty.
    - Repository test drives a real applied complete_focus_session result through applyResult, reopens a new repository/DB, and asserts loadSnapshot deep-equals result.nextState.
    - Append-only test proves idempotent no-op on double-apply and session_conflict/operation_conflict on payload mismatch; it also asserts no mutation API exists.
    - Errors test covers the RESEARCH §6.2 DOMException-name table and the indexedDB-undefined case.
  </behavior>
  <action>Create the five test files. Use `import { test, expect, beforeEach } from 'vitest';` and `import { IDBFactory } from 'fake-indexeddb';` for resets. In beforeEach do `(globalThis as { indexedDB: IDBFactory }).indexedDB = new IDBFactory();`. tests/persistence/schema.test.ts: open a `new FlowgridDatabase('schema-' + uniqueSuffix)`; assert `db.tables` contains the nine store names; call `await repo.loadSnapshot()` and assert `client.contentVersion === STARTER_CONTENT_VERSION`, core has `convertAllocationPercent === DEFAULT_CONVERT_ALLOCATION_PERCENT` and zeroed counters, settings has `DEFAULT_SESSION_LENGTH_SECONDS`, and cells/moduleInstances/routes/sessions/operations/forgeHistory are empty (Map size 0 / array length 0). tests/persistence/repository.test.ts: build starter state via `buildStarterSnapshot('repo')`; run `runSimulationCommand` with a valid `complete_focus_session` to get an applied result (mirror tests/simulation/foundation-loop.test.ts fixtures and env); `await repo.applyResult(result)`; open a second repository on a new DB? No — reopen the SAME db name in a new FlowgridDatabase/FlowgridRepository instance to simulate reload; `const loaded = await repo2.loadSnapshot(); expectStateReplayEqual(loaded, result.nextState)` using tests/helpers/replay.ts. Also assert `applyResult` on a rejected result and a not_implemented result writes nothing (compare store counts before/after). tests/persistence/diff.test.ts: pure test — call `diffFlowgridSnapshots` on identical snapshots (empty plan) and on a pair where one cell's `xp` changed (assert exactly one cellPut, no other puts, singleton/client null when unchanged). tests/persistence/append-only.test.ts: apply an applied result twice → second apply returns `{ ok: true }` and store counts unchanged; construct a SessionRecord with an existing id but mutated `xpGained` and drive it through the conflict path → assert `error.kind === 'session_conflict'`; same for operations → `operation_conflict`; assert `typeof (repo as unknown as { updateSession?: unknown }).updateSession === 'undefined'` (no mutation API). tests/persistence/errors.test.ts: assert each row of RESEARCH §6.2 — `mapDomException(new DOMException('m','QuotaExceededError')).kind === 'quota_exceeded'` and recoverable true; SecurityError→persistence_denied; and a case where global indexedDB is temporarily undefined → `indexeddb_unavailable`/recoverable false (mock and restore).</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm run test -- --run tests/persistence/</automated>
  </verify>
  <acceptance_criteria>
    - tests/persistence/schema.test.ts exits 0 and asserts all nine store names and the three seeded singletons.
    - tests/persistence/repository.test.ts exits 0 and `expectStateReplayEqual(loaded, result.nextState)` passes.
    - tests/persistence/diff.test.ts exits 0 with an empty plan for identical snapshots and exactly one cellPut for a single-cell change.
    - tests/persistence/append-only.test.ts exits 0, proving double-apply no-op and the two conflict kinds.
    - tests/persistence/errors.test.ts exits 0 covering QuotaExceededError, SecurityError, and the indexedDB-undefined case.
    - `npm run typecheck`, `npm run lint`, and `npm run test -- --run` all exit 0 together.
  </acceptance_criteria>
  <done>The core VER-03 tier passes: schema creation, first-run seeding, reload durability, diff correctness, append-only/conflict enforcement, and error mapping.</done>
</task>

</tasks>

## Artifacts this phase produces

- Dependencies/IDs: `dexie` ^4.4.4 (runtime), `zod` ^4.4.3 (runtime), `fake-indexeddb` ^6.2.5 (dev); `generateClientId()` (crypto.randomUUID).
- Error contract: `PersistenceError`, `PersistenceErrorKind` (8-member union), `mapDomException`.
- Database: `FlowgridDatabase` class, `version(1)` nine-store schema, `on('populate')`/`on('blocked')` hooks.
- Seeding: `seedStarterState(tx)` (three singletons only).
- Diff: `FlowgridWritePlan` type, `diffFlowgridSnapshots`.
- Repository: `FlowgridRepository` (open, close, applyResult, loadSnapshot), `ApplyResult` type.
- Barrel: `src/persistence/index.ts` re-exports.
- Tests: `tests/helpers/setup-indexeddb.ts`, `tests/persistence/{boundaries,schema,repository,diff,append-only,errors}.test.ts`.
- Config changes: `package.json`, `vitest.config.ts` (setupFiles), `eslint.config.js` (persistence no-restricted-imports), `src/persistence/README.md` (realized layer).

<must_haves>
- DATA-01: applied results reload identically from IndexedDB (repository + schema tests prove it).
- DATA-02: nine normalized entity stores with v1 schema versioning; no blob store, no moduleDefinitions store.
- DATA-03: each applied result writes changed records + appends inside one Dexie transaction over all nine stores.
- SESS-04: completed sessions persist as append-only SessionRecords with all required fields, enforced by idempotent-upsert conflict checks.
- VER-03: schema creation, repository writes, append-only sessions, and error mapping are covered by persistence tests.
</must_haves>

<verification>
Run `npm run typecheck`, `npm run lint`, and `npm run test -- --run`. The persistence boundary, schema, repository reload, diff, append-only, and errors suites must pass alongside all existing Phase 1 tests.
</verification>

<success_criteria>
A Flowgrid reload preserves Cells, Core, modules, routes, sessions, operations, settings, and client across a Dexie transaction; rejected/not_implemented results write nothing; sessions and operations are append-only with payload-mismatch conflicts; and the persistence layer never imports simulation runtime.
</success_criteria>

<output>
Create `.planning/phases/02-durable-local-first-spine/02-01-SUMMARY.md` when done.
</output>
