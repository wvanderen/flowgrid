---
phase: 02-durable-local-first-spine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/persistence/export-json.ts
  - src/persistence/export-csv.ts
  - src/persistence/index.ts
  - tests/persistence/export-json.test.ts
  - tests/persistence/export-csv.test.ts
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - VER-03
must_haves:
  truths:
    - "exportJson returns an archive whose archiveVersion === 1, with an exportedAt ISO timestamp and all nine entity collections."
    - "D-09: JSON full-state export contains every entity record plus the full operation log and session history — The JSON archive includes the full operation log and session history (the operation log is not stripped on export)."
    - "exportSessionCsv emits a header row with exactly the ten D-10 columns and one row per session sorted by startedAt ascending, with cellName joined from cells and durationMinutes floored from durationSeconds."
    - "CSV fields containing a comma, double quote, or newline are RFC-4180 quoted."
    - "An export-then-import-replace round-trip reproduces the original snapshot (when import ships in 02-03; here the export shape is self-consistent and re-loadable via loadSnapshot-equivalent reads)."
  prohibitions:
    - "Export never invokes simulation rules or emits economy/visual events."
    - "CSV never emits a raw cellId; cellName is always the joined name (or '(unknown)' if the cell is missing)."
    - "JSON export never strips or redacts the operation log."
    - "Export never parses untrusted input with Zod (Zod is import-only, delivered in 02-03)."
  artifacts:
    - path: "src/persistence/export-json.ts"
      provides: "JSON full-state archive export"
      exports: ["JsonArchive", "exportJson"]
      contains: "archiveVersion"
    - path: "src/persistence/export-csv.ts"
      provides: "human-readable CSV session export"
      exports: ["exportSessionCsv", "csvEscape"]
  key_links:
    - from: "src/persistence/export-json.ts"
      to: "src/persistence/repository.ts"
      via: "read all stores"
      pattern: "loadSnapshot|db"
    - from: "src/persistence/export-csv.ts"
      to: "src/domain/records.ts"
      via: "SessionRecord/CellRecord field mapping"
      pattern: "durationSeconds"
---

<objective>
Deliver the ownership-out slice: users can get their full local state out as a JSON archive and their completed sessions out as a human-readable CSV. Builds on the 02-01 repository read path.

Purpose: DATA-04 and DATA-05 — user-owned data must be portable and inspectable, not trapped in IndexedDB.
Output: JSON archive type + exporter, CSV exporter with cell-name join and RFC-4180 escaping, barrel re-exports, and the export tier of VER-03.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-durable-local-first-spine/02-CONTEXT.md
@.planning/phases/02-durable-local-first-spine/02-RESEARCH.md
@.planning/phases/02-durable-local-first-spine/02-PATTERNS.md
@.planning/phases/02-durable-local-first-spine/02-01-SUMMARY.md
</context>

## Dependency Graph

| Plan | Needs | Creates | Wave |
|---|---|---|---|
| 02-01 | Phase 1 contracts | Dexie schema, repository, error contract | 1 |
| 02-02 | 02-01 read path | JSON + CSV export | 2 |
| 02-03 | 02-01 repository + errors | Import/restore validation, migration harness | 2 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement JSON full-state archive export</name>
  <files>src/persistence/export-json.ts, src/persistence/index.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/records.ts
    src/domain/operation-records.ts
    src/persistence/database.ts
    src/persistence/repository.ts
    src/simulation/operation-events.ts
  </read_first>
  <behavior>
    - JsonArchive is a readonly, JSON-serializable envelope: archiveVersion literal 1, exportedAt ISO timestamp, and every entity collection as readonly arrays (Maps flatten to arrays; no Map in JSON).
    - exportJson reads all nine stores and assembles the archive including the full operations and sessions arrays.
    - No simulation involvement; no Zod (import boundary only, delivered in 02-03).
  </behavior>
  <action>Create src/persistence/export-json.ts. Define `export const ARCHIVE_VERSION = 1;` (module-local const in the style of PAYLOAD_VERSION in operation-events.ts). Define a readonly `JsonArchive` interface with: `archiveVersion: 1`, `exportedAt: IsoDateTimeString`, `client: ClientRecord`, `cells: readonly CellRecord[]`, `core: CoreRecord`, `moduleInstances: readonly ModuleInstance[]`, `routes: readonly RouteRecord[]`, `sessions: readonly SessionRecord[]`, `operations: readonly SyncOperation[]`, `settings: SettingsRecord`, `forgeHistory: readonly ForgeHistoryRecord[]`. Import all record types type-only from `../domain/index.js`. Define `export async function exportJson(db: FlowgridDatabase): Promise<JsonArchive>` that reads each store with `db.<table>.toArray()` and returns the archive with `exportedAt: new Date().toISOString()` (persistence MAY use ambient time for export timestamps). Do not strip or redact operations or sessions. Update src/persistence/index.ts to `export * from './export-json.js';`. Header comment documents that `archiveVersion` is a fourth version axis (envelope shape) distinct from Dexie schema version, ContentVersion, and payloadVersion.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/export-json.ts exports `JsonArchive`, `exportJson`, and `ARCHIVE_VERSION`.
    - `ARCHIVE_VERSION === 1`.
    - The `JsonArchive` type has exactly the eleven fields listed (archiveVersion, exportedAt, and nine entity collections) with `operations: readonly SyncOperation[]`.
    - export-json.ts has no import of `zod` or any `../simulation` path.
    - src/persistence/index.ts re-exports `./export-json.js`.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>A complete, serializable JSON archive of all durable records (including the operation log) can be produced from the repository.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CSV session export with cell-name join and escaping</name>
  <files>src/persistence/export-csv.ts, src/persistence/index.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/records.ts
    src/content/formulas.ts
    src/persistence/database.ts
    src/persistence/repository.ts
  </read_first>
  <behavior>
    - csvEscape wraps fields containing comma, double-quote, or newline in RFC-4180 double quotes and doubles internal quotes.
    - exportSessionCsv produces a header row with the exact ten D-10 columns and one data row per session sorted by startedAt ascending.
    - cellName is joined from the cells store; a missing cell maps to '(unknown)'. durationMinutes is Math.floor(durationSeconds / 60). Dates are the first 10 chars (YYYY-MM-DD) of the ISO timestamp.
  </behavior>
  <action>Create src/persistence/export-csv.ts. Export `csvEscape(field: string): string`: if the field contains `,`, `"`, or `\n`/`\r`, wrap in `"..."` and replace every `"` with `""`; otherwise return unchanged. Export `export async function exportSessionCsv(db: FlowgridDatabase): Promise<string>`. Read sessions via `db.sessions.toArray()` and cells via `db.cells.toArray()`; build a `Map<CellId, CellRecord>` from cells for the join. Sort sessions ascending by `startedAt`. Header row (comma-joined): `startedDate,endedDate,durationMinutes,cellName,xpGained,currentGenerated,energyGained,coreChargeGained,bloomFired,activationGranted`. Per row: `startedDate = session.startedAt.slice(0,10)`, `endedDate = session.endedAt.slice(0,10)`, `durationMinutes = Math.floor(session.durationSeconds / 60)` (floor for consistency with focusToXp in formulas.ts), `cellName = cellsMap.get(session.cellId)?.name ?? '(unknown)'`, the numeric fields as-is, `bloomFired`/`activationGranted` as the literal strings `'true'`/`'false'`. Apply csvEscape to every field. Join rows with `\r\n` (RFC 4180) and terminate the string with a trailing `\r\n`. Header comment cites D-10. Update src/persistence/index.ts to `export * from './export-csv.js';`. No simulation or zod imports.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/export-csv.ts exports `exportSessionCsv` and `csvEscape`.
    - `csvEscape('a,b')` returns `'"a,b"'`; `csvEscape('a"b')` returns `'"a""b"'`; `csvEscape('plain')` returns `'plain'`.
    - The header row literal contains exactly the ten column names in D-10 order.
    - export-csv.ts has no import of `zod` or any `../simulation` path.
    - src/persistence/index.ts re-exports `./export-csv.js`.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Sessions export to a spreadsheet-friendly CSV with joined cell names, minute durations, and RFC-4180 escaping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Export VER-03 tests — JSON archive shape, CSV mapping, and round-trip</name>
  <files>tests/persistence/export-json.test.ts, tests/persistence/export-csv.test.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    tests/helpers/fixtures.ts
    tests/simulation/foundation-loop.test.ts
    src/persistence/export-json.ts
    src/persistence/export-csv.ts
    src/persistence/database.ts
    src/persistence/repository.ts
    src/simulation/engine.ts
  </read_first>
  <behavior>
    - JSON export test seeds known state (including an applied focus session so sessions/operations are non-empty), exports, and asserts every collection deep-equals the input and archiveVersion === 1.
    - CSV export test uses a cell named with a comma and a session of 1500s and asserts the header, minute value (25), joined (quoted) cell name, YYYY-MM-DD dates, and true/false booleans; it also unit-tests csvEscape for comma/quote/newline.
    - Tests use the fake-indexeddb reset + unique DB name pattern established in 02-01 Task 6.
  </behavior>
  <action>Create tests/persistence/export-json.test.ts and tests/persistence/export-csv.test.ts. Share the reset/unique-name setup from 02-01 (beforeEach `new IDBFactory()`). export-json.test.ts: build starter state, run an applied `complete_focus_session` via runSimulationCommand, `applyResult` it through a repository, then `const archive = await exportJson(db);`; assert `archive.archiveVersion === 1`, `archive.exportedAt` matches `/^\d{4}-\d{2}-\d{2}T/`, and that `archive.cells`, `archive.core`, `archive.settings`, `archive.client`, `archive.moduleInstances`, `archive.routes`, `archive.sessions`, `archive.operations`, `archive.forgeHistory` each deep-equal the corresponding collections from `result.nextState` (use `[...nextState.cells.values()]` for maps). Assert `archive.operations.length === 1` and `archive.sessions.length === 1`. export-csv.test.ts: create a cell whose `name` is `"Starter, Cell"` (comma) and a session with `durationSeconds: 1500`; `const csv = await exportSessionCsv(db);` split on `\r\n`; assert the first row equals the exact ten-column header; assert the data row's `durationMinutes` column is `25`; assert the `cellName` column is `"Starter, Cell"` (quoted, containing the doubled-quote-free comma); assert `startedDate`/`endedDate` columns match `YYYY-MM-DD`; assert `bloomFired`/`activationGranted` are `'true'` or `'false'`. Add direct unit assertions for `csvEscape` covering comma, double-quote, newline, and a plain value.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm run test -- --run tests/persistence/export-json.test.ts tests/persistence/export-csv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/persistence/export-json.test.ts exits 0 and asserts `archiveVersion === 1`, a present exportedAt, all nine collections, `operations.length === 1`, and deep-equality with the applied result's nextState collections.
    - tests/persistence/export-csv.test.ts exits 0 and asserts the exact header, `durationMinutes === 25`, the quoted `"Starter, Cell"` cell name, YYYY-MM-DD dates, and true/false booleans.
    - csvEscape is asserted for comma, double-quote, newline, and plain inputs.
    - `npm run typecheck`, `npm run lint`, and `npm run test -- --run` all exit 0 together.
  </acceptance_criteria>
  <done>The export tier of VER-03 passes: JSON archive completeness and CSV column/escaping correctness are verified.</done>
</task>

</tasks>

## Artifacts this phase produces

- Exporters: `JsonArchive` type, `ARCHIVE_VERSION = 1`, `exportJson`; `exportSessionCsv`, `csvEscape`.
- Barrel additions: `src/persistence/index.ts` re-exports `./export-json.js` and `./export-csv.js`.
- Tests: `tests/persistence/export-json.test.ts`, `tests/persistence/export-csv.test.ts`.

<must_haves>
- DATA-04: full local state (all nine collections, including the operation log and session history) is exportable as a JSON archive with archiveVersion === 1.
- DATA-05: completed sessions export as a human-readable CSV with the exact ten D-10 columns, joined cell names, minute durations, and RFC-4180 escaping.
- VER-03: export behavior is covered by persistence tests.
</must_haves>

<verification>
Run `npm run typecheck`, `npm run lint`, and `npm run test -- --run`. The JSON and CSV export suites must pass alongside all existing tests.
</verification>

<success_criteria>
Flowgrid's full durable state is exportable as a complete JSON archive and its sessions as a spreadsheet-friendly CSV, both without invoking simulation rules or touching untrusted-input parsing.
</success_criteria>

<output>
Create `.planning/phases/02-durable-local-first-spine/02-02-SUMMARY.md` when done.
</output>
