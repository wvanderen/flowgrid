---
phase: 02-durable-local-first-spine
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/persistence/validation-schemas.ts
  - src/persistence/import-validation.ts
  - src/persistence/import.ts
  - src/persistence/index.ts
  - tests/persistence/migration-harness.ts
  - tests/persistence/migration-harness.test.ts
  - tests/persistence/import-validation.test.ts
  - tests/persistence/import-replace.test.ts
  - tests/persistence/import-merge.test.ts
autonomous: true
requirements:
  - DATA-06
  - DATA-07
  - VER-03
must_haves:
  truths:
    - "importArchive validates the entire archive before any local write; any ValidationIssue returns { ok: false, issues } and leaves local data byte-identical to its pre-import state."
    - "Validation composes Zod shape parsing (mapped to invalid_operation_shape with path) with Phase 1 validateFlowgridSnapshot (references/resources/allocations/operations)."
    - "Replace mode wipes all nine stores then writes the archive in a single transaction; merge mode upserts by ID without wiping."
    - "mapDomException maps QuotaExceededError to quota_exceeded (recoverable) and the missing-indexedDB case to indexeddb_unavailable (non-recoverable)."
    - "The migration-fixture harness exercises a synthetic field-transform upgrade and is structured for reuse by a future real v1->v2 migration."
  prohibitions:
    - "Import never partially accepts an archive that produced any ValidationIssue."
    - "Import never writes before validation passes."
    - "No forward-looking v2 stub Dexie schema is shipped (the harness proves the mechanism synthetically)."
    - "Zod is never imported outside validation-schemas.ts / import-validation.ts (never in the repository write or reload paths)."
  artifacts:
    - path: "src/persistence/validation-schemas.ts"
      provides: "Zod schemas mirroring domain record shapes"
      exports: ["archiveSchema"]
      contains: "z.object"
    - path: "src/persistence/import-validation.ts"
      provides: "all-or-nothing archive validator"
      exports: ["validateArchive"]
    - path: "src/persistence/import.ts"
      provides: "replace/merge restore execution"
      exports: ["importArchive", "ImportResult", "ImportStats"]
    - path: "tests/persistence/migration-harness.ts"
      provides: "reusable migration-fixture runner"
      exports: ["runMigrationFixture"]
  key_links:
    - from: "src/persistence/import-validation.ts"
      to: "src/domain/invariants.ts"
      via: "reuse Phase 1 validateFlowgridSnapshot"
      pattern: "validateFlowgridSnapshot"
    - from: "src/persistence/import.ts"
      to: "src/persistence/import-validation.ts"
      via: "validate-before-write gate"
      pattern: "validateArchive"
---

<objective>
Deliver the restore-safety slice: all-or-nothing import/restore validation and execution, a reusable synthetic migration-fixture harness, and completion of the DATA-07 storage-error coverage. Builds on the 02-01 repository and error contract.

Purpose: DATA-06 (restore validates before touching local data), DATA-07 (typed recoverable/non-recoverable storage-error contract), D-07 (migration harness reusable by real v2), and the import/migration tier of VER-03.
Output: Zod boundary schemas, archive validator composing Zod + Phase 1 invariants, replace/merge import execution, migration-fixture harness, and the import/restore/migration test suite.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-durable-local-first-spine/02-CONTEXT.md
@.planning/phases/02-durable-local-first-spine/02-RESEARCH.md
@.planning/phases/02-durable-local-first-spine/02-PATTERNS.md
@.planning/phases/02-durable-local-first-spine/02-01-SUMMARY.md
</context>

## Dependency Graph

| Plan | Needs | Creates | Wave |
|---|---|---|---|
| 02-01 | Phase 1 contracts | Dexie schema, repository, error contract | 1 |
| 02-02 | 02-01 read path | JSON + CSV export | 2 |
| 02-03 | 02-01 repository + errors | Import/restore validation, migration harness | 2 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Zod validation schemas for the archive boundary</name>
  <files>src/persistence/validation-schemas.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/records.ts
    src/domain/operation-records.ts
    src/persistence/export-json.ts
  </read_first>
  <behavior>
    - One z.object schema per durable record type mirrors src/domain/records.ts; the archive schema composes them with archiveVersion === 1.
    - Integer economy fields use z.number().int().nonnegative(); ISO timestamps use z.string().datetime(); IDs use z.string(); booleans use z.boolean(); SyncOperation.payload uses z.unknown().
    - A type-level assertion catches schema/type drift so a future record-shape change surfaces here.
  </behavior>
  <action>Create src/persistence/validation-schemas.ts with `import { z } from 'zod';` (this and import-validation.ts are the only Phase 2 modules importing zod at runtime). Define `z.object({...})` schemas mirroring each record interface in src/domain/records.ts: clientSchema, cellSchema, coreSchema, moduleInstanceSchema, routeSchema, sessionSchema, settingsSchema, forgeHistorySchema, and operationSchema (mirroring SyncOperation in src/domain/operation-records.ts). Field rules: id/commandType/entityType/entityId/name/localDayBoundary → `z.string()`; integer economy fields (xp, current, charge, momentum, activation, dailyMilestoneProgressSeconds, energy, coreCharge, lifetimeEnergy, integration, moduleTokens, forgeCount, convertAllocationPercent, storeAllocationPercent, allocationPercent, xpGained, currentGenerated, energyGained, coreChargeGained, durationSeconds, defaultSessionLengthSeconds, dailyTargetSeconds, payloadVersion, level, forgeCount) → `z.number().int().nonnegative()`; createdAt/updatedAt/startedAt/endedAt → `z.string().datetime()`; bloomFired/activationGranted → `z.boolean()`; lastBloomLocalDate → `z.string().nullable()`; status → `z.enum(['pending','applied','failed'])`; payload → `z.unknown()`. Define `export const archiveSchema = z.object({ archiveVersion: z.literal(1), exportedAt: z.string(), client: clientSchema, cells: z.array(cellSchema), core: coreSchema, moduleInstances: z.array(moduleInstanceSchema), routes: z.array(routeSchema), sessions: z.array(sessionSchema), operations: z.array(operationSchema), settings: settingsSchema, forgeHistory: z.array(forgeHistorySchema) })`. Add a type-level drift guard: `type _SessionCheck = z.infer<typeof sessionSchema> satisfies SessionRecord;` (import SessionRecord type-only) and equivalent for SyncOperation. Header comment cites STACK.md ("Do not put Zod in hot simulation loops") and D-12.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/validation-schemas.ts imports `z` from `'zod'` and exports `archiveSchema`.
    - `archiveSchema` requires `archiveVersion: z.literal(1)`.
    - operationSchema uses `payload: z.unknown()` and `status: z.enum(['pending','applied','failed'])`.
    - The `satisfies` drift-guard lines typecheck (no compile error).
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Zod boundary schemas mirror every durable record shape and compose into the archive schema, with a drift guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement all-or-nothing archive validator</name>
  <files>src/persistence/import-validation.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/records.ts
    src/domain/invariants.ts
    src/domain/validation.ts
    src/persistence/validation-schemas.ts
    src/persistence/export-json.ts
  </read_first>
  <behavior>
    - validateArchive(unknown) returns readonly ValidationIssue[] and performs no writes.
    - Pipeline: Zod safeParse (shape) -> map failures to ValidationIssue code 'invalid_operation_shape' with path -> assemble parsed arrays into a FlowgridSnapshot -> reuse Phase 1 validateFlowgridSnapshot -> concatenate issues.
    - Zero issues means the caller may proceed to write.
  </behavior>
  <action>Create src/persistence/import-validation.ts. Import `archiveSchema` from `./validation-schemas.js`. Import `validateFlowgridSnapshot` and the types `FlowgridSnapshot`, `ValidationIssue` from `../domain/index.js` (validateFlowgridSnapshot is a runtime import from domain/invariants — allowed; it is pure and not simulation-rule execution). Export `export function validateArchive(input: unknown): readonly ValidationIssue[]`. Steps: (1) `const parsed = archiveSchema.safeParse(input);` if `!parsed.success`, map each Zod issue to a `ValidationIssue` with `code: 'invalid_operation_shape'`, `severity: 'error'`, `entityType: 'operation'`, `entityId: 'archive'`, `path` from the joined Zod issue path, and a message (use zod's issue message); return the array. (2) On success, assemble a `FlowgridSnapshot`: `client`, `core`, `settings` taken directly; `cells/moduleInstances/routes` built as `new Map(parsed.data.<coll>.map(r => [r.id, r]))`; `sessions/operations/forgeHistory` taken as the parsed arrays. (3) `return [...validateFlowgridSnapshot(snapshot)]`. Header comment cites D-12 (all-or-nothing, reuses Phase 1 ValidationIssue contract and invariants — single source of truth for economy safety).</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/import-validation.ts exports `validateArchive` and returns `readonly ValidationIssue[]`.
    - It calls `archiveSchema.safeParse` and `validateFlowgridSnapshot`.
    - Zod failures map to `ValidationIssue` with `code: 'invalid_operation_shape'`.
    - import-validation.ts has no dexie import and no `../simulation` import (it imports domain only).
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The archive validator composes Zod shape parsing with Phase 1 invariants and returns typed issues without writing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement replace/merge import execution</name>
  <files>src/persistence/import.ts, src/persistence/index.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/validation.ts
    src/persistence/database.ts
    src/persistence/repository.ts
    src/persistence/import-validation.ts
    src/persistence/errors.ts
    src/persistence/export-json.ts
  </read_first>
  <behavior>
    - importArchive validates first; on any issue returns { ok: false, issues } and writes nothing.
    - Replace mode wipes all nine stores then bulkPuts the archive inside one transaction.
    - Merge mode upserts each archive record by ID inside one transaction, applying the same D-04 payload-mismatch conflict check as the repository.
    - Errors surface as typed PersistenceError via mapDomException.
  </behavior>
  <action>Create src/persistence/import.ts. Import `FlowgridDatabase` from `./database.js`, `validateArchive` from `./import-validation.js`, `mapDomException`/`PersistenceError` from `./errors.js`, types `ValidationIssue`/`SessionRecord`/`SyncOperation` type-only from `../domain/index.js`, and `JsonArchive` from `./export-json.js`. Define `export type ImportMode = 'replace' | 'merge';` and `export interface ImportStats { readonly client: number; readonly cells: number; readonly core: number; readonly moduleInstances: number; readonly routes: number; readonly sessions: number; readonly operations: number; readonly settings: number; readonly forgeHistory: number; }`. Define `export type ImportResult = { ok: true; stats: ImportStats } | { ok: false; issues: readonly ValidationIssue[] } | { ok: false; error: PersistenceError }`. Export `export async function importArchive(db: FlowgridDatabase, archive: unknown, mode: ImportMode = 'replace'): Promise<ImportResult>`. First `const issues = validateArchive(archive);` if `issues.length > 0` return `{ ok: false, issues }` WITHOUT any write. Cast the validated archive to JsonArchive. For 'replace': `await db.transaction('rw', db.client, db.cells, db.core, db.moduleInstances, db.routes, db.sessions, db.operations, db.settings, db.forgeHistory, async () => { await Promise.all([db.client.clear(), db.cells.clear(), ... all nine ...]); await db.cells.bulkPut(archive.cells); ... etc; })`. For 'merge': same transaction scope but no clear; for each collection upsert by ID reusing the idempotent-upsert conflict rule (get -> JSON.stringify compare -> skip on equal, throw a session_conflict/operation_conflict PersistenceError on mismatch; non-append stores use write_failure on mismatch). Wrap in try/catch mapping DOMException via mapDomException to `{ ok: false, error }`. Return `{ ok: true, stats }` with counts from the archive. Update src/persistence/index.ts to `export * from './import-validation.js'; export * from './import.js';`. Header comment cites D-11 (replace default, merge opt-in) and D-12 (validate-before-write).</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/persistence/import.ts exports `importArchive`, `ImportResult`, `ImportStats`, `ImportMode`.
    - `importArchive` calls `validateArchive` before any `db.transaction`.
    - When `validateArchive` returns issues, `importArchive` returns `{ ok: false, issues }` and performs no `db.transaction` call.
    - Replace mode calls `.clear()` on all nine tables then `bulkPut`; merge mode calls no `.clear()`.
    - import.ts has no `../simulation` import.
    - src/persistence/index.ts re-exports `./import-validation.js` and `./import.js`.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Import/restore validates all-or-nothing then executes replace (wipe+write) or merge (per-record upsert) atomically with typed errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement reusable synthetic migration-fixture harness</name>
  <files>tests/persistence/migration-harness.ts, tests/persistence/migration-harness.test.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-CONTEXT.md
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    src/domain/records.ts
    tests/helpers/fixtures.ts
    src/persistence/database.ts
  </read_first>
  <behavior>
    - The harness is a generic fixture runner that proves an upgrade function transforms an old-shape record into the new shape.
    - It is structured so the real v1->v2 migration (when it arrives) reuses the same runner by adding a real fixture.
    - No forward-looking v2 stub Dexie schema is shipped.
  </behavior>
  <action>Create tests/persistence/migration-harness.ts exporting `export interface MigrationFixture<OldShape, NewShape> { readonly description: string; readonly input: OldShape; readonly upgrade: (record: OldShape) => NewShape; readonly expected: NewShape; }` and `export function runMigrationFixture<OldShape, NewShape>(fixture: MigrationFixture<OldShape, NewShape>): void` that wraps a Vitest `it(fixture.description, () => { expect(fixture.upgrade(fixture.input)).toEqual(fixture.expected); })`. The module imports `it, expect` from 'vitest'. Create tests/persistence/migration-harness.test.ts that imports `runMigrationFixture` and registers at least one synthetic fixture: an old-shape object using a field like `{ id, milestoneProgress: 900 }` (seconds) and an upgrade function that returns `{ id, dailyMilestoneProgressSeconds: 900 }`, asserting the rename. Add a second fixture proving a defaulted-field addition (old shape missing a field -> upgrade fills a default). The test file header must state "designed for reuse by the real v1->v2 migration" per CONTEXT D-07.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm run test -- --run tests/persistence/migration-harness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/persistence/migration-harness.ts exports `MigrationFixture` and `runMigrationFixture`.
    - tests/persistence/migration-harness.test.ts exits 0 and registers at least two synthetic fixtures (a field rename and a defaulted-field addition).
    - No `db.version(2)` declaration exists anywhere in src/ (grep-verified: src/persistence/database.ts still declares only version(1)).
    - `npm run typecheck`, `npm run lint`, and the harness test exit 0.
  </acceptance_criteria>
  <done>A reusable migration-fixture harness proves field-transform upgrades synthetically without shipping a v2 schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Import/restore VER-03 tests — validation rejection modes, replace, merge, atomicity</name>
  <files>tests/persistence/import-validation.test.ts, tests/persistence/import-replace.test.ts, tests/persistence/import-merge.test.ts</files>
  <read_first>
    .planning/phases/02-durable-local-first-spine/02-RESEARCH.md
    .planning/phases/02-durable-local-first-spine/02-PATTERNS.md
    tests/helpers/fixtures.ts
    tests/simulation/foundation-loop.test.ts
    src/persistence/import-validation.ts
    src/persistence/import.ts
    src/persistence/export-json.ts
    src/persistence/database.ts
    src/persistence/repository.ts
    src/domain/validation.ts
    src/simulation/engine.ts
  </read_first>
  <behavior>
    - import-validation.test covers every D-12 rejection mode (malformed shape, negative resource, broken reference, route sum > 100, core allocation != 100) and the all-clear case, asserting no writes occur.
    - import-replace.test proves replace wipes local data and writes the archive atomically (state B fully replaces state A); a forced mid-write failure leaves no partial state.
    - import-merge.test proves merge upserts by ID without wiping (untouched local records survive) and that a payload-mismatch conflict surfaces a PersistenceError.
  </behavior>
  <action>Create the three test files using the same fake-indexeddb reset + unique DB name setup as 02-01 Task 6. Build archives via exportJson after applying known results. tests/persistence/import-validation.test.ts: start from a valid archive (exportJson of an applied-result state); call `validateArchive(archive)` -> `issues.length === 0`. Then mutate copies and assert each rejection: (a) malformed shape — `validateArchive({ ...archive, archiveVersion: 99 })` or delete a required field -> an issue with `code: 'invalid_operation_shape'`; (b) negative resource — set a cell's `xp` to -1 -> `negative_resource`; (c) broken reference — set a session's `cellId` to a non-existent id -> `invalid_reference`; (d) route sum > 100 on a shared sourceCell -> `invalid_route_allocation`; (e) core convert+store != 100 -> `invalid_core_allocation_total`. For each, assert `validateArchive` returns the expected code AND assert no local data changed (snapshot the DB before and after calling validateArchive — it performs no writes by construction). tests/persistence/import-replace.test.ts: seed state A (apply one result), build archive B from a different applied result; `await importArchive(db, archiveB, 'replace')`; `const loaded = await repo.loadSnapshot();` assert loaded deep-equals B and no A-only records survive. Atomicity: stub one table's bulkPut to throw mid-transaction, run replace, then reload and assert the DB is either fully A or fully B (no partial mix). tests/persistence/import-merge.test.ts: seed state A; build an archive containing one new cell and one updated cell not present in A; `await importArchive(db, partialArchive, 'merge')`; assert A's untouched cells/sessions survive and the new+updated cells are present. Conflict case: build an archive cell with the same id as an A cell but a different `xp`; `importArchive(..., 'merge')` -> result `ok: false` with a `PersistenceError` (or appropriate conflict kind).</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm run test -- --run tests/persistence/import-validation.test.ts tests/persistence/import-replace.test.ts tests/persistence/import-merge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/persistence/import-validation.test.ts exits 0 and asserts the all-clear plus all five rejection codes (invalid_operation_shape, negative_resource, invalid_reference, invalid_route_allocation, invalid_core_allocation_total) and that validation writes nothing.
    - tests/persistence/import-replace.test.ts exits 0 and proves B fully replaces A and that a forced mid-transaction failure leaves no partial state.
    - tests/persistence/import-merge.test.ts exits 0 and proves untouched records survive, new/updated records upsert, and a payload mismatch surfaces a PersistenceError.
    - `npm run typecheck`, `npm run lint`, and `npm run test -- --run` all exit 0 together.
  </acceptance_criteria>
  <done>The import/restore tier of VER-03 passes: all D-12 rejection modes, replace atomicity, and merge conflict behavior are verified.</done>
</task>

</tasks>

## Artifacts this phase produces

- Validation: `archiveSchema` (+ per-record schemas), `validateArchive`.
- Import: `importArchive`, `ImportResult`, `ImportStats`, `ImportMode`.
- Migration harness: `MigrationFixture`, `runMigrationFixture`.
- Barrel additions: `src/persistence/index.ts` re-exports `./import-validation.js` and `./import.js`.
- Tests: `tests/persistence/migration-harness.{ts,test.ts}`, `tests/persistence/import-validation.test.ts`, `tests/persistence/import-replace.test.ts`, `tests/persistence/import-merge.test.ts`.

<must_haves>
- DATA-06: import/restore validates schema version, required records, references, and resource invariants all-or-nothing before changing local data; every D-12 rejection mode is covered by a test.
- DATA-07: the typed PersistenceError contract distinguishes recoverable (quota_exceeded, persistence_denied, blocked_upgrade) from non-recoverable (indexeddb_unavailable) states (contract delivered in 02-01; import surfaces it via mapDomException here).
- VER-03: import validation, replace/merge restore, and migration fixtures are covered by persistence tests.
</must_haves>

<verification>
Run `npm run typecheck`, `npm run lint`, and `npm run test -- --run`. The import-validation, import-replace, import-merge, and migration-harness suites must pass alongside all existing tests, with no v2 stub schema shipped.
</verification>

<success_criteria>
Flowgrid restores only archives that pass Zod shape parsing and Phase 1 invariant validation, executes replace or merge atomically, surfaces typed recoverable/non-recoverable storage errors, and ships a migration harness the real v1->v2 migration can reuse.
</success_criteria>

<output>
Create `.planning/phases/02-durable-local-first-spine/02-03-SUMMARY.md` when done.
</output>
