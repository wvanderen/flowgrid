// Phase 1 starter content version.
//
// Starter ModuleDefinitions are versioned content. Bump this when starter module
// shapes change in a way that requires a content migration or a re-balanced
// starter state.

import type { ContentVersion } from '../domain/index.js';

export const STARTER_CONTENT_VERSION: ContentVersion = 'flowgrid:starter:v1';
