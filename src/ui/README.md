# `src/ui` — UI Components (Phase 1 boundary)

Phase 1 defers UI. This folder is a placeholder for the future React/Radix component layer described in Phase 3+ of the roadmap.

UI dispatches commands and displays selectors; it does not calculate economy rules. Phase 1 simulation must not import from this layer, and must not import `react`, `react-dom`, or `zustand`.
