// Plan 03-02 Task 1 placeholder FlowgridHome. This stub exists so the router
// (src/app/routes.tsx) and App shell can typecheck and mount before Task 2 ships
// the real Pixi canvas, store selectors, and accessible heading. Task 2 (TDD RED →
// GREEN) replaces this stub wholesale with the FlowgridHome component that reads
// the store, renders the loading state, and mounts FlowgridCanvas.

export function FlowgridHome() {
  return <section aria-label="Flowgrid placeholder">Flowgrid home (placeholder)</section>;
}
